# Wake listener config notes (runtime uses env + phrases file primarily).
# Mode: phrase (default) | openwakeword | energy
# Phrases: /etc/aipc/wake/phrases
mode: phrase
model_path: /var/lib/aipc-voice/wake/user-model.onnx
