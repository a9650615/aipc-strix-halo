# Stamped at image build time by tools/aipc render bootc.
# These are PLACEHOLDER values replaced by post-install.sh using
# $AIPC_IMAGE_REF and $AIPC_BUILD_DATE injected by the renderer.
IMAGE_REF=PLACEHOLDER
BUILD_DATE=PLACEHOLDER
