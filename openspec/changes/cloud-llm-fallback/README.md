# cloud-llm-fallback

Cloud LLM fallback: Anthropic, OpenAI, and Google models routed through the existing LiteLLM gateway via `-cloud` suffixed aliases. Manual switching only, SOPS-encrypted API keys.
