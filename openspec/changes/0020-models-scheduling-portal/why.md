# Why — models-scheduling-portal

Operators need a single Control Center surface that explains **why** models are
loaded, held warm, or would be unloaded — SMO admission gates, lemonade
idle-release keep-warm, and OOM-guard events — without reading journals and
YAML by hand.
