# Tasks — peer-mcp-progress-observability

- [ ] 1.1 Define the peer MCP event schema, bounded journal writer, fold logic,
  and user-only file handling.
- [ ] 1.2 Instrument the shared peer subprocess runner with incremental output,
  heartbeats, terminal events, and stop/timeout handling.
- [ ] 1.3 Add `peer_agent_runs` and preserve the existing ask/stop contracts.
- [ ] 1.4 Add portal journal aggregation and stale-PID handling to the agents
  snapshot.
- [ ] 1.5 Add the Peer MCP dashboard section and detail rendering.
- [ ] 1.6 Add focused static tests for MCP runtime, portal API, and SPA shape.
- [ ] 1.7 Run OpenSpec strict validation and both render targets; document the
  live MCP sync and hardware verification boundary.
