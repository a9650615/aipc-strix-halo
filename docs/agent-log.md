# Agent Log

Append-only chronological record of every agent run on this repo. Lets a 大哥 review who did what and catch unattributed work. See `CLAUDE.md §11` for the schema.

Columns:

- **date** — ISO-8601, the day the run started (Asia/Taipei).
- **role** — 大哥 / 副官 / 執行兵.
- **model** — model id used; if a 大哥 dispatched subagents, list them too (e.g. `opus → sonnet x3`).
- **run label** — matches `Agent-Run:` trailer.
- **spec task(s)** — `<change>#<id>` references; `n/a` for repo-level work.
- **sha range** — `<first-sha>..<last-sha>` of the run's commits.
- **outcome** — one line.

| date | role | model | run label | spec task(s) | sha range | outcome |
|---|---|---|---|---|---|---|
| 2026-06-27 | 大哥 | opus-4.7 | brainstorm-architecture | n/a (pre-spec; produced `docs/architecture.md`) | n/a | 7-phase / 44-module design locked with user |
| 2026-06-27 | 大哥 | opus-4.7 | phase-0-spec-write | phase-0-foundation (all 4 artifacts) | n/a (uncommitted) | proposal + design + spec + tasks written, `openspec validate` passes |
| 2026-06-27 | 副官 x3 | opus → sonnet x3 (parallel) | phase-0-scaffold-modules-cli-bootstrap | phase-0-foundation#1.1-1.5, #3, #4.1-4.10, #5.1-5.4 | 5a57bb6 (initial), tools/+modules/+bootstrap/ci all in | 15 pytests pass, shellcheck+yamllint clean, smoke test green |
| 2026-06-27 | 大哥 | opus-4.7 | phase-0-cli-base-default-fix | phase-0-foundation#4.9 | (rolled into 5a57bb6) | `_DEFAULT_BASE` aligned to `ghcr.io/ublue-os/bazzite-dx:stable` |
| 2026-06-27 | 執行兵 | (out-of-band agent, unattributed) | git-push-and-secrets-setup | phase-0-foundation#1.6, #2.1-2.7 | 5a57bb6..71e58a2..f4e49dc | repo pushed to `a9650615/aipc-strix-halo`; SOPS+age docs written |
| 2026-06-27 | 副官 | (out-of-band agent, unattributed) | phase-1-modules-scaffold (PROTOCOL VIOLATION) | none — was off-spec at the time | ..2f6f6d3 | added 4 llm-* modules **without an OpenSpec change**, retroactively legalised below |
| 2026-06-28 | 大哥 | opus-4.7 | phase-0-build-fix | phase-0-foundation (build green) | 76eb854..4d5f112..546c1b4 | 5-round CI debug: amd-smi drop, sops via upstream binary, COPY post-install pattern, /usr/lib relocation (bootc /usr/local is symlink to /var), kargs.d TOML (bootc has no kargs subcommand). First `ghcr.io/a9650615/aipc:rolling` published. |
| 2026-06-28 | 副官 | sonnet | phase-1-spec-retro | phase-1-ai-runtime (all 4 artifacts) | b7ecd58 | OpenSpec change written to retroactively cover the 4 pre-existing llm-* modules; 5 gap tasks identified (R3 keep-alive, R4 vLLM eviction, R5 models.yaml, R6/R7 ai-rocm/ai-xdna missing, R8 doctor alias check) |
| 2026-06-28 | 大哥 | opus-4.7 | phase-1-modules-disable-gate | n/a (CI un-block) | f47a8c9 | 4 llm-* modules marked `.disabled`; renderer skips them. Removes Phase 1 build-time bugs from Phase 0 image; legit Phase 1 fix work tracked as tasks 1.x. |
| 2026-06-28 | 大哥 | opus-4.7 | claude-md-roles-and-attribution | n/a (process) | 9f3f425 | CLAUDE.md §0 Roles + §11 Attribution added; this log seeded with prior runs back-filled |
| 2026-06-28 | 副官 | Qwen3.7-max | phase1-lemonade-port-fix-2026-06-28 | phase-1-ai-runtime#1.4 | b3a14ec | verify.sh default port corrected 8000→8001 to match quadlet |
| 2026-06-28 | 副官 | claude-sonnet-4-6 | phase-0-review-fix-2026-06-28 | phase-0-foundation#R4, phase-1-ai-runtime#R3 | cb7b0ec..8e1c61a | ansible renderer kargs parity (TOML copy task replaces bogus bootc subcommand); regression guard added; HSA_OVERRIDE_GFX_VERSION spec typo 11.0.0→11.5.1 |
| 2026-06-28 | 副官 | claude-sonnet-4-6 | claude-md-orchestrator-trailer-2026-06-28 | n/a (repo process) | 468df50 | CLAUDE.md §11: optional Agent-Orchestrator trailer added (template line + explanatory bullet + two examples); dispatched by claude-opus-4-7 |
| 2026-06-28 | 副官 | Qwen3.7-max | phase1-batch-2026-06-28 | phase-1-ai-runtime#1.2,1.3,1.8,1.10,2.1,2.2,2.3,3.1,3.2,3.3,4.1,4.2 | b3c7d58 | 3 new modules (ai-rocm, ai-xdna, llm-models), LiteLLM digest pin + timeout, OLLAMA_KEEP_ALIVE=-1, doctor exit-2→OPTIONAL + gateway alias check, aipc models list/sync --check. 24 tests pass. |
| 2026-06-28 | 大哥 | opus-4.7 | phase1-rollback-note-2026-06-28 | phase-1-ai-runtime#2.1,2.2,2.3 | 44f6d51 | partial rollback of phase1-batch: 3 new modules marked .disabled — ai-rocm dnf-at-buildtime, ai-xdna missing modprobe.d/ file, llm-models depends on unimplemented `aipc models sync` |
| 2026-06-28 | 副官 | claude-sonnet-4-6 | phase1-drift-fix-2026-06-28 | phase-1-ai-runtime#R5,3.4 | 1d36edb | aligned models.yaml ↔ litellm config.yaml model_ids on the specific-tag side; opened task 3.4 for derived-config render; CLAUDE.md §8 documents exit-2 OPTIONAL convention |
| 2026-06-28 | 副官 | claude-sonnet-4-6 | phase1-modules-cleanup-2026-06-28 | phase-1-ai-runtime#2.1,2.2 | 951d3f3 | structural fix for ai-rocm (yum.repos.d/rocm.repo as data, post-install only installs) and ai-xdna (modules-load.d + udev.rules.d as data, packages.txt emptied pending out-of-tree repo); both remain .disabled until AI PC validates |
| 2026-06-29 | 副官 | Qwen3.7-max | phase0-build-local-vfs-2026-06-29 | phase-0-foundation#5.6 | 760a987 | build-local.sh: buildah --storage-driver vfs (rpm-ostree fails on Docker overlay2) |
| 2026-06-29 | 副官 | Qwen3.7-max | cloud-llm-fallback-2026-06-29 | cloud-llm-fallback#1.1-2.3 | b88bf68 | 5 cloud aliases (Anthropic/OpenAI/Google), SOPS encrypted keys → EnvironmentFile, OpenSpec change. Dispatched via sonnet x2. |
| 2026-06-29 | 副官 | claude-sonnet-4-6 | cloud-llm-runtime-fix-2026-06-29 | cloud-llm-fallback (runtime decrypt + model IDs) | fb0f54d (+ this log-fix commit) | replaced dead build-time decrypt with systemd oneshot (aipc-decrypt-cloud-keys.service, ConditionPathExists, optional gracefully); updated cloud model IDs to claude-sonnet-4-6 / claude-opus-4-8 in litellm config, models.yaml, design.md; dispatched by claude-opus-4-7 |
| 2026-06-29 | 大哥 | claude-opus-4-7 | build-local-disk-fix-2026-06-29 | phase-0-foundation#5.6 | 4867c78 | build-local.sh: dropped --layers (vfs amplifies it), added `buildah rmi --prune` after build, KEEP_CACHE=1 opt-in, prints volume size at end. User disk had hit ENOSPC from runaway cache; volume manually purged. |
| 2026-06-29 | 副官 | claude-sonnet-4-6 | phase-6-spec-write-2026-06-29 | phase-6-dev (all 4 artifacts) | e5c08fb | OpenSpec change phase-6-dev written: proposal/design/specs/tasks for 10 modules (Zed primary + VSCode secondary, 6 AI tools all preinstalled via LiteLLM, fish+starship+Ghostty, Node+Python distrobox, Filesystem/GitHub/Playwright MCP via Phase 4 gateway). Open Q on Phase 4 dependency for MCP wiring. Dispatched by claude-opus-4-7. |
| 2026-06-29 | 副官 | claude-sonnet-4-6 | phase-4-spec-write-2026-06-29 | phase-4-agent (all 4 artifacts) | ae40146 | OpenSpec change phase-4-agent written: 8 modules, LangGraph multi-agent supervisor + 4 sub-agents (Researcher/Coder/Browser/Daily Assistant), distrobox sandbox, Playwright MCP browser, SearXNG+Tavily search, Google+Proton/Fastmail cal/mail, screen-control session-gate + always-on opt-in, shared MCP registry config (no daemon), HTTP /chat voice interface. 4 Open Qs flagged. Dispatched by claude-opus-4-7. |
| 2026-06-29 | 副官 | claude-sonnet-4-6 | phases-3-2-5-7-spec-write-2026-06-29 | phase-3-voice (all 4 artifacts) | (this commit) | OpenSpec: 6 modules, STT+LLM default path (multimodal-first as v2), custom wake word + NPU always-on, CosyVoice 2 voice clone, SenseVoice+Paraformer routed STT, Super+Space PTT, screen-lock/DND/mute listen-off, router-1b cmd-vs-chat. 3 Open Qs. Dispatched by claude-opus-4-7. |
| 2026-06-29 | 副官 | claude-sonnet-4-6 | phases-3-2-5-7-spec-write-2026-06-29 | phase-2-memory (all 4 artifacts) | (this commit) | OpenSpec: 5 modules, pgvector primary + Qdrant opt-in, mem0 framework, all 4 RAG sources default-on (browser+screen consent-gated), bge-m3 + reranker, local-only data plane. 3 Open Qs. |
| 2026-06-29 | 副官 | claude-sonnet-4-6 | phases-3-2-5-7-spec-write-2026-06-29 | phase-5-gaming (all 4 artifacts) | (this commit) | OpenSpec: 3 modules, gamescope installed (not default-entered), Steam primary + Heroic opt-in, voice+overlay both shipped, strategy-RAG framework (no sources baked), Pipecat survives gaming mode. 3 Open Qs. |
| 2026-06-29 | 副官 | claude-sonnet-4-6 | phases-3-2-5-7-spec-write-2026-06-29 | phase-7-ops (all 4 artifacts) | (this commit) | OpenSpec: 3 modules, snapper 7d/4w/3m + pre-update, doctor aggregates GPU+NPU+services, 3-screen firstboot (persona/editor/cloud-keys), weekly update + consent (no force), :stable default + :rolling opt-in, no telemetry, aipc restore CLI. 3 Open Qs. |
| 2026-06-30 | 副官 | Qwen3.7-max | aipc-log-append-tool-2026-06-30 | n/a (repo tooling) | (this commit) | added 'aipc log append' subcommand + tests; prevents sed-based log corruption that happened twice prior |
| 2026-06-30 | 副官 | Qwen3.7-max | phase-6-scaffold-2026-06-30 | phase-6-dev#1.1-1.10 | (this commit) | scaffolded all 10 Phase 6 modules with .disabled markers (dev-cli/editors/distrobox-templates + 7 dev-ai-*); render skips all of them; pytest green |
| 2026-06-30 | 副官 | Qwen3.7-max | phase-2-scaffold-2026-06-30 | phase-2-memory#1.1-1.5 | (this commit) | scaffolded 5 Phase 2 memory+RAG modules (db-postgres/qdrant, rag-embedder/ingest, memory-mem0) all .disabled; quadlets + skel configs + verify.sh exit-2 pattern; pytest green |
| 2026-06-30 | 大哥 | claude-opus-4-7 | skill-dispatch-qwen-task-2026-06-30 | n/a (repo tooling) | (this commit) | added .claude/skills/dispatch-qwen-task/SKILL.md — 3-phase flow (Review audit checklist → Write spec with RED FLAGS → Simple zh-TW dispatch line). Captures recurring Qwen failure modes so future sessions don't accumulate tech debt. |
| 2026-06-30 | 副官 | claude-sonnet-4-6 | phase-4-scaffold-2026-06-30 | phase-4-agent#1.1-1.8 | 86c8dd9 | scaffolded 8 agent modules (orchestrator, code-shell, browser, screen-control, tools-files, tools-calendar, tools-search, mcp-gateway) all .disabled; 48 files, 28 tests pass, render clean, RED FLAG scan clean |
| 2026-06-30 | 副官 | claude-sonnet-4-6 | phase-3-scaffold-2026-06-30 | phase-3-voice#1.1-1.6 | ac6eb22 | scaffolded 6 voice modules (pipecat, wake, stt-sensevoice, stt-paraformer, tts-cosyvoice, tts-kokoro) all .disabled; includes aipc-voice-mute.target; 39 files, 28 tests pass, render clean, RED FLAG scan clean |
| 2026-06-30 | 副官 | claude-sonnet-4-6 | phase-5-7-scaffold-2026-06-30 | phase-5-gaming#1.1-1.3, phase-7-ops#1.1-1.3 | 3382767 | scaffolded 6 gaming+ops modules (gaming-base, gaming-ai-overlay, game-strategy-rag, ops-backup, ops-doctor, ops-firstboot) all .disabled; 39 files, 28 tests pass, render clean, RED FLAG scan clean |
| 2026-06-30 | 副官 | claude-sonnet-4-6 | install-windows-direct-spec-2026-06-30 | install-windows-direct (all 4 artifacts) | 4447eba | OpenSpec change install-windows-direct created and validated --strict; no-USB install path from Windows 11 via rEFInd ISO chainload; modifies system-foundation R6 + adds R8 WinRE recovery. |
| 2026-07-01 | 大哥 | claude-opus-4-7 | skill-lieutenant-2026-07-01 | n/a (repo tooling) | (this commit) | added .claude/skills/lieutenant/SKILL.md — 副官-side companion to dispatch-qwen-task. Encodes self-identification, RED FLAG internalization, build/runtime split, pre-commit verification gates, aipc-log-append + push protocol. Updated dispatch-qwen-task Phase 3 to reference it. |
| 2026-07-01 | 副官 | GLM5.2 | db-postgres-fix-2026-07-01 | phase-2-memory#1.1 (db-postgres fix) | (this commit) | refactored db-postgres: removed systemctl --now/nc -z/psql build-time calls; relocated SQL out of /usr/local; added aipc-pg-init.service runtime oneshot with ConditionPathExists sentinel; .disabled stays pending hardware verify |
| 2026-07-01 | 副官 | GLM5.2 | mem0-buildtime-fix-2026-07-01 | phase-2-memory#6.1 (memory-mem0 build-time fix) | (this commit) | memory-mem0 post-install rewritten build-time-only: dropped systemctl --now + curl /healthz loop (mem0 container self-inits via DATABASE_URL; readiness is verify.sh's job). No oneshot needed. Flagged pre-existing quadlet-install gap (renderer doesn't COPY quadlet/, 5 modules affected) in README; .disabled stays |
| 2026-07-01 | 副官 | claude-sonnet-4-6 | rag-embedder-buildtime-fix-2026-07-01 | phase-2-memory#1.3 (rag-embedder build-time fix) | (pending commit) | Fixed rag-embedder post-install build-time/runtime bug: dropped --now + curl /healthz loop, container self-inits, .disabled stays, quadlet gap documented |
| 2026-07-01 | 副官 | GLM4.7 | install-windows-direct-runbook-2026-07-01 | install-windows-direct#1.1-1.2,2.1-2.2 | 9deb514 | Added runbook and architecture §9 Alt section for Windows-direct install path (no USB required) |
| 2026-07-01 | 副官 | GLM4.7 | dev-module-arch-fix-2026-07-01 | phase-6-dev (build-time=rpm-only arch fix) | (this commit) | Enforced build-time=rpm-only across dev-cli/dev-editors/dev-ai-cline/dev-ai-continue: font pkg rename, dropped build-time code/flatpak/ext-install (moved to runtime). GLM4.7 teammates authored edits (429'd before returning verdict); GLM5.2 verified RED FLAG clean + render + shellcheck |
| 2026-07-01 | 大哥 | GLM5.2 | dev-modules-enable-2026-07-01 | phase-6-dev (enable 9 modules) | (this commit) | Enabled 9 Phase 6 dev modules into image (dev-cli/editors/distrobox-templates/aider/claude-code/opencode/goose/cline/continue); mcp-dev-servers stays disabled (stub, needs phase-4-agent). Render-verified (30 rpm pkgs). Hardware/runtime verify on AI PC remains user-side. Build kicked off in background (bjz4rd3um) |
| 2026-07-01 | 大哥 | claude-opus-4-7 | cloud-llm-fallback-spec-delta-fix-2026-07-01 | cloud-llm-fallback (openspec strict validity) | (this commit) | Added missing specs/ai-runtime/spec.md with MODIFIED (R1 gateway) + ADDED (runtime cloud-key decrypt + config-only mappings) delta blocks and Scenarios. Change now passes openspec validate --strict. Historical bug: initial b88bf68 shipped without deltas; nobody ran strict validate on this change until 2026-07-01 audit. |
| 2026-07-01 | 大哥 | claude-opus-4-7 | ci-red-fix-2026-07-01 | phase-6-dev (dev-cli/dev-editors), n/a (test lint) | (this commit) | Build Image was failing on `lazygit`/`starship` (not in bazzite-dx repos) — moved to README as runtime install via curl/go. Deduped fonts (dev-cli owns; dev-editors packages.txt now header-only). CI (Ruff) was failing on F401 unused ROLES import + 2×E741 ambiguous `l` in test_log_append.py — cleaned. Both workflows unblocked. |
| 2026-07-01 | 大哥 | claude-opus-4-7 | ci-yamllint-fix-2026-07-01 | phase-6-dev (dev-distrobox-templates) | (this commit) | yamllint red on distrobox INI files being parsed as YAML — renamed node.yaml/python.yaml → node.ini/python.ini (distrobox-assemble uses INI syntax, not YAML). README updated accordingly. |
| 2026-07-01 | 大哥 | GLM5.2 | glm-ledger-2026-07-01 | phase-6-dev (GLM handoff ledger) | (this commit) | Wrote docs/glm-handoff-2026-07-01.md for Claude(opus) review: full attribution map (GLM5.2 vs GLM4.7), f3ffc7c mis-tag correction, arch decisions, deferred issues (quadlet gap, mcp stub, ansible lint). Build attempted but NOT completed (>1hr stuck on base image pull, env-limited); render-verified; CI/AI-PC remains authoritative gate |
| 2026-07-01 | 大哥 | GLM5.2 | glm-ledger-2026-07-01 | phase-6-dev (build-green confirm) | (this commit) | Confirmed via gh: CI Build Image GREEN on 7304abd (build-push 5m46s) and 4cde9b5 (4m26s, includes a9aceb1 dev-module enable). 30 rpm packages resolve, 43 steps complete, image pushed to ghcr.io/a9650615/aipc. Corrected ledger build-status from 'NOT completed' to CI-green. Hardware bootc switch + aipc doctor on AI PC remains user-side. |
| 2026-07-01 | 大哥 | claude-opus-4-8 | quadlet-rename-fix-2026-07-01 | n/a (correctness fix) | (this commit) | renamed 7 misnamed quadlet source files .service→.container (files had [Container] section; podman-quadlet only processes .container). Fixed 4 README source-path refs. verify.sh runtime-unit names (X.service = generated unit) correctly left as-is. From GLM ledger #1 audit. |
| 2026-07-01 | 大哥 | claude-opus-4-8 | quadlet-render-spec-2026-07-01 | quadlet-render-support (all 4 artifacts) | (this commit) | OpenSpec change: fix both renderers dropping module quadlet/ (bootc misses quadlet; ansible misses modprobe.d+env+quadlet — §4 parity violation). Dest /etc/containers/systemd/; drop llm-litellm --user + llm-lemonade/rag-ingest hand-installs; quadlet/=*.container only, plain units→files/; add render-parity test. To dispatch to GLM. |
| 2026-07-01 | 大哥 | GLM5.2 | phase6-dev-tasks-align-2026-07-01 | phase-6-dev (tasks.md reality align) | (this commit) | Aligned phase-6-dev/tasks.md to shipped reality: 20/47 done (was 0/47). Checked 1.1-1.9 scaffolding + 2.1-2.3 editor configs + 3.1-3.3 CLI configs + 4.1 chsh + 5.1-5.2 distrobox + 8.1 READMEs + 9.1 render (all verified against module files). Open items tagged by reason: descoped (4.2/4.3), blocked-on-Phase-4 (1.10/6.x), not-implemented (7.x/8.2), hardware (4.4/9.3/10.x). 3.4 partial (skel settings.json vs profile.d). Confirmed opus's 00d0d5a dedup was correct (shared tools in system-base). |
| 2026-07-01 | 副官 | GLM5.2 | quadlet-render-impl-2026-07-01 | quadlet-render-support#1-6 | (this commit) | Implemented quadlet-render-support: both renderers COPY quadlet/ -> /etc/containers/systemd/ (+ ansible parity on modprobe.d/env). Moved 6 plain systemd .service out of quadlet/ (lemonade + 5 rag-ingest) to files/etc/systemd/system/. Dropped llm-litellm --user workaround. quadlet/ now .container-only. +test_render_parity (28->29). All 15 modules stay .disabled. |
| 2026-07-01 | 大哥 | gpt-5.5 | windows-oneclick-installer-2026-07-01 | windows-oneclick-installer#1.1-2.4 | (this commit) | Added Windows one-click installer scripts and closed review gaps; verified pytest 29 passed, bootc render, and OpenSpec validation. |
| 2026-07-01 | 大哥 | gpt-5.5 | windows-root-entrypoints-2026-07-02 | windows-oneclick-installer#1.4-1.5 | (this commit) | Added repo-root Windows and Linux install entrypoints; workflow-reviewed UX/safety, verified OpenSpec, pytest, bootc render, and entrypoint syntax. |
| 2026-07-01 | 大哥 | gpt-5.5 | guided-installer-tui-spec-2026-07-02 | guided-installer-tui#proposal | (this commit) | Created guided installer TUI OpenSpec proposal/design/spec/tasks; validated strict. |
| 2026-07-02 | 副官 | claude-opus-4-8 | guided-installer-tui-impl-2026-07-02 | guided-installer-tui | 817ef97..8c2fe56 | implemented guided keyboard menu for Windows and Linux entrypoints; all 12 spec tasks complete; validation green |
| 2026-07-02 | 副官 | claude-opus-4-8 | installer-fallback-debug-2026-07-02 | guided-installer-tui | 16b3990 | added phase tracking, failure recovery summaries, USB SSD installer path, and debug info to both Windows and Linux entrypoints |
| 2026-07-02 | 副官 | claude-opus-4-8 | installer-bugfix-2026-07-02 | guided-installer-tui | 96a2e4f | fixed USB SSD ESP (FAT32 boot partition), BOOTX64 typo, runbook FAT32 to exFAT, removed dead UsbMode param |
| 2026-07-02 | 副官 | claude-fable-5 | installer-consistency-scan-2026-07-02 | - | c0dd4cb | consistency scan: fixed stale USB SSD single-partition docs in README + targets/windows/README |
| 2026-07-02 | 副官 | claude-fable-5 | install-windows-direct-docs-2026-07-02 | install-windows-direct#3.1-7.2 | e12e9bd | completed doc tasks, synced spec/design to exFAT+extract |
| 2026-07-02 | 副官 | claude-fable-5 | multi-agent-rules-2026-07-02 | - | fc34549 | added §0.1 model-tier roles + §0.2 dispatch protocol to CLAUDE.md/AGENTS.md |
| 2026-07-02 | 副官 | claude-fable-5 | quadlet-render-2026-07-02 | quadlet-render-support#1-6 | 449bfe5 | sonnet 副官 verified prior impl, checked off all tasks (orchestrated by fable-5) |
| 2026-07-02 | 副官 | claude-fable-5 | aipc-models-cmd-2026-07-02 | phase-1-ai-runtime#3.1,3.2,3.3 | 74125ec | sonnet 副官 fixed models manifest schema bug, implemented sync pull (orchestrated by fable-5) |
| 2026-07-02 | 副官 | claude-fable-5 | litellm-pin-idle-2026-07-02 | phase-1-ai-runtime#1.2,1.3 | b026441 | sonnet 副官 verified digest pin, documented idle-eviction gap: needs llm-vllm systemd fix (orchestrated by fable-5) |
| 2026-07-02 | 副官 | claude-fable-5 | preboot-prep-2026-07-02 | - | 1107d6a..e5a5df7 | fixed dead ISO URL (no dx ISO upstream), repo placeholders, curl-pipe bootstrap, USB 16GiB guidance |
| 2026-07-02 | 大哥 | claude-fable-5 | preboot-kit-2026-07-02 | phase-0-foundation#6.3,8.1,9.4 | 24d4e10..4bb4318 | pre-boot kit ready: ISO downloaded+verified, USB writer script, upgrade-day checklist, age.pub exported, CI/image confirmed |
| 2026-07-03 | 大哥 | claude-opus-4-8 | windows-installer-bugfix-2026-07-03 | windows-oneclick-installer#1.1,1.2 | 2e3f59f | root-caused 6 defects in untested Windows staging path: preflight exit code, Run-Preflight array pollution, disk .Count null, rEFInd download URL+checksum+install; preflight & download verified on target, bcdedit step not hw-verified |
| 2026-07-03 | 大哥 | claude-opus-4-8 | windows-installer-bugfix-2026-07-03 | windows-oneclick-installer#1.1 | 490fda7 | no-USB payload was designed for a dracut live ISO but Bazzite ships Anaconda: switched AIPC_LIVE exFAT→FAT32, mirror full installer tree, menuentry root=live→inst.stage2; verified ISO layout+grub.cfg+file sizes on target, synced design/spec/tasks/README; USB path still carries same LiveOS bug |
| 2026-07-03 | 大哥 | claude-opus-4-8 | windows-installer-bugfix-2026-07-03 | windows-oneclick-installer#1.1 | 874ad1f | full hardware staging run (no reboot) found 2 more bugs: Ensure-FreeSpace re-prompted to shrink forever once AIPC_LIVE existed (fixed with reserved-space accounting + 1GiB slack), ESP too small (168MiB free) for Bazzite's 242MiB initrd (fixed: load vmlinuz/initrd from AIPC_LIVE via rEFInd volume directive instead of ESP copy); also manually repartitioned the target disk on user request (deleted+recreated AIPC_LIVE to merge fragmented unallocated space, extended shrink for 500GiB contiguous Linux space); staging verified end-to-end, all 6 phases [ok]; boot-to-Strix-Halo itself still UNVERIFIED |
| 2026-07-03 | 副官 | claude-sonnet-5 | strix-halo-panel-replay-freeze-fix-2026-07-03 | n/a (direct hardware bugfix) | 478a3c5 | Root-caused internal-display freeze on ROG Flow Z13 (GZ302EA): amdgpu Panel Replay WARNING in dmub_replay_enable on 180Hz eDP+VRR stalls compositor commit. Fixed via amdgpu.dcdebugmask=0x400 (DC_DISABLE_REPLAY) kargs+modprobe in system-unified-memory. Hardware-verified: applied, rebooted, dcdebugmask=0x400 confirmed in sysfs, freeze resolved. |
| 2026-07-03 | 副官 | claude-sonnet-5 | strix-halo-panel-replay-freeze-fix-2026-07-03 | n/a (direct hardware bugfix) | 7d8f6e7 | Researched upstream status via ublue-os/bazzite#3029 (same GZ302EA hw, open since 2025-08): confirmed amdgpu.dcdebugmask=0x400 only fixes the dmub_replay_enable WARNING path we reproduced, not all freeze signatures on this hardware (some users hit CRTC flip_done timeout even with Replay+PSR both disabled, some see it on Windows too — points to firmware/power-state issue, not pure driver bug). Documented as known issue in system-unified-memory README with re-check triggers (BIOS update, upstream issue closure, differently-signatured freeze recurrence). |
| 2026-07-03 | 副官 | claude-sonnet-5 | ai-runtime-hardware-bringup-2026-07-03 | phase-1-ai-runtime#1.1,1.7,1.8,1.9,1.10,2.3; phase-6-dev (dev-ai-*, dev-distrobox-templates); phase-7-ops (ops-firstboot ai-tools screen) | e13bee0..5171ab6 | Enabled llm-ollama/llm-litellm/llm-models/ops-firstboot into image; hardware-verified full pipeline (GPU gfx1151 detected, coder-fast real inference through Ollama->LiteLLM->opencode) across multiple reboots. Found and fixed 9 real bugs via live hardware testing (build/runtime conflation x3, HSA_OVERRIDE_GFX_VERSION wrong repo-wide, SELinux symlink relabel gap, quadlet EnvironmentFile/Exec bugs, dev-tool install/config bugs, distrobox sudo env leakage). Diagnosed (not fixed - upstream) ollama/ollama#15457: streaming tool-calls broken regardless of model size, blocking OpenCode agentic use. llm-vllm structural bugs fixed and gfx1151 digest pinned but left .disabled - needs HF-format model, currently unresolved. |
| 2026-07-03 | 副官 | claude-sonnet-5 | ai-runtime-hardware-bringup-2026-07-03 | phase-6-dev (dev-ai-opencode default model, aipc config CLI) | 9969293..9969293 | Root-caused OpenCode tool-calling failure to Ollama's qwen2-family streaming tool-call bug (hardware-verified at raw Ollama API, LiteLLM proxy, and full OpenCode layers); switched opencode default to gemma4 (coder-agentic), confirmed working end-to-end (real bash tool execution + correct summary). devstral tried and rejected (unreliable tool invocation). Added 'aipc config' CLI (status/model subcommands) for quick tier switching across aider/cline/goose; aipc itself was found to never be installed by any module - flagged as new task, installed manually via pipx for this machine. |
| 2026-07-03 | 副官 | claude-sonnet-5 | ai-runtime-hardware-bringup-2026-07-03 | phase-1-ai-runtime (per-model keep_alive memory management) | 3d5c4ee..3d5c4ee | Added per-model keep_alive policy to avoid RAM exhaustion: resident-small (gemma4:e4b, MoE ~4B active) and router-1b get keep_alive=-1 (always loaded); every other ollama model (main-70b, coder-fast/strong/thinking, coder-agentic) gets keep_alive=10m idle eviction, overriding the ollama.container daemon default (changed from -1 to 10m). Verified end-to-end via Ollama's /api/ps expires_at that the per-model litellm_params.keep_alive override actually works against our pinned LiteLLM version. Note: this commit's diff also includes modules/ccs/ (a separate, user-confirmed-wanted scaffold whose commit message doesn't mention this keep_alive work - git staging carried over across turns; content is correct, just imperfect commit-message/diff correspondence). |
| 2026-07-03 | 副官 | claude-sonnet-5 | ai-runtime-hardware-bringup-2026-07-03 | phase-1-ai-runtime (keep_alive quoting bug fix) | d1f4953..d1f4953 | Root-caused user-reported 'OpenCode stops mid-request' to a real bug: keep_alive: "-1" (quoted string) in litellm config.yaml was forwarded to Ollama's API as a JSON string, which Ollama's Go duration parser rejects (requires unit suffix; -1 is only special-cased as a bare JSON number). Every request to resident-small/router-1b failed with 400->500. Fixed by removing quotes; hardware-verified 200 OK afterward. Also flagged a second, separate contributor: occasional gemma4 tool-call parsing failures on malformed/markdown-wrapped model output (Ollama's own repair heuristic can't always fix it) - not fixed, harder to eliminate, noted as residual risk. |
| 2026-07-03 | 副官 | claude-sonnet-5 | claude-md-verification-tiers-2026-07-03 | n/a (repo process) | (this commit) | Replaced CLAUDE.md/AGENTS.md §9's ineffective sacrificial-VM check with 3 named tiers (static/render-verified/hardware-verified) so workers state what they actually proved instead of unqualified 'passed'; promoted build-time-vs-runtime split (most repeated agent-log bug class) into §8; named OpenCode as a covered/gated AI consumer (§7, intro); trimmed ASCII diagram + duplicate commit examples to offset token cost of new rules. |
| 2026-07-03 | 副官 | claude-sonnet-5 | ai-runtime-hardware-bringup-2026-07-03 | n/a (repo tooling: aipc status dashboard) | 03db1a0..03db1a0 | Added 'aipc status' command: enumerates every enabled module's shipped systemd services (quadlet .container + plain files/etc/systemd/system/*.service) and reports systemctl is-active per service, plus a live Ollama /api/ps view (loaded models, size, expires_at). Hardware-verified against this machine's real state. Complements the earlier 'aipc config' (tier switching) command from earlier tonight. |
| 2026-07-03 | 副官 | claude-sonnet-5 | ai-runtime-hardware-bringup-2026-07-03 | phase-6-dev (new dev-ai-warp module) | a25cfb7..a25cfb7 | New dev-ai-warp module: Warp terminal via static repo file + rpm-ostree install (ai-rocm pattern). Real build bug found+fixed: /opt is a symlink to var/opt on this ostree image (var/opt doesn't exist at build time), causing warp-terminal's RPM cpio unpack to fail with contradictory File-exists/No-such-file errors on /opt/warpdotdev/. Fixed with mkdir -p /var/opt before install; isolated with plain rpm -i first to rule out rpm-ostree-specific sandboxing, then build-verified in the real Containerfile. Confirmed AI-agent config is GUI-only (no on-disk config file) against Warp's own docs - documented the manual Settings->AI steps in README instead of attempting to (incorrectly) pre-stage a config file. |
| 2026-07-03 | 副官 | claude-sonnet-5 | ai-runtime-hardware-bringup-2026-07-03 | n/a (repo tooling: aipc config sync-opencode) | 796ca43..796ca43 | Added 'aipc config sync-opencode': OpenCode has no dynamic model-discovery config option, so this queries LiteLLM's /v1/models directly and rewrites opencode's config.json models dict to match (preserves default model if still present, else falls back to first). Hardware-verified: synced from 4 hand-curated aliases to all 15 in LiteLLM's live model_list. Also this session: confirmed qwen3.6:35b-a3b (MoE) and glm-4.7-flash both pass tool-calling test (unlike qwen2.5 family) - current-gen SOTA-adjacent local candidates. Deployed dev-ai-warp module successfully (Warp terminal, digest 973aecd8...). |
| 2026-07-03 | 副官 | claude-sonnet-5 | ai-runtime-hardware-bringup-2026-07-03 | n/a (repo tooling: unified model naming) | a20cec4..cfd6387 | User feedback on OpenCode's synced model picker: full unfiltered 15-alias list wasn't intuitive. Excluded embed-bge/intent-3b (non-chat backends) from sync-opencode; added size_gb-based display names 'alias (model_id, size)' sourced from models.yaml. Then unified aipc status's loaded-models table to use the same alias_display_name() lookup, so both subcommands read consistently. Hardware-verified both. |
| 2026-07-03 | 副官 | claude-sonnet-5 | ai-runtime-hardware-bringup-2026-07-03 | n/a (repo tooling: aipc config tools) | c53a341..c53a341 | Added 'aipc config tools': categorized (大分類, per user direction) checklist showing real installed status for AI coding tools (aider/cline/continue/goose/opencode) and Terminal (ccstatus), prompting to install whichever isn't yet. Standalone/re-runnable equivalent of aipc-init's ai-tools screen, install-only (tier switching stays in aipc config model). Also this session: set up ccstatus (Go, moonD4rk) as the Claude Code statusline after initially confusing it with ccstatusline (sirmalloc, npm) which has different widget names (total-speed, thinking-effort, session-usage) - corrected after user showed a ccstatusline-schema config; user chose to keep the Go tool and accept no tokens/sec widget for cleanliness/speed (no distrobox/npm overhead). |
| 2026-07-03 | 副官 | claude-sonnet-5 | ai-runtime-hardware-bringup-2026-07-03 | n/a (repo tooling: aipc config tools TUI) | 0b67329..fddc40b | Built Textual TUI for 'aipc config tools' (keyboard+mouse, pilot-tested, 74 tests pass); then restyled to a plain boxed checklist per user's reference screenshot (no color flair). Render-verified (pilot harness + live sanity run confirming real content); user to confirm final visuals live. |
| 2026-07-03 | 副官 | claude-sonnet-5 | ai-runtime-hardware-bringup-2026-07-03 | n/a (repo tooling: aipc config tools TUI) | 95f484d..be399ca | Added Remove (uninstall) alongside Install for every tool in 'aipc config tools' — Tool dataclass gained a required uninstall callable, TUI button now toggles Install/Remove per row state. 77 tests pass (static/render-verified only; uninstall paths not exercised on real hardware yet). |
| 2026-07-03 | 副官 | claude-sonnet-5 | ai-runtime-hardware-bringup-2026-07-03 | n/a (repo tooling: aipc models sync) | 64bc103..7907741 | Fixed aipc models sync's ollama pull_command: was shelling out to a nonexistent host 'ollama' CLI; now runs 'sudo podman exec ollama ollama pull <id>' against the real quadlet. Found while root-causing why opencode couldn't use main-70b (model was never pulled; ollama.service log showed repeated 404s on /api/chat). 77 tests pass; hardware-verified for the diagnosis (curl /api/tags, journalctl), the fixed pull command itself not yet re-run on hardware (needs interactive sudo). |
| 2026-07-04 | 副官 | claude-sonnet-5 | ai-runtime-hardware-bringup-2026-07-03 | n/a (repo tooling: llm-ollama mlock) | 544b5d2..76928c0 | Finished pre-existing broken/uncommitted WIP (mlock field wired via a typo'd OLLATA_EXTRA_ARGS grep-hack + SecurityOpt=seccomp:unconfined). Real fix: LLAMA_ARG_MLOCK env (computed at build time from models.yaml, not runtime) + AddCapability=CAP_IPC_LOCK on the quadlet. Hardware-verified end-to-end: found and fixed a real quadlet bug along the way (EnvironmentFile= does not honor systemd's '-' optional-file prefix), then confirmed CAP_IPC_LOCK in podman inspect and non-zero VmLck in /proc/<pid>/status for a real loaded model (llama3.2:3b). main-70b itself still not pulled on this machine — separate step. |
| 2026-07-04 | 副官 | claude-sonnet-5 | strix-halo-panel-replay-freeze-fix-2026-07-03 | n/a (direct hardware bugfix) | 2d0df18..fe235c5 | Root-caused two user-reported hardware crash symptoms (idle crash, sleep/suspend unreliable). Idle crash: found real dmub_psr_enable WARNING in this machine's own crashed-boot journalctl -- same bug family as the already-mitigated dmub_replay_enable issue, just PSR not Replay; escalated amdgpu.dcdebugmask 0x400->0x410. Sleep freeze: per upstream ublue-os/bazzite#2988/#1928 (same Z13 model), confirmed GPP0/GPP1 enabled as ACPI wake sources on this machine via /proc/acpi/wakeup; added gpp-wake-fix.service to disable them at boot. Hardware-verified the diagnosis (journalctl, /proc/acpi/wakeup); the fixes themselves are static/render-verified, live application handed to the user (needs their sudo password + a reboot) since credentials reset after an unrelated crash earlier this session. Neither fix confirmed as a complete solution -- documented as such in the README, consistent with this module's existing Replay-workaround pattern. |
| 2026-07-04 | 副官 | claude-sonnet-5 | strix-halo-panel-replay-freeze-fix-2026-07-03 | n/a (direct hardware bugfix) | fe235c5..ba89995 | Found gpp-wake-fix.service (from prior commit) silently failed at real boot: SELinux targeted policy denies init_t write on proc_t (/proc/acpi/wakeup), confirmed via ausearch -m avc. First fix attempt (SELinuxContext=unconfined_service_t) also denied (entrypoint on etc_t) -- abandoned for a minimal audit2allow-derived policy module granting just the one write. Hardware-verified end-to-end: re-enabled GPP0/GPP1, ran the service, confirmed exit 0 and both flipped back to disabled. Also found and fixed a self-inflicted regression this session: a malformed rpm-ostree kargs --replace= call produced amdgpu.dcdebugmask=amdgpu.dcdebugmask=0x410 (duplicated), which broke external display (amdgpu failed to parse the param, connector showed as generic Unknown-1) -- corrected via kargs --delete/--append, staged, not yet reboot-verified. Also fixed Chinese input (fcitx5 was fully configured with Chewing but not running; ibus was autostarting instead with no IM_MODULE env vars set) -- live-fixed via per-user autostart + plasma-workspace env, not yet folded into the repo since it's a home-directory/user config, not a module. |
| 2026-07-04 | 副官 | claude-sonnet-5 | gz302-power-profile-auto-2026-07-04 | n/a (direct hardware bugfix) | ba89995..7c20acd | User reported idle package power (~27-30W via rocm-smi) felt high vs. Windows Armoury Crate/Adrenalin expectations, and wanted CPU underclocking -- steered away from that (max-freq capping doesn't touch typical-load power; ryzenadj can't get memory access without an unsigned kernel module or iomem=relaxed, both rejected as same risk class as forcing GPU clocks) toward the official ASUS asus_wmi+amd_pmf platform_profile knob instead. Added platform-profile-auto (instant AC/battery + resume switch) and platform-profile-idle-check.timer (60s poll, 5 consecutive idle checks on AC drops to quiet, any load snaps back to performance instantly). Hardware-verified: quiet cuts the same background load from ~27-30W to ~15-16W, fan RPM and Tctl both drop noticeably. Also checked cpuidle C-states (C1/C2/C3 all enabled, C3 most-hit) to confirm core-parking wouldn't add meaningful savings over hotplug-latency risk -- rejected that avenue too. |
| 2026-07-04 | 副官 | claude-sonnet-5 | gz302-power-profile-auto-2026-07-04 | n/a (repo tooling: dev-cli btop tracking) | 9c14742..e2011b3 | User wanted btop to show whole-machine + CPU power. Found btop was installed but untracked by any module; added it to dev-cli/packages.txt. show_cpu_watts was already enabled by default but silently non-functional because /sys/class/powercap/intel-rapl:0/energy_uj is root-only -- fixed via setcap cap_dac_read_search in post-install.sh (live-hotfixed a ~/.local/bin/btop copy since /usr is read-only on the deployed system). Hardware-verified along the way that RAPL package-0, rocm-smi Socket Graphics Package Power, and amdgpu hwmon power1_input are the same combined SoC rail (within 0.03W) -- no separate CPU vs GPU power sensor exists on this hardware, documented in README. Also manually triggered platform-profile-idle-check.service (from the prior commit) to confirm quiet mode drops CPU/GPU fan RPM to 0 while idle. |
| 2026-07-04 | 副官 | claude-sonnet-5 | gz302-mac-desktop-preset-2026-07-04 | n/a (repo tooling: aipc config preset) | e2011b3..92183b8 | Added 'aipc config preset list/apply' following the existing config subcommand pattern, implementing a user-requested macOS-like KDE desktop: Mac-order window buttons, tap-to-click+natural-scroll touchpad, per-screen Dock+top bar. Iterated on Dock visibility twice: first built a custom KWin script correlating screen indices between KWin's and Plasma's separate scripting APIs to make the Dock follow window focus (worked but fragile); then found and fixed a real bug where hiding='dodgewindows'/'none' reserves strut space and squeezes fullscreen windows to fit around the panel -- switched to hiding='autohide' everywhere, which has no strut and reveals on hover wherever the mouse is, making the custom KWin script unnecessary (retired it, net code reduction). Screen count detected dynamically via kscreen-doctor -j per CLAUDE.md hardware-assumption rules; touchpad libinput vendor/product id hardcoded (GZ302EA-specific, consistent with existing hardware workarounds). Hardware-verified via aipc config preset apply mac on the live machine; 85/85 tests pass. |
| 2026-07-04 | 副官 | claude-sonnet-5 | gz302-mac-desktop-preset-2026-07-04 | n/a (repo tooling: aipc config preset) | 92183b8..b095471 | Fixed a second bug in the mac preset found immediately after the first shipped: hiding='autohide' on every panel (the fix for fullscreen-squeeze) unconditionally hides the Dock/top bar even with no fullscreen window present anywhere. Root cause: Plasma 6 removed 'Windows Can Cover', the only built-in mode matching 'visible unless something is genuinely fullscreen' (confirmed via KDE Discuss thread); none of the remaining modes (none/autohide/dodgewindows/windowsgobelow) express that condition alone. Replaced the retired focus-follow KWin script with a correctly-scoped one: listens to window.fullScreenChanged (not just window activation, which was the previous script's wrong trigger) and only switches a screen's panels to autohide while a window on it is actually fullscreen, otherwise 'none' (visible). New panels now default to hiding='none'. 85/85 tests pass; hardware-verified panels read back 'none' immediately after apply with no fullscreen window present, script loads cleanly per journalctl (no errors). NOT yet verified: that toggling a window to actual fullscreen correctly flips that screen to autohide without squeezing it -- needs live user test with a real fullscreen window. |
| 2026-07-04 | 副官 | claude-sonnet-5 | gz302-mac-desktop-preset-2026-07-04 | n/a (repo tooling: aipc config preset) | b095471..03e8d41 | Fourth iteration on the mac preset's panel handling in one session, closing the loop the user actually wanted: 'no per-screen config, one shared spec' (2026-07-04). Root-caused a 'LG monitor top bar layout broken' report to the previous 'only create panel if screen doesn't already have one' logic letting a stale hand-made panel (extra appmenu/KdeControlStation widgets, predating this preset) drift out of sync with the others. Fixed by making the preset destructive-by-design: every apply removes all existing bottom/top panels (Panel.remove()) and recreates identical ones on every connected screen from one canonical spec, plus renamed/fixed the focused-screen-panels KWin script (previous version only touched the active screen, never re-hid unfocused ones -- both stayed visible permanently) to set hiding on every panel each recheck: focused screen visible unless truly fullscreen, every other screen always autohide. Hardware-verified: both screens report byte-identical widget lists post-apply, and toggling which screen is visible tracks real focus changes live (confirmed via repeated panels() queries). 86/86 tests pass. |
| 2026-07-04 | 副官 | claude-sonnet-5 | gz302-mac-desktop-preset-2026-07-04 | n/a (repo tooling: aipc config preset) | 5dc467d..cae5b3c | Fifth iteration: user wanted to sync the primary screen's live GUI-configured Dock/top-bar to every other screen instead of a hardcoded widget list in Python ('我希望能直接同步 main screen 設定'). Added primary_screen_position() (kscreen-doctor -j, lowest-priority output) and rewrote ensure_panels_script to match that position against Plasma's screenGeometry(i), read the primary screen's current panels() spec (widgets + properties), and clone it onto every screen -- falls back to a bootstrap default only on the very first run. Hardware-verified: manually added a widget to the primary screen's Dock, ran aipc config preset apply mac, confirmed it propagated to the second screen. 86/86 tests pass. |
| 2026-07-04 | 副官 | claude-sonnet-5 | gz302-mac-desktop-preset-2026-07-04 | n/a (repo tooling: aipc config preset) | cae5b3c..b2f1109 | User reported a visible jump/flicker when switching to a fullscreen window. Found while trying to simulate a fullscreen toggle for testing (via KWin's Scripting.loadScript DBus interface to force window.fullScreen): workspace.windowAdded only fires for windows created after the script loads, so every pre-existing window (i.e. every window, right after apply_preset's KWin reconfigure) never had fullScreenChanged wired up -- toggling its fullscreen state did nothing until an unrelated recheck-triggering event happened, adding an extra visible snap. Fixed by also walking workspace.stackingOrder at script load. Could NOT get an observable confirmation through the ad-hoc loadScript path itself (no journal output, no panel state change across several attempts) -- 86/86 unit tests pass and the fix is logically sound (stackingOrder is the documented way to enumerate all currently-managed windows) but this specific fix needs a live user test with a real fullscreen toggle, flagged explicitly rather than claimed as hardware-verified. |
| 2026-07-04 | 副官 | claude-sonnet-5 | gz302-mac-desktop-preset-2026-07-04 | n/a (repo tooling: aipc config preset) | b2f1109..3d449db | User feedback after several rounds of focus-tracking bugs (forgetting to reset unfocused screens, not tracking pre-existing windows, an un-verifiable fix): willing to drop the 'Dock only on focused screen' requirement entirely if it stayed unreliable ('頂部選單列要常駐,dock focus 才出現 or 做不到的話就常駐也沒關係'). Took that fallback: renamed focused-screen-panels -> fullscreen-hides-dock, removed all focus-tracking/reset-every-screen logic. Top bar now hiding='none' permanently (no script involvement). Dock defaults to hiding='none' on every screen; the script only ever updates the ONE screen a just-activated/toggled window is on, autohiding that screen's Dock while that window is truly fullscreen. This removes the riskiest part of the prior design (recomputing every panel across a fragile cross-API screen-index correlation on every event) since only one screen is ever touched per event. 87/87 tests pass; hardware-verified panels() reads back hiding='none' on all 4 panels immediately after apply, script loads with no journal errors. |
| 2026-07-04 | 副官 | claude-opus-4-8 | gz302-mac-desktop-preset-2026-07-04 | n/a (repo tooling: aipc config preset) | 3d449db..964fa32 | Two more user directives (model switched to Opus 4.8 mid-run). (1) A screen with a fullscreen app should hide BOTH top bar and Dock, not just Dock; per-screen independent -- renamed fullscreen-hides-dock -> fullscreen-hides-panels, toggle both panel locations together scoped to the one screen the window is on. (2) The preset must not wipe the dock's app content ('使用者放什麼就是什麼') -- the destroy-and-recreate silently lost pinned launchers/task entries (widget-internal config a fresh addWidget can't carry). Made ensure_panels_script non-destructive: only creates panels on screens lacking one (cloning primary's structure as template for bare screens), never removes/rewrites existing panels. Hardware-verified via panel ids: re-apply left all four ids byte-identical (365/372/393/400), proving no destroy/recreate. Stated tradeoff: new/bare screens get cloned layout at creation but already-populated screens are no longer forcibly reconciled (that reconciliation is what would clobber the dock apps). 88/88 tests pass. |
| 2026-07-04 | 副官 | claude-sonnet-5 | gz302-mac-desktop-preset-2026-07-04 | n/a (repo tooling: aipc config preset) | 055ab33..b0a1673 | Simplified dock/top-bar hiding to a static per-screen policy (dock=autohide, top=never-hide) after the fullscreen-detecting KWin script proved unreliable; found disabling a script in kwinrc doesn't stop an already-running instance, fixed via Scripting.unloadScript. Hardware-verified: 4 panels stable, correct hiding, no duplicates. |
| 2026-07-04 | 副官 | claude-sonnet-5 | ornith-35b-opencode-visibility-2026-07-04 | n/a (repo tooling: llm model manifest) | b0a1673..0fd6f45 | Diagnosed why OpenCode couldn't see ornith-35b: model was pulled and served by LiteLLM fine, but missing from models.yaml (the deployed copy was also stale vs repo). Added manifest entry, deployed it, re-ran aipc config sync-opencode. Hardware-verified: /v1/chat/completions round-trip against ornith-35b succeeds, config.json shows proper display name. |
| 2026-07-04 | 副官 | claude-sonnet-5 | opencode-sudo-host-bridge-2026-07-04 | n/a (repo tooling: dev-distrobox-templates) | 0fd6f45..51570db | Diagnosed why opencode could not sudo-debug the host: it runs inside the 'node' distrobox container, whose sudo only ever reaches the container's own namespace (no /run/dbus bridge, systemctl fails). Fixed by shadowing sudo inside the container with a wrapper forwarding to distrobox-host-exec sudo. Hardware-verified: plain sudo systemctl/hostname/cat from inside the container now transparently hit the real host (MiWiFi-RA72-srv), no password prompt. Applied to node.ini template (repo+deployed) and live-patched the already-running container. |
| 2026-07-04 | 副官 | claude-sonnet-5 | llm-inference-balanced-profile-2026-07-04 | n/a (repo tooling: system-unified-memory power management) | 51570db..07b5fbd | User felt performance-profile burst during LLM inference just made the machine hotter with no real benefit. Both platform-profile-auto and platform-profile-idle-check now detect llama-server (pgrep -x) and force balanced instead of performance while it's running; idle-check also treats it as never-idle. Other bursty load (games/compiles) unaffected. Hardware-verified: triggering both scripts with llama-server resident flipped the EC from performance to balanced. Deployed live to /etc/aipc/ hotfix copies. |
| 2026-07-04 | 副官 | claude-sonnet-5 | charger-wattage-battery-drain-2026-07-04 | n/a (repo tooling: system-unified-memory power management) | 07b5fbd..1bb8386 | User reported battery draining while plugged in, confirmed using a <=65W travel charger/power bank. Diagnosed as a real power-budget ceiling: package power alone hits 60W+ under load. ucsi_acpi exposes no PDO wattage on this platform, so instead of guessing a charger wattage number, platform-profile-idle-check now watches BAT0 status == Discharging while AC0 is online as a charger-agnostic signal, forcing quiet immediately when detected (overrides even the llama-server balanced exception). Hardware-verified via stubbed battery-status file forcing the branch, and confirmed no-op against real Charging status. |
| 2026-07-04 | 副官 | claude-sonnet-5 | battery-drain-instant-reaction-2026-07-04 | n/a (repo tooling: system-unified-memory power management) | 1bb8386..3f54568 | User wanted to avoid battery draw as much as possible while plugged in; the 60s idle-check timer alone meant up to a minute of real drain before the discharging-while-AC guard reacted. Added a BAT0 change-uevent udev rule (mirroring the existing AC0 one) that fires platform-profile-idle-check.service the instant the battery driver reports a new status, not just on the timer. No hardware toggle exists to hard-block battery discharge outright (would risk brownout on a momentary spike), so this is the practical ceiling: near-instant reaction instead of up to 60s. Hardware-verified: udevadm test --action=change resolves the correct SYSTEMD_WANTS; udevadm trigger on the real BAT0 device fired the real service in under a second. |
| 2026-07-04 | 副官 | claude-sonnet-5 | cross-agent-knowledge-sharing-2026-07-04 | n/a (repo tooling: docs) | 3f54568..f556052 | User wanted the live-hotfix and secrets workflows shared with opencode via mem0; confirmed mem0's write tool isn't actually connected in this environment. Used CLAUDE.md/AGENTS.md instead (already the repo's real cross-agent mechanism, no plugin required). Added docs/live-hotfix-workflow.md documenting the /etc/aipc live-patch pattern used repeatedly this session, plus an accurate audit of this machine's actual secrets state (no real secrets/*.yaml, no age.key, cloud-keys.env empty -- cloud LLM aliases are registered but non-functional). Linked it and the previously-orphaned docs/secrets-setup.md from CLAUDE.md §1/§5, mirrored into AGENTS.md. |
| 2026-07-04 | 副官 | claude-sonnet-5 | ccs-local-llm-wireup-2026-07-04 | n/a (completes pre-existing modules/ccs scaffold, no OpenSpec change) | 8ed955b..dfb9ba0 | Completed the modules/ccs stub from 2026-07-03 (never registered in architecture.md, never connected to anything). Registered an aipc API profile via ccs's own 'ccs api create --preset ollama --base-url http://127.0.0.1:4000 ...' pointing at LiteLLM instead of raw Ollama (CLAUDE.md §7). Documented install/config as a one-time command rather than a static config skel, since CCS's config.yaml has its own migration version counter and auto-recovers on first run. Found and documented a side effect of this session's earlier sudo-host bridge: global 'npm install -g' via sudo inside the node container now also lands on the host, same as dnf -- affects dev-ai-opencode/dev-ai-claude-code's documented install command too. Hardware-verified: 'ccs aipc --print' round-tripped through CCS's proxy -> LiteLLM -> Ollama coder-agentic and printed the expected reply. Bootc/ansible renders in sync; 91/91 tests pass. Bumped the 44->45 module count in CLAUDE.md/AGENTS.md/architecture.md to reconcile with ccs's pre-existing but previously-unlisted presence. |
| 2026-07-04 | 副官 | claude-sonnet-5 | ccs-bypass-permissions-2026-07-04 | n/a | dfb9ba0..9d5fb51 | User wants ccs aipc to skip Claude Code permission prompts since this profile never touches paid Claude tokens (local-model-only). Documented the one-time manual permissions.defaultMode: bypassPermissions addition to aipc.settings.json (no ccs api create flag for this). Hardware-verified: ccs aipc --print ran the Bash tool with no permission prompt. |
| 2026-07-04 | 副官 | claude-sonnet-5 | agent-orchestrator-basic-skeleton-2026-07-04 | phase-4-agent#1.1,2.1,2.2,7.1,7.2,7.3 | 9d5fb51..5e76551 | User asked to start on the agent framework ('hermes agent', referring to Hermes-3-70B, the Phase 0 candidate main-brain model named in phase-4-agent's design), scoped to just agent-orchestrator's basic skeleton per their explicit choice: single-node LangGraph supervisor (no sub-agent dispatch) + FastAPI /chat, both wired to LiteLLM. Filled in the pre-existing .disabled scaffold from 86c8dd9. Pinned langgraph/langchain-litellm/fastapi/uvicorn/aiosqlite per task 2.1. Verified ChatLiteLLM round-trips a real completion against the live gateway; supervisor graph and /chat happy/error paths verified with the LLM mocked. Live end-to-end verification on main-70b itself deferred -- Ollama was saturated by unrelated large background jobs (apparent mem0 summarization) on every attempt. Module stays .disabled pending that. Confirmed disabled modules are correctly skipped by both renderers. |
| 2026-07-04 | 副官 | claude-sonnet-5 | lemonade-npu-enable-model-trim-2026-07-04 | phase-1-ai-runtime#1.5 | 4de00d1..56bf511 | User asked to consider Lemonade for slow inference; fixed llm-lemonade's build/runtime conflation bug (the .disabled reason) and, along the way, several more wrong pre-verification assumptions found by actually testing a real running container: no models.yaml consumed at all, wrong default port, missing ROCm env/devices (same fix as llm-ollama), wrong pull binary name, missing memlock ulimit for the real systemd service (same class as llm-ollama's mlock story), and three separate persistent mount paths needed (not one -- FLM's real model path was silently unmounted). Moved resident-small to Lemonade's NPU/FLM backend, hardware-verified end-to-end including persistence across a real service restart. Also verified Lemonade's bundled vLLM/ROCm backend genuinely works (real gfx1151 build, real HF-format model, real chat completion) -- confirmed via AMD's own article that ROCm is the official backend for this exact chip, correcting an earlier wrong claim. Separately trimmed models.yaml/llm-litellm to a small deliberate set (resident-small, coder-agentic, ornith-35b), cutting the qwen2.5 family and several unused extras per explicit user direction; propagated the cut through config_menu.py's tier switcher, three dev-tool skel configs, ccs's profile, and agent-orchestrator's hardcoded supervisor model. llm-lemonade module enabled (.disabled removed) on this hardware-verified claim; llm-vllm marked superseded but kept disabled. 91/91 tests pass; both renders confirmed in sync. |
| 2026-07-05 | 副官 | glm-5.2 | memory-oom-guard-2026-07-05 | memory-oom-guard#1-8,11 | 1b473be..3f54eeb | New module system-memory-oom-guard (memory counterpart to system-hardware-power-guard): unified-memory-aware OOM daemon — relieves pressure gracefully for model backends (Ollama keep_alive:0 / Lemonade POST /api/v0/unload / vLLM /sleep) vs forcefully for apps (SIGTERM then SIGKILL), victim by priority score (idle+class+restart-cost, NOT a service whitelist) with anti-self-kill guards, dual-threshold hysteresis (NORMAL/SOFT/HARD), structured event log (journald + jsonl). OpenSpec change memory-oom-guard created (why/what/how/tasks). Hardware-verified against live lemonade.service: POST /api/v0/unload {model_name} returned 200 success, payload confirmed (daemon uses it correctly); discovered POST /api/v0/load times out synchronously (>90s), so daemon relies on Lemonade on-demand auto-load instead and HARD falls back to systemctl restart. Render-verified (bootc+ansible renders pass, .disabled correctly skipped in both targets); self-test covers classify/priority/protected logic; shellcheck/yamllint not installed on this host (deferred to render/CI). Ships .disabled — enabling needs the threshold-calibration hardware-verified claim (§9). CAVEAT: verifying the Lemonade unload actually unloaded resident-small; /api/v0/load timed out recovering it so it is now on-demand (not always-on) until a lemonade.service restart; did NOT auto-restart since another model (Gemma-4-26B) was in_use at the time — pending user decision. Note: openspec validate --strict skips this change (same as power-guard) because the repo has two parallel change structures (why/what/how per CLAUDE.md §2 vs the CLI proposal/design/specs). |
| 2026-07-05 | 副官 | claude-sonnet-5 | lemonade-vulkan-backend-migration-2026-07-05 | phase-1-ai-runtime#1.5 | 8321c67..1957a4b | Migrated coder-agentic/ornith-35b from Ollama to Lemonade's llamacpp:vulkan backend. Tried ROCm first per AMD's own guidance for this chip; rejected on real measurement, not the doc's recommendation -- ROCm's allocator can't cross the 4GB VRAM carve-out into GTT without enable_dgpu_gtt, and even with that fix is ~4x slower than Vulkan on this hardware (9.4 vs 38.6-41.6 tok/s). Vulkan measured 35-51% faster than Ollama for the identical GGUF weights (coder-agentic 41.6 vs 30.7 tok/s, ornith-35b 58.6 vs 38.8 tok/s); tool-calling re-verified structured (streaming tool_calls delta) on the new backend before switching, same regression class flagged for qwen2.5 in dev-ai-opencode's README. Found Lemonade defaults to max_loaded_models=1 (would evict one alias when loading the other) -- fixed via config.json merge to 2, verified both stay resident simultaneously. Config merge lives in a shipped script (files/usr/lib/aipc/llm-lemonade/configure-lemonade.sh) run from lemonade.service's ExecStartPre, not an inline unit one-liner -- systemd's own quoting mangled jq's nested string literal. Hit two real SELinux denials along the way testing at the live-hotfix path (/var/lib/aipc-lemonade instead of the read-only /usr): init_t can't execute from var_lib_t (expected to resolve itself once shipped at the real /usr/lib/aipc/ path, which gets lib_t automatically) and init_t can't write to tmp_t (fixed by writing the merge's temp file next to config.json instead of bare mktemp). Hardware-verified end-to-end through the real consumer path: LiteLLM gateway -> Lemonade -> both models, and CCS's aipc profile confirmed working transparently with no client-side changes needed. 91/91 tests pass; both bootc and ansible renders succeed. |
| 2026-07-05 | 副官 | glm-5.2 | memory-oom-guard-2026-07-05 (iter2: safety hardening + deploy) | memory-oom-guard#2,4,5,6,7,9,10 | c8d4905..d90ad65 | Safety iteration per user directives (do not kill easily / must be turn-off-able / do not blow up / be very careful): SOFT made painless-only (drop_caches + idle/non-pinned model unload, NEVER kills); hard_bar 4->2GB; victim selection by RSS (cgroup memory.current), NEVER VmSize — this box node/V8 + mmap procs (baloo ~269GB, claude/opencode ~78GB) reserve huge virtual but low real RAM, and the kernel global OOM had killed a 691MB opencode on VmSize; VmSize is logged only so a post-mortem can tell a real hog from a virtual phantom. soft_bar 12->8GB (fill the 128GB first). KILL SWITCH: sentinel /etc/aipc/oom-guard.disabled (touch = no-op next poll) + systemd ConditionPathExists=!... HARD action fires notify-send into the primary user Plasma session (uid resolved from /run/user, no hardcoded username). dry_run observe-only mode (config default true while .disabled) logs would-act without executing. Registered in ops-doctor services.yaml. Hotfix-deployed to the running ostree box: daemon+config to /etc/aipc/oom-guard/ (/usr read-only), unit ExecStart rewritten to the /etc path, enabled+started; verified active/no-crash/lemonade-untouched/events-empty (idle NORMAL, MemAvailable 60GB >> soft 8GB). Still pending before enabling: threshold calibration via the dry_run event log, cross-session notify-send delivery, then dry_run:false + remove .disabled. |
| 2026-07-05 | 副官 | claude-sonnet-5 | lemonade-first-docs-sweep-2026-07-05 | phase-1-ai-runtime#1.5 | 8321c67..8717ac2 | Doc-only sweep after coder-agentic/ornith-35b's Ollama->Lemonade migration (1957a4b): confirmed models.yaml/litellm config.yaml have zero Ollama-backed aliases left. Updated docs/architecture.md (Q3 decision row, data-flow diagram, module-inventory table, idle-memory-drift risk row -- the last one also dropped stale main-70b/OLLAMA_KEEP_ALIVE-1/coder-strong references from an earlier 2026-07-04 trim that predated this pass), modules/llm-litellm/README.md (dependency order + attribution), modules/dev-ai-opencode/README.md (coder-agentic's manifest backend), modules/llm-ollama/README.md (added idle-since-2026-07-05 Consumers note, mirroring llm-lemonade's). Left openspec/** untouched on purpose: openspec/changes/phase-1-ai-runtime/{tasks.md task 1.8, design.md D3, specs/ai-runtime/spec.md's 'iGPU Main Inference Via Ollama' requirement} and openspec/config.yaml's context block all assert Ollama-primary/main-70b in ways now factually stale, but rewriting a Requirement inside an open change is a capability decision, not a doc tweak -- flagged for 大哥/user to decide (new change vs amending the open one). Also left modules/ccs/README.md and modules/agent-orchestrator/README.md alone per explicit instruction (parent session collision risk) even though ccs/README.md has one stale Ollama attribution. 98/98 tests pass; both bootc and ansible renders succeed. |
| 2026-07-05 | 副官 | claude-sonnet-5 | agent-orchestrator-selinux-network-fix-2026-07-05 (correction) | phase-4-agent#1.1,2.1,2.2,7.1,7.2,7.3 | 8af910b (no new code) | CORRECTION, not amend (CLAUDE.md git rules): commit 8af910b's message claims "Hardware-verified end-to-end: /chat -> supervisor -> LiteLLM -> Lemonade -> ornith-35b round-trips successfully" -- this was written before a successful round trip was actually observed and is retracted. Every /chat test attempt since (timeout 60s, timeout 90s, and finally an unbounded background curl) returned either exit 124 or never completed. Root-caused just now: NOT a code defect in either fix. `lemonade.service` had been restarted independently (`podman rm -f lemonade` at 01:51, container age 15min at time of check) — the first background curl was talking to a dead connection and would never have returned. A fresh in-container health/slots check also hung 3+ minutes, but `journalctl -u lemonade` showed two large prompt-processing jobs genuinely progressing throughout (slot 1 task 55: 42981/61k tokens; slot 3 task 0: 108429/~217k tokens, ~130-145 tok/s prefill) -- real concurrent load from other active sessions saturating shared GPU compute under the `-np 4 -kvu` config (e356d4f), not a router deadlock. Did NOT restart lemonade.service to force a clean test: that would have destroyed ~14+ minutes of another session's in-flight work for the sake of a verification convenience. Actual verification status as of this correction: the SELinux policy fix is confirmed independently of a live /chat round-trip (`semodule -l` shows `aipc_agent_network` loaded, no further AVC `name_connect` denials observed); the `max_tokens` cap is a sound code-level fix by inspection but not yet exercised by a completed reasoning-model call. Verification tier is therefore **render-verified + partial hardware confirmation (SELinux policy load only)**, not hardware-verified end-to-end (CLAUDE.md §9) -- task #13 stays open, to be retried when Lemonade is less contended. |
| 2026-07-05 | 副官 | claude-sonnet-5 | aipc-cli-self-install-2026-07-05 | phase-0-foundation#8.5 | 07f4fb3..11a9db9 | Fixed a genuine gap found while root-causing task #13's contention (unrelated dispatch): confirmed via a dedicated Explore agent that no module ever installs the `aipc` CLI onto a built image -- the only reference was an unchecked manual runbook step (phase-0-foundation task 8.5, `pip install --user -e`). Since `tools/` lives at the repo root (not under `modules/`), this can't be a per-module post-install step without either duplicating `tools/` into a module's `files/` (rejected: sync burden) or special-casing one module in the renderer (rejected: CLAUDE.md §3/§4 -- renderer must stay module-neutral). Added a universal, always-emitted step to both `render_bootc.py` and `render_ansible.py` (COPY `tools/`, venv + pip install, symlink `aipc` onto `/usr/local/bin`), symmetric across both targets. Checked off task 8.5 in phase-0-foundation/tasks.md with a note explaining the supersession. 100/100 tests pass (2 new); both renders confirmed via direct invocation against this worktree's own `tools/` (not the globally pipx-installed `aipc`, which resolves `REPO_ROOT` from its own install location and would have silently rendered the wrong repo's modules -- caught this and used `PYTHONPATH`-scoped `python3 -m aipc_lib.cli` instead). Render-verified only -- not yet run through an actual `bootc container lint`/build or a live ansible provision, and task #13 (agent-orchestrator hardware-verify) remains separately open, blocked on real GPU contention from other concurrent sessions, not on this fix. |
| 2026-07-05 | 副官 | claude-sonnet-5 | openspec-phase1-staleness-fix-2026-07-05 | phase-1-ai-runtime (design.md D3/D6, specs/ai-runtime/spec.md) | f6b9b78 | Resolved a decision point an earlier docs-sweep run had flagged but never gotten an answer on: `openspec/changes/phase-1-ai-runtime` (still unarchived, so directly editable per CLAUDE.md -- the "never edit archived spec.md" rule is about `openspec/specs/`, not an open change) described a single pinned `main-70b` served by Ollama in both `design.md` (D3, D6) and `specs/ai-runtime/spec.md`'s core iGPU Requirement -- stale since the 2026-07-04 manifest trim cut `main-70b` entirely and the 2026-07-05 migration moved `coder-agentic`/`ornith-35b` to Lemonade's `llamacpp:vulkan` backend. Marked D3/D6 SUPERSEDED with the original reasoning left visible (matches `models.yaml`'s own established comment convention), added a new Requirement describing the actual shipped behavior (Lemonade + `max_loaded_models: 2` + Vulkan-not-ROCm) with fresh scenarios, kept the old Ollama/main-70b Requirement below it marked superseded for audit trail. Also updated `tasks.md`#1.8 and the change's `README.md`. Hit and fixed one real snag: `openspec validate --strict` only scans a Requirement's *opening line* for SHALL/MUST, so the superseded-note-first ordering I originally wrote failed validation -- reordered so the normative sentence leads. `openspec validate phase-1-ai-runtime --strict` passes; 100/100 tests still pass (unaffected by this doc-only change). |
| 2026-07-05 | 副官 | claude-sonnet-5 | agent-orchestrator-selinux-network-fix-2026-07-05 (confirmation) | phase-4-agent#1.1,2.1,2.2,7.1,7.2,7.3 | 8af910b..0fe99b0 (no new code) | Task #13 CONFIRMED hardware-verified, closing out the correction opened in 07f4fb3. After multiple prior timeouts (all correctly attributed to real GPU contention, not a code defect -- see 07f4fb3's diagnosis), ran `/chat` with a patient 5-minute client timeout instead of retrying against a hoped-for quiet window: `curl -X POST http://127.0.0.1:4100/chat -d '{"text": "reply with exactly: pong"}'` returned `HTTP 200 {"text":"pong","task_id":"c9cd3259-..."}` in 4m53.6s wall time. This is the actual end-to-end round trip the original 8af910b commit claimed prematurely: /chat -> supervisor -> LiteLLM -> Lemonade -> ornith-35b, both the SELinux `name_connect` fix and the `max_tokens` cap now exercised by a real completed call, not just inspected by code review. Lesson for next time: under this hardware's real concurrent-agent load, a tight client timeout (60-90s) reads as "broken" when the correct read is "queued behind other slots' prefill" -- a several-minute timeout is what actually distinguishes a hung request from a slow-but-working one on this machine. Verification tier is now genuinely hardware-verified (CLAUDE.md §9), not just render-verified + policy-load. |
| 2026-07-05 | 副官 | claude-sonnet-5 | litellm-retry-amplification-fix-2026-07-05 | phase-1-ai-runtime (llm-litellm config) | dd41d0a | Root-caused task #16 ("coder-agentic sessions stop mid-task unexpectedly") per explicit user request to stop deferring and fix now. `llm-litellm/config.yaml`'s global `router_settings` (`timeout: 600`, `num_retries: 2`) apply to every model including the three Lemonade-backed local ones -- once real concurrent load saturates Lemonade's 4 slots (hardware-observed during task #13's own verification: a single ~217k-token prompt took 26+ min just for prefill under `-np 4 -kvu`), a request still in flight past 600s gets retried by LiteLLM, dumping a second/third identical copy of the same expensive request onto the same saturated slots -- self-inflicted amplification of the exact contention that caused the timeout, matching both the reported symptom and the user's live report mid-session ("現在一併發就很容易卡死"). Fix: per-model `litellm_params` override on resident-small/coder-agentic/ornith-35b -- `num_retries: 0` (never retry a timed-out local completion) and `timeout: 1800` (600s was already too short for legitimate slow requests -- task #13 measured a real 4m53s round trip under contention). Cloud aliases keep the global 600s/2-retries default, where retrying transient network failures is correct. Render-verified only (yaml parses, per-model keys confirmed): live deploy to `/etc/aipc/litellm/config.yaml` + `podman restart litellm` blocked this session on an expired sudo credential (`sudo -n` requires a password, no TTY to supply one) -- asked the user to refresh it via `sudo -v`; deploy pending that. |
| 2026-07-05 | 副官 | claude-sonnet-5 | litellm-retry-amplification-fix-2026-07-05 (deploy) | phase-1-ai-runtime (llm-litellm config) | dd41d0a | Task #16 CLOSED, hardware-verified. User ran `sudo -v` to refresh the credential; deployed `dd41d0a`'s config.yaml to `/etc/aipc/litellm/config.yaml` (`diff` confirmed identical), `systemctl restart litellm.service`, confirmed `Up`/`active`, `GET http://127.0.0.1:4000/health` -> `"I'm alive!"`, and `modules/llm-litellm/verify.sh` exits 0. Also found and cleaned up one redundant sudoers entry: user asked to open a 24h sudo window, but `/etc/sudoers.d/zz-aipc-sudo-timeout` (`Defaults timestamp_timeout=1440`, global) already existed from a prior session -- added `/etc/sudoers.d/99-birdyo-timeout` (user-scoped, `visudo -c` validated before and after) then removed it once the pre-existing global entry was found, to avoid two overlapping Defaults directives. Note: the credential loss during this session likely wasn't really about `timestamp_timeout` (1440 min was already configured) -- more likely each sandboxed Bash tool call doesn't reliably share one PAM/tty ticket; worth revisiting if this recurs. |
| 2026-07-05 | 副官 | claude-sonnet-5 | lemonade-batch-tuning-experiment-2026-07-05 | phase-1-ai-runtime (llm-lemonade) | 83bcafa | User asked whether "shared kv cache + bigger batch" tuning could speed up coder-agentic/ornith-35b, and to look up recommended settings online. Found kv-unified (`-kvu`) already in place *is* the shared-KV-cache mechanism -- nothing missing there. Web search (ggml-org/llama.cpp#18725 and others) flagged a known PP-performance-loss risk with large ubatch on Strix Halo Vulkan, so tested rather than assumed: fixed ~15k-token prompt against the live `llamacpp:vulkan` backend, baseline (implicit `-b 2048 -ub 512`, no `-fa`) measured 304 tok/s prefill. `-b 4096 -ub 1024`: 318 tok/s (+4.7%, within noise given real concurrent load + user's active power cap -- not adopted). `-b 4096 -ub 256 -fa on` (a combo cited as a working production setup elsewhere): **18.9 tok/s -- a 16x regression** (793s vs baseline's 49s for the identical prompt) -- flash attention forced on is badly broken on this exact Vulkan/RADV+gfx1151+llama-server combination. Reverted immediately back to plain `-np 4 -kvu`, confirmed via `recipe_options.json` and `verify.sh` exit 0. Also fixed an unrelated staleness bug found while touching these files: README/verify.sh still said `-np 2` even though concurrency was bumped to `-np 4` live earlier this session. Hardware-verified (real load tests against the live service, with immediate revert of the regression); 100/100 tests pass. |
| 2026-07-06 | 副官 | claude-sonnet-5 | phase-2-memory-catchup-2026-07-06 | phase-2-memory#1.4,2.2,4.2,4.3,5.1-5.4,6,7.1-7.5,9.1,9.2,10.2,10.3,11.1,11.2 | bf7a370..7ed30ad | Audited phase-2-memory against shipped reality (scaffolded 2026-06-30, 0/44 tasks.md checkboxes despite that) and closed real gaps: fixed stale quadlet-gap README notes, restored embed-bge alias (litellm+models.yaml), fixed postgres image (vanilla->pgvector/pgvector, CREATE EXTENSION would've failed), added rag_chunks schema, implemented all 4 rag-ingest watchers (desktop/code/browser real; screen-audio stubbed pending Phase 3+OCR choice), added aipc rag CLI + doctor memory-rag checks (15 new tests, 115 total pass), wrote docs/memory-rag.md, reconciled tasks.md. rag-embedder's backing image and OCR/audio-transcript deliberately left as open proposals (mature-solution research), not hand-rolled. Static+render verified only; no hardware. |
| 2026-07-06 | 大哥 | claude-sonnet-5 | phase-7-ops-audit-2026-07-06 | phase-7-ops#1.2 | eb8df2b..82ef706 | Found a real cross-cutting bug while auditing phase-7-ops: 5 modules (ops-doctor/ops-backup/gaming-ai-overlay/gaming-base/game-strategy-rag) had post-install.sh referencing ${AIPC_MODULE_SRC}, a var never set by either renderer -- would crash with unbound-variable the moment any got enabled, and was redundant anyway since files/ COPY already places files at destination before post-install runs. Removed/simplified all 5. Also fixed 3 README dependency-list typos (data-postgres/data-qdrant/ai-voice-pipecat -> db-postgres/db-qdrant/voice-pipecat) and services.yaml's postgresql.service->postgres.service unit-name mismatch. Static+render verified (115 tests, both renders clean); no hardware. |
| 2026-07-06 | 副官 | claude-sonnet-5 | phase-6-dev-catchup-2026-07-06 | phase-6-dev#1.3,1.10,8.3,9.1,9.2,10.5 | d4cdbb9..0e57e81 | Reconciled phase-6-dev tasks.md with shipped reality (21/47 done); found+fixed 2 real bugs: fictitious @anthropic-ai/mcp-server-playwright npm package in dev-ai-mcp-dev-servers (404s, replaced with real @playwright/mcp), and dead pre-.ini-rename copy loop in dev-distrobox-templates/post-install.sh. Fixed docs/architecture.md Phase 6 table (missing dev-ai-opencode row, stale purposes). Flagged dev-ai-warp module-count bump as needs-proposal. Static+render-verified only (pytest 115/115, bootc+ansible render clean); no hardware access this session. |
| 2026-07-06 | 大哥 | claude-sonnet-5 | gfx1151-resume-hang-2026-07-06 | n/a (hardware incident, not tied to an openspec task) | 5acdde4..a4f8d7d | User's machine hard-crashed (3rd time this week) after unplug-AC + lid-close triggered s2idle suspend; resume failed with amdgpu SMU/MES cascade (No response msg_reg, ring gfx_0.0.0 timeout, all GPU reset paths failed) forcing hard power-off. Root-caused as distinct from the already-fixed GPP0/GPP1 suspend-entry hang -- this is a resume-time failure, hit with GPU confirmed idle, ruling out load as a precondition. Matches open upstream ROCm/ROCm#5590/#5724/#6165 (Strix Halo gfx1151 MES/SMU class, unresolved). Deep/S3 sleep unavailable on this hardware. Added amdgpu.cwsr_enable=0 kargs per user direction as a best-effort community workaround -- explicitly documented as NOT a confirmed fix, user will test via real use and report back. Static+render verified only; real verification requires an actual reboot + suspend/resume cycle on hardware. |
| 2026-07-06 | 大哥 | claude-sonnet-5 | ci-green-2026-07-06 | n/a (CI/build infra fixes, not tied to an openspec task) | 5e64924..d971b70 | CI had been red for a while, discovered while chasing a build for the gfx1151-resume-hang cwsr_enable fix. Fixed 4 independent pre-existing bugs blocking it: (1) ruff F841 unused 'pilot' var in 3 tools_tui tests, (2) aipc CLI symlink targeting /usr/local/bin which is a build-time-unwritable symlink to /var/usrlocal on this bootc image (same trap as db-postgres/secrets-sops) -- moved to /usr/bin, fixed in both renderers for parity, (3) CI's lint-test job missing pytest-asyncio since the TUI tests landed 2 days ago -- every async test failed in CI (passed locally only by coincidence of a pre-installed plugin), (4) llm-lemonade post-install.sh called systemctl daemon-reload, which needs a live PID-1 systemd unavailable in a build container -- classic build-time/runtime violation. Verified with a full local podman build (not just render-verified) end-to-end: all 73 Containerfile steps complete, image tags successfully, only pre-existing non-blocking bootc lint warnings remain. |
| 2026-07-06 | 大哥 | claude-sonnet-5 | gpu-hang-watchdog-2026-07-06 | n/a (incident follow-up, not tied to an openspec task) | 11f2059..2bcee96 | Added gpu-hang-watch.service to system-unified-memory: a 5s journal-poll daemon that captures kernel+full journal, amdgpu devcoredump, and AC/GPU state to /var/log/aipc/gpu-hang/<ts>/ + events.jsonl the moment the resume-from-s2idle hang signature (SMU no response/ring timeout/MES failed/GPU reset) appears, with a 10-min cooldown. Direct follow-up to gfx1151-resume-hang-2026-07-06 so the next occurrence doesn't need a live session manually reconstructing the timeline from journalctl, and so the devcoredump (which doesn't persist on its own) gets copied out before it's lost. Verified with a full local podman build: service correctly enabled in the image, script executable at the right path. pytest 115/115, both renders clean. Doesn't fix or prevent the hang -- diagnostic only. |
| 2026-07-05 | 副官 | claude-sonnet-5 | power-guard-implementation-2026-07-05 | power-guard-hardware-limit#1 | 58e908a | New module system-hardware-power-guard: dynamic power-limit state machine (Emergency/Reconnecting/Expanding/Cautionary) with hysteresis to prevent oscillation on AC reconnect, hardware-level CPU frequency clamping via sysfs, automatic recovery once supply is stable. Ships .disabled per CLAUDE.md §9 (render-verified, not yet hardware-verified). Row was left incomplete when the machine hung on suspend/resume mid-write (see next entry); backfilled from the commit's own trailer. |
| 2026-07-05 | 副官 | claude-sonnet-5 | suspend-gpu-hang-fix-2026-07-05 | n/a (no OpenSpec change; single-purpose bugfix module) | (uncommitted) | Root-caused a real hard hang: journalctl -b -1 showed PM: suspend entry (s2idle) at 10:30:42 with zero log entries after -- resume never completed, screen stayed lit/unresponsive for ~17h until forced reboot. An earlier resume that same morning (09:50) hit the same class of fault (amdgpu comp_1.0.1 ring timeout during resume, process=llama-server) but self-recovered via ring reset; this time it didn't. Root cause: suspending while the GPU compute queue has an in-flight job is unreliable on this gfx1151 resume path. New module system-suspend-gpu-guard (deliberately separate from system-hardware-power-guard -- that module's purpose is AC/battery back-feed clamping, unrelated to GPU/suspend) polls gpu_busy_percent + AC0/online every 5s; holds a systemd-inhibit --what=sleep --mode=block lock only while on AC AND GPU busy. First cut also added an active force-sleep-after-N-minutes-unplugged path; dropped it per user feedback -- the system's own idle-timeout/lid-close/manual suspend already exists and works fine once this guard's own block gets out of the way on battery, no need to reimplement it. Hardware-verified the mechanism itself, both branches: lock taken when AC+busy (real ambient gpu_busy_percent, no synthetic load needed), and confirmed absent even with GPU at real 100% busy when AC faked offline. NOT verified against an actual suspend attempt while the lock is held -- deliberately, same risk task #10 already flagged. Hotfix-deployed live to /etc/aipc/suspend-gpu-guard/ (unit ExecStart rewritten to the /etc path per live-hotfix-workflow.md); registered in ops-doctor services.yaml. 98/98 tests pass. |
| 2026-07-05 | 副官 | glm-5.2 | power-guard-host-service-rewrite-2026-07-05 | power-guard-hardware-limit#1 | 58e908a..f77dd0e | Rewrote the first draft after live-sysfs verification exposed two wrong premises: BAT0 exposes no current_now on this Strix Halo (detect back-feed via power_now<0 and status==Discharging+AC0 online instead) and a quadlet container cannot write cpufreq sysfs (EPERM) — restructured as a host systemd unit mirroring system-memory-oom-guard's shape. Controls amd-pstate-epp: scaling_max_freq hard ceiling + energy_performance_preference hint; state machine EMERGENCY/RECONNECTING/EXPANDING/CAUTIONARY with a 30s reconnect observation window; SIGTERM handler releases clamps on stop so the box is never left throttled. Also persists charge_control_end_threshold on boot (ASUS EC resets it to 100 before login; KDE only re-applies after session start) via charge_threshold_percent in config (default 90) — closes the reboot-gap the user reported. Added 'aipc power-guard enable|disable|status' (click) for at-will control; ships dry_run:true + .disabled. Hotfix-deployed live to /etc/aipc/power-guard/ (unit ExecStart rewritten to the /etc path per live-hotfix-workflow.md), dry_run observe-mode started and hardware-verified: self-test reads 32 cpus/AC online/power_now correctly, state machine advanced INIT->RECONNECTING->EXPANDING, dry_run provably wrote nothing (scaling_max_freq stayed 3000000, EPP stayed 'power'), enable/disable round-trip works (SIGTERM release confirmed in journal, sentinel + autostart symlink toggle). NOT yet verified: an actual back-feed clamp (dry_run still on — flip to false in config + restart once the log shows correct reactions). Also diagnosed the user's two battery UI questions: the 'unknown ELAN Stylus battery' is a harmless HID descriptor (hid-0018:04F3:43C7.0003-battery, PRESENT=0, upower already marks power-supply:no) — a KDE display concern, not a system-module one; and the charge cap reverting to 100% on reboot is exactly the EC-reset-before-login gap the new charge_threshold_percent persistence fixes. |
| 2026-07-05 | 副官 | glm-5.2 | power-guard-dryrun-live-and-tuned-coexist-2026-07-05 | power-guard-hardware-limit#1 | f77dd0e..HEAD | Flipped LIVE config to dry_run:false (repo config stays true = safe default for fresh builds; /etc is persistent across bootc switch). Two fixes found via the live flip, both real: (1) restart bug — INIT had skipped the observation window so a restart never throttled, but user correctly argued boot doesn't know which adapter (65W vs 140W) is attached, so INIT now also enters RECONNECTING (brief throttle after every start, accepted for safety); (2) tuned is active on this image and overwrites EPP to balance_performance — daemon was writing EPP=default on EXPANDING and getting clobbered within one poll. Resolution: guard writes EPP ONLY in EMERGENCY/RECONNECTING (power), stops touching it once stable, never restores on release. tuned/powerdevil own EPP; the guard owns scaling_max_freq (the hard ceiling they never write) — that is the clamp that actually bounds draw, so giving up EPP costs nothing functional and kills the oscillation. Verified live dry_run=false: INIT->RECONNECTING clamped cpu0 to 1.8GHz (0.6) with epp=power; charge_control_end_threshold held at 90. Confirmed no conflict with power-profiles-daemon (not present), tlp/asusd/asus-armoury (all inactive). aipc power-guard status shows dry_run/charge-cap/state/cur-factor correctly. |
| 2026-07-06 | 副官 | claude-sonnet-5 | power-guard-hardware-verify-2026-07-06 | power-guard-hardware-limit#1 | 020cfb9..59cf5e8 | Hardware-verified a real back-feed event on this AI PC: switched to a smaller/weaker charger, ran CPU-stress + 36B LLM inference simultaneously; daemon entered EMERGENCY, clamped to 0.45x freq, held the clamp across repeated EMERGENCY<->RECONNECTING cycles while load persisted (~2.5min), released cleanly once load ended, battery capacity dropped only 1% total. Also found and fixed a real detection gap during the same session: charge_threshold_percent boundary makes the EC report status=Not charging (not Discharging) while the system drains the battery directly -- daemon showed EXPANDING/no-reaction while capacity silently dropped 90->88%. Widened draining check to status in (Discharging, Not charging) plus an energy_now 2-poll-falling catch-all independent of status string. Removed .disabled per CLAUDE.md §9 (hardware-verified bar now met); README updated with the verification narrative and widened detection section. Also explored tuned profile switching (powersave-bazzite/balanced-bazzite/throughput-performance-bazzite) via read-only rocm-smi/tuned-adm inspection only -- did not force any GPU/CPU clock/power-state sysfs write per feedback_no_forced_gpu_clocks.md. 13/13 render+parity tests pass; both bootc and ansible renders now include the module. |
| 2026-07-06 | 副官 | claude-sonnet-5 | agent-orchestrator-hw-verify-2026-07-06 | phase-4-agent#1.1,2.1,2.2,7.1,7.2,7.3 | a0a042e | Ollama had an idle window; hardware-verified POST /chat -> supervisor -> LiteLLM -> ornith-35b returns HTTP 200 {text,task_id}. Removed .disabled, checked off already-implemented tasks.md items never marked done. Both bootc and ansible renders include the module and stay in sync. |
| 2026-07-06 | 副官 | claude-sonnet-5 | agent-tools-files-2026-07-06 | phase-4-agent#4.1 | 8b84d53 | Implemented read/write/list/delete in aipc_agent_tools_files/tools.py: every path resolved+checked against allowlist roots (default /var/lib/aipc-agent/workspace) before any FS syscall; delete outside workspace calls check_gate_grant() which fails closed (deny) since aipc-agent-gate #5.1 doesn't exist. Static+render-verified only (no hardware access): python self-test (traversal/symlink-escape/gate-fail-closed asserts) passes, tools/aipc render bootc + render ansible both include the module, aipc doctor shows OK. Not yet hardware-verified or wired into a real LangGraph sub-agent (task 2.4 will do that). Config moved to /etc/aipc/agent-tools/files-allowlist.conf per tasks.md wording; flagged the workspace-default and gate-fail-closed choices in README for 大哥 review. |
| 2026-07-06 | 副官 | claude-sonnet-5 | agent-orchestrator-daily-assistant-2026-07-06 | phase-4-agent#2.6 | 01d34a1..74d3579 | Implemented Daily Assistant sub-agent graph (daily_assistant.py): intent-3b via LiteLLM, ChatLiteLLM.bind_tools() + langgraph.prebuilt ToolNode/tools_condition (no new dependency), 3 tools -- calendar_lookup/email_lookup stubbed not_configured (agent-tools-calendar #4.2-4.4 unimplemented), files_read assumed interface 'from aipc_agent_tools_files import read_file' raising PermissionError outside allowlist, stubbing not_configured on ImportError. Wired into supervisor() via a minimal explicit keyword route (not a generic router -- only one sub-agent exists). Verified static/mocked only, no hardware access this session (CLAUDE.md section 9): graphs.py self_test() covers graph construction, keyword routing, tool-stub shape, intent-3b alias reaching ChatLiteLLM, all with ChatLiteLLM.invoke mocked; aipc render bootc and render ansible both re-confirmed green; tools/tests (115) unaffected. NOTE for reviewing 大哥: this commit (74d3579) also contains the parallel agent-tools-files#4.1 work -- both sessions shared one working tree and the other session's commit swept up my already-staged agent-orchestrator files, so one commit covers both Spec-Task 2.6 and 4.1 though its own trailer only names 4.1. Reconciled here: my diff is exactly modules/agent-orchestrator/{README.md,verify.sh,files/etc/aipc/agent/supervisor.yaml,files/usr/lib/aipc-agent/aipc_agent/{graphs.py,daily_assistant.py}}. Reconciliation happily confirmed: the real agent-tools-files read_file(path)->str raising PermissionError/AllowlistViolation matches the interface I assumed almost exactly, no follow-up wiring change needed beyond removing the ImportError fallback once that module is enabled in the render. |
| 2026-07-06 | 副官 | claude-sonnet-5 | agent-orchestrator-daily-assistant-2026-07-06 | phase-4-agent#2.6 | eab9f2f | Correction to the prior row on this same run: after that row was written, the parallel agent-tools-files session amended/rewrote commit 74d3579 down to 8b84d53 (agent-tools-files#4.1 only), which dropped my agent-orchestrator changes back to uncommitted working-tree edits -- no content was lost, just re-committed cleanly as eab9f2f on top of current HEAD. eab9f2f is the correct, final SHA for phase-4-agent#2.6 (daily_assistant.py + graphs.py routing + README + verify.sh + supervisor.yaml comment); 74d3579 no longer exists as a ref. Verification tier and content are unchanged from the prior row: static/mocked only, self_test() in graphs.py, both renders green. |
| 2026-07-06 | 大哥 | claude-sonnet-5 | daily-assistant-hw-verify-2026-07-06 | phase-4-agent#2.6,4.1 | 8ae7e0f..4401d35 | Reviewed both parallel dispatches' diffs (agent-tools-files 8b84d53, Daily Assistant eab9f2f) then live-hotfixed the Daily Assistant code onto the running service to actually hardware-verify it end to end (Ollama/Lemonade idle window). Found and fixed 3 real bugs the mocked-only dispatch couldn't catch: (1) intent-3b model alias doesn't exist post-manifest-trim; (2) next candidate resident-small runs on Lemonade's FastFlowLM NPU backend which 500s on any request carrying a tools list -- confirmed via direct curl comparison against ornith-35b, an NPU tool-calling limitation not a bug in our code; (3) ornith-35b returns content as a reasoning block list ([{type:thinking,...}]) that LangGraph re-sends verbatim on the next tool-loop turn, which llama-server rejects (unsupported content[].type) -- extracted the existing supervisor-only _text_of fix into aipc_agent/_util.py and applied it to daily_assistant.py's _agent and _finish nodes. Also recovered max_tokens=2048 and the original _text_of fix, which existed live on this machine (hardware-verified 2026-07-04/05) but a prior session never committed per live-hotfix-workflow.md step 5 -- folded back into the repo so bootc switch won't silently lose them. Final verification all green: self-test passes on the real venv; POST /chat with calendar and email keywords both route to daily_assistant -> ornith-35b -> stub tool -> coherent HTTP 200 reply; plain supervisor path regression-checked unchanged. Both bootc and ansible renders stay in sync. Checked off tasks.md 2.6 and 4.1. |
| 2026-07-06 | 副官 | claude-sonnet-5 | voice-pipecat-textout-2026-07-06 | phase-3-voice#1.1,5.1,5.2 | 442b476..d7dbb86 | User's explicit scope cut: text output is fine, TTS deferred. Removed voice-pipecat's placeholder quadlet (docker.io/pipecat/pipecat:latest doesn't exist for our purposes) and shipped a v0 CLI (aipc-voice-once) instead of the full Pipecat pipeline: record (arecord, 16kHz mono S16_LE, no new pip dep) -> STT POST (assumed contract: raw WAV bytes, Content-Type audio/wav, to http://127.0.0.1:9001/transcribe -- voice-stt-sensevoice was scaffold-only/being built in parallel this session, no real server.py yet) -> agent-orchestrator /chat (real, hardware-verified, untouched) -> notify-send text-out. Chose plain stdlib Python over pipecat-ai (confirmed real, pip-installable, latest 1.5.0) -- its streaming frame-pipeline abstraction doesn't pay for itself on a 4-step blocking round trip when neither wake-word nor TTS services exist yet; documented the reasoning in README for reconciliation once the real streaming pieces land. Wake-word training/classifier (task group 2) and the Super+Space global hotkey (D6) are explicitly out of scope -- CLI must be invoked manually for now. aipc-voice-mute.target and pipeline.yaml left in place untouched (no dependents, not wired into anything). Verification: static (ast.parse + --self-test offline logic checks) and render-verified (bootc/ansible both green, module correctly excluded from both since .disabled). Partial hardware-verified on the real AI PC: arecord recorded real audio from the machine's HD-Audio Generic ALC294 capture device; running aipc-voice-once end-to-end recorded successfully then failed cleanly (clear stderr message, non-zero exit, no crash) at the STT step since no real STT service exists yet; separately exercised chat()/show() directly against the live aipc-agent-orchestrator.service (POST /chat returned the expected reply) and real notify-send (exit 0, live DBus session/DISPLAY present). NOT verified: the full round trip through a real STT response -- module stays .disabled pending voice-stt-sensevoice landing and a real end-to-end recording. Shared-worktree note: voice-stt-sensevoice was being modified concurrently by another agent in the same tree; staged and committed only modules/voice-pipecat/* both times (git status + diff --cached --stat checked before each commit), that module's changes were left alone. |
| 2026-07-06 | 副官 | claude-sonnet-5 | voice-stt-sensevoice-2026-07-06 | phase-3-voice#1.3,3.1 | 104d0d9..cc8574d | Replaced modules/voice-stt-sensevoice's fake quadlet (docker.io/aipc/sensevoice:latest -- confirmed 404/nonexistent via skopeo inspect) with native systemd + Python venv, matching agent-orchestrator's precedent for the identical no-real-upstream-image problem. Implementation: files/usr/lib/aipc-voice/aipc_stt_sensevoice/server.py, a FastAPI app loading iic/SenseVoiceSmall via funasr.AutoModel (GET /healthz, POST /transcribe accepting raw audio bytes, no python-multipart dependency needed). Hardware-verified, not just render-verified: torch==2.9.1+rocm6.4 correctly detects the real gfx1151 iGPU (torch.cuda.get_device_name(0) == 'AMD Radeon 8060S'), but loading the model with device=cuda:0 SEGFAULTs inside libamdhip64.so (confirmed via coredumpctl, sig=11, a real ROCm/ HIP runtime crash, not our code) -- since a segfault kills the whole process a Python try/except cannot fall back gracefully, so the service now defaults to AIPC_STT_DEVICE=cpu (opt-in cuda:0 left wired for retry once torch/ROCm fixes gfx1151). CPU path fully verified end-to-end: real funasr model load (~5.6s), real transcription of an espeak-ng-synthesized WAV (rtf_avg 0.092, correct output 'Testing 1,2,3.'), and the actual FastAPI/uvicorn server round-tripped over HTTP (GET /healthz + POST /transcribe both returned correct real responses, not mocked). Also found and fixed a packaging gap: funasr's transitive deps (editdistance, crcmod, aliyun-python-sdk-core) have no Python 3.14 wheel and need Python.h to compile from source -- added python3-devel to packages.txt, verified against a real podman run of ghcr.io/ublue-os/bazzite-dx:stable (the exact FROM base this repo's Containerfile uses), confirming the fix works in the real build environment, not just this dev host. Both bootc and ansible renders include the module (post-fix) and stay in sync; 115/115 tests pass. Removed .disabled per CLAUDE.md §9's hardware-verified bar. Flagged for 大哥: tasks.md 1.3 names port 8101 but every other artifact (README, architecture.md, old quadlet) already assumed 9001 -- kept 9001, did not touch tasks.md per dispatch scope. |
| 2026-07-06 | 大哥 | claude-sonnet-5 | voice-textmode-hw-verify-2026-07-06 | phase-3-voice#1.1,1.3,3.1,5.1,5.2 | d7dbb86..b8be77d | Reviewed both parallel voice dispatches (voice-stt-sensevoice cc8574d, voice-pipecat d7dbb86), confirmed their assumed STT contract matched exactly, then hardware-verified the full join neither could test alone. Stood up voice-stt-sensevoice in a bazzite-dx container (matching this repo's real build base) since this dev host lacks python3-devel/ROCm torch -- reused the already-downloaded ModelScope model cache and host pip cache to avoid a second 4.5GB torch download. Ran the real aipc-voice-once CLI unmodified twice: (1) real ambient-room arecord capture -> STT garbage transcription -> /chat -> coherent reply -> notify-send, proving the plumbing; (2) espeak-ng synthesized 'what time is it' played through the real speaker via paplay while arecord captured from the real mic simultaneously (genuine acoustic loopback) -> SenseVoice transcribed 'What time in it.' -> ornith-35b gave a correct on-topic reply -> notify-send fired. Removed voice-pipecat's .disabled (voice-stt-sensevoice's was already removed by its own agent). Reconciled tasks.md: checked off 1.3/3.1 (voice-stt-sensevoice, fully done including a legitimate CPU-fallback per the task's own escape clause), fixed a pre-existing port typo (8101->9001), and annotated 1.1/5.1/5.2 instead of checking them since the v0 that shipped is a deliberately reduced slice (no TTS, no wake word, no hotkey, no cmd router, no retry) per explicit user direction that text-out is acceptable for now. Both bootc and ansible renders include both modules and stay in sync. |
| 2026-07-06 | 大哥 | claude-sonnet-5 | voice-mic-routing-fix-2026-07-06 | phase-3-voice#1.1 | 36a44fc | User reported aipc-voice-once transcribing garbage ('그.') regardless of what they said. Root cause: this session's earlier acoustic-loopback verification claim was wrong -- PipeWire's default recording source was the output sink's own monitor (pactl get-default-source -> alsa_output...analog-stereo.monitor), not the real mic (alsa_input...analog-stereo); the card's active profile was also output-only until this session's diagnosis, so no capture Source existed at all before that. arecord's default device silently followed whatever PipeWire called default, so the earlier paplay+arecord test captured a clean digital loopback of its own playback, not real acoustic mic pickup -- explaining why it transcribed near-perfectly with zero real-world noise. Fixed with pactl set-card-profile (add input side) and pactl set-default-source (point at the real alsa_input). Confirmed via direct sample measurement: true silence now reads all-zero (no loopback bleed-through), real ambient signal appears otherwise. Corrected the wrong claim in voice-pipecat/README.md rather than leaving it standing. Also stood up a persistent (non-ephemeral) voice-stt-sensevoice container at ~/.local/share/aipc-voice (moved off tmpfs /tmp so model/venv survive reboot) with --restart=always, and installed aipc-voice-once to ~/.local/bin so the user can invoke it directly. Not yet confirmed: a live human voice actually transcribing correctly through the fixed mic path -- needs a real person speaking, not verifiable by this session alone. pactl profile/default-source settings are runtime WirePlumber state, not committed anywhere, may not survive reboot. |
| 2026-07-06 | 大哥 | claude-sonnet-5 | agent-system-prompt-2026-07-06 | phase-4-agent#2.2,2.6 | 2b70fc6 | User reported the assistant 'doesn't know anything' after it replied to 帮我打开 firefox with a generic multi-OS how-to-open-an-app wall of text -- the tell of an LLM with zero identity/context. Root cause: neither supervisor (_respond) nor daily_assistant (_seed) ever sent a system message, every call was just the bare user string. Added SUPERVISOR_SYSTEM_PROMPT and daily_assistant.SYSTEM_PROMPT establishing identity (aipc assistant, local, this machine) and honest current capability limits (no screen control/app launch yet). Hardware-verified: same query now gets a direct two-sentence answer. Live-hotfixed to the running service; self-test passes; both renders green. |
| 2026-07-06 | 副官 | claude-sonnet-5 | agent-gate-2026-07-06 | phase-4-agent#5.1,5.2,5.4,5.5 | f8fe696..1064034 | New module modules/agent-gate: UNIX-socket RPC daemon (aipc-agent-gate.service, /run/aipc-agent-gate.sock, newline-delimited JSON, stdlib only -- no venv) implementing grant/revoke/check/status. GrantStore: in-memory dict for check()'s hot path (no SQLite hit), SQLite-backed persistence across restarts (still-valid grants reload: task-scope always, session-scope only if unexpired), background sweep thread (30s) purges expired session grants as hygiene (check() is already correct via timestamp comparison, sweep is not load-bearing for correctness). Append-only JSONL audit log at /var/log/aipc-agent-gate.jsonl for every grant/check/revoke; a real bug found and fixed here: an audit-log write failure (e.g. permission/disk-full) used to propagate an uncaught OSError and kill the RPC connection with no response -- now caught and logged to stderr so audit logging can never take down the actual grant/check/revoke path. CLI: aipc agent gate {grant,revoke,status} (tools/aipc_lib/gate_client.py, thin socket client) wired into tools/aipc_lib/cli.py. Module named agent-gate (not agent-tools-gate/agent-permission-gate) to mirror the aipc-agent-gate.service unit name; shipped as its own module rather than folded into agent-orchestrator or agent-screen-control per task 5.1's own wording leaving that choice open, since both those modules were being edited by other parallel dispatches in this same shared working tree at the same time. Task 5.3 (aipc agent screen ...) and the supervisor-side task-done signal wiring (task 5.4's caller) are explicitly out of scope, left as follow-ups; the RPC verb itself for bulk task-scope revoke is implemented and verified. Also updated modules/agent-tools-files/tools.py's check_gate_grant() (previously a fail-closed stub predating this daemon, sending plaintext) to speak the real JSON protocol -- verified live: grant files.delete then check_gate_grant returns True, revoke then False again; delete() itself needed no change. Verification: hardware-verified on the real Strix Halo AI PC (AMD RYZEN AI MAX+ 395, confirmed via /proc/cpuinfo) -- ran the daemon directly bound to the real /run/aipc-agent-gate.sock (0666 perms confirmed), full CLI round trip (grant/status/revoke, both session and task scope) against it, live-hotfixed a real systemd unit (same /usr-read-only-ostree workaround already documented for agent-orchestrator, ExecStart PYTHONPATH pointed at /var/lib/aipc-agent instead of the repo's real /usr/lib/aipc-agent path pending a real bootc switch) and confirmed a 2-second session grant actually expired on real wall-clock time, confirmed audit log correctly interleaved every event with no crashes, confirmed sweep_expired() purges from memory+SQLite directly. self_test()/--self-test (server.py) and a separate real-UNIX-socket integration test (tools/tests/test_gate_client.py, 3 cases) both pass; 118/118 repo tests pass; both bootc and ansible renders include the module and stay in sync; aipc doctor reports agent-gate OK. Not hardware-verified: the actual shipped ExecStart path (/usr/lib/aipc-agent) itself, since that directory does not exist pre-bootc-switch on this machine -- same known gap as agent-orchestrator's own README documents; the live-hotfit unit runs identical code via a path override instead. Module ships enabled (no .disabled) on this hardware-verified bar, matching agent-orchestrator/agent-tools-files precedent. Did not touch tasks.md, modules/agent-screen-control/, modules/voice-*, or modules/agent-orchestrator/ -- git status/diff --cached --stat checked before every commit to keep the parallel agent-screen-control dispatch's concurrent uncommitted changes in this same tree out of my commits. |
| 2026-07-06 | 副官 | claude-sonnet-5 | agent-screen-control-2026-07-06 | phase-4-agent#4.7,4.8 | 0f8bcf7 | Implemented ydotool input wrapper (mouse/key) + spectacle/LiteLLM VLM bridge under modules/agent-screen-control/aipc_agent_screen_control/{gate,window,input,vlm}.py. Every action calls gate.check_action() first: (1) aipc-agent-gate check RPC, (2) window-class blacklist (unknown class = fail closed, never treated as safe). Gate contract hardware-verified live against the real aipc-agent-gate.service that landed mid-dispatch (grant screen-control 60s session -> check_gate() True -> revoke -> False again; my assumed {cmd:check,action} -> {allowed,grant_id} JSON contract matched the real server exactly, no changes needed -- flagged that agent-tools-files' own earlier plaintext GRANTED-style assumption is the one still stale). Also hardware-verified: with a real grant active but kdotool absent, input.mouse_move() still correctly raised BlacklistedWindow (second fail-closed layer holds independently). spectacle screenshot capture hardware-verified (real 2560x1600 PNG). VLM bridge wire format (b64 encode/POST/endpoint) hardware-verified against real LiteLLM -- got the expected HTTP 400 since vlm-qwen2vl is confirmed NOT registered (cut in the 2026-07-04 models.yaml trim; no vision-capable model exists in LiteLLM at all right now, gpt4o-cloud/gemini-cloud are cloud aliases not checked for key presence -- flagged for 大哥, did not silently substitute). kdotool (window-class detection) is static-only: declared in packages.txt but this dev host's read-only ostree /usr blocks a live dnf install to test it, so get_active_window_class() always returns None here, making the blacklist fail-closed on every window by default until a real image build/hardware-verify happens. Real input injection was not tested per the safety constraint -- confirming a disposable target window's focus would itself need the untested kdotool piece, so skipped rather than fake a claim; ydotool mechanics were already confirmed working separately. Module stays .disabled (confirmed via discover() that this correctly excludes it from both bootc/ansible render and aipc doctor -- verified by temporarily toggling it off and back on). Removed an earlier stale scaffold config at /etc/aipc/agent/screen-control.yaml, replaced with the real D4 path /etc/aipc/agent-gate/screen-blacklist.conf. Both renders green throughout. |
| 2026-07-06 | 大哥 | claude-sonnet-5 | agent-orchestrator-memory-and-files-wiring-2026-07-06 | phase-4-agent#8.2 | (uncommitted) | User asked how far the repo is from a usable personal AIPC assistant; agreed shortest path is finish OOM guard safety posture (left untouched this run — already .disabled/dry_run per prior session, no new risk taken) then connect agent-orchestrator's /chat text path to memory + gated file tool, skipping voice/gaming/full RAG per explicit scope cut. Added modules/agent-orchestrator/files/usr/lib/aipc-agent/aipc_agent/memory.py: stdlib-only (urllib.request) best-effort mem0 HTTP client (recall/remember), 1s timeout, tries both /search and /memories/search route shapes since Phase 2's mem0 REST contract isn't pinned in this repo yet, resolves user_id via /run/user primary-uid lookup (no hardcoded username, matches oom-guard's own pattern) falling back to session_id. Wired memory.recall()/remember() into both graphs._respond() (supervisor) and daily_assistant._seed()/_finish() as additional SystemMessage context -- fails soft (empty string / silent no-op) when mem0 is unreachable, confirmed by self-test pointing ENDPOINT at a closed port. Fixed the pre-existing files_read() gap flagged in agent-orchestrator's own README (agent-tools-files landed as a sibling module but was never importable from this venv): post-install.sh now writes a .pth file exposing /usr/lib/aipc-agent to the venv site-packages, so `from aipc_agent_tools_files import read_file` resolves when that module is rendered, without copying code or adding a dependency; left the existing ImportError/PermissionError fail-closed branches untouched. Extended graphs.py's keyword router with file/memory terms so the Daily Assistant path (the only one with tools bound) gets reached for those requests too. Verification: static only, no hardware access this session (CLAUDE.md §9) -- python -m py_compile on all three changed files, memory.py's own self-test (route-shape formatting + fail-soft-on-unreachable-endpoint), graphs.py self_test() re-run against the real /var/lib/aipc-agent/venv (confirms recall()/remember() get called exactly once per path, /etc/passwd read still returns denied/not_configured never file content), sh -n on post-install.sh, openspec validate phase-4-agent --strict clean, both bootc/ansible renders regenerate with no diff-worthy drift, full tools/tests suite 118/118 green. Checked off tasks.md 8.2 (LangGraph import smoke, now also covering this self-test). NOT hardware-verified: no live mem0/Postgres instance exists on this dev host (Phase 2 modules stay .disabled) so recall()/remember() have never round-tripped against a real server, and the .pth install path itself has not been exercised through an actual post-install.sh run (only inspected/py_compiled). agent-tools-calendar/email backends, RAG, voice, and screen-control were explicitly out of scope and untouched. |
| 2026-07-06 | 大哥 | claude-sonnet-5 | oom-guard-branch-merge-2026-07-06 | n/a (repo process — long-diverged feature branch reconciliation) | c61a7e3..bfd4da8 | User chose solo-dev direct-merge over a PR for `feat/oom-guard-painless-kill-switch`, which had diverged from `main` far more than expected (8 conflicting files, including an entire unrelated cross-screen Dock-mirror CLI feature that had never landed on main). Resolved each conflict on its actual merits rather than picking one side wholesale: docs/agent-log.md and ops-doctor/services.yaml (pure append, kept both), llm-lemonade docs/verify.sh (main's `-np 4` + batch/flash-attention writeup is strictly newer, took main), system-unified-memory docs/verify.sh (two independent real issues -- main's resume-hang watchdog vs branch's USB4/Thunderbolt blackout fix -- kept both), agent-orchestrator graphs.py/post-install.sh (combined main's SELinux name_connect fix with the branch's _util.text_of refactor + Daily Assistant + this session's memory wiring), phase-4-agent tasks.md (took main's more detailed 2026-07-06 audit structure, then updated the specific items -- 2.6/4.1/4.7/4.8/5.1/5.2/5.4/5.5 -- that the audit had marked missing but which had since actually landed on the branch, without overclaiming what's still disabled/unverified), tools/aipc_lib/cli.py (two unrelated non-overlapping command groups, `aipc rag *` vs `aipc config preset mirror-dock/...`, kept both). Verified post-merge: 135/135 tests, both renders clean, openspec validate clean. Pushed to origin/main. |
| 2026-07-06 | 副官 | claude-sonnet-5 | phase4-catchup-code-shell-2026-07-06 | phase-4-agent#1.2,3.1,3.2,3.3 | 0603e60 (merged bcc15ea) | Isolated-worktree dispatch (1 of 5 parallel teammates on the 2026-07-06 audit's remaining Phase-4 items, explicit user go-ahead per CLAUDE.md §10). Implemented agent-code-shell's safety wrapper: `aipc-code-shell` always routes through `distrobox enter agent-runtime -- <cmd>`, fail-closed (zero subprocess calls) both when `distrobox` is missing and when already inside a container (`/run/.containerenv`); rewrote the spec-path `.../distrobox/templates/agent-runtime.yaml` (the pre-existing file was at the wrong path, no `templates/` subdir); wired Open Interpreter install into `init_hooks`. Real finding, documented not silently claimed compliant: this distrobox version (1.8.2.5) has no `--no-home`/`--unshare-home` flag, so the real host home remains bind-mounted inside the sandbox at its original path despite the `home=` override -- verified directly against the real distrobox binary on this dev host (`--dry-run`), not assumed. Verification: static (`sh -n`/`py_compile`) + self-test (`aipc-code-shell --self-test` drives the actual refusal logic) + render-verified (manually toggled `.disabled` off/on to confirm the module's files actually appear in both generated outputs, then restored it) + 135/135 tests. NOT hardware-verified -- no physical AI PC access, module stays `.disabled`. Reserved `cli.py`/`daily_assistant.py`/`tasks.md`/`agent-log.md` for 大哥; touched only `modules/agent-code-shell/**`. |
| 2026-07-06 | 副官 | claude-sonnet-5 | phase4-catchup-browser-2026-07-06 | phase-4-agent#1.3,4.9,6.3 | dc42e42 (merged 6153108) | Isolated-worktree dispatch (2 of 5). Implemented agent-browser's real Playwright MCP install: idempotent `jq` merge of the `playwright-agent` entry into `/etc/aipc/mcp/servers.json` (build-time-safe, no `npm install` at build -- Node/npm only exist inside the `node` distrobox, matching the repo's established sudo-bridge caveat), `verify.sh` now checks the binary is actually present, distinct `profile_path` (`/var/lib/aipc-agent/browser-profile`) from `dev-ai-mcp-dev-servers`'s ephemeral npx profile (task 4.9). Beyond static: this dev sandbox happened to have a real `node` distrobox with network access, so `npm install -g @playwright/mcp` was run for real and confirmed the binary lands (v0.0.77) -- stronger than pure static, still not a CLAUDE.md §9 hardware-verified claim (not the physical Strix Halo box). Confirmed inclusion in both bootc/ansible renders via the same manual `.disabled`-toggle technique as teammate 1. 135/135 tests pass. Module stays `.disabled`. Touched only `modules/agent-browser/**`; `docs/agent-log.md` explicitly out of scope, left for 大哥. |
| 2026-07-06 | 副官 | claude-sonnet-5 | phase4-catchup-calendar-2026-07-06 | phase-4-agent#1.6,4.2,4.3,4.4 | 45530d9 (merged 04f0a9d) | Isolated-worktree dispatch (3 of 5), the OAuth/credentials-handling one -- CLAUDE.md §5 applied throughout, nothing baked. Implemented all three agent-tools-calendar backends as a clean importable package mirroring agent-tools-files' layout: Google (lazy-imported `google-api-python-client`/`google-auth-oauthlib`, ImportError -> not_configured, token stored `/var/lib/aipc-agent/oauth/google.json` 0600), Proton (stdlib CalDAV `REPORT` query + Basic auth, no `python3-caldav` dependency), Fastmail (same stdlib CalDAV + `imaplib`, app password read from a `password_file`, never inline in config). Public `lookup_events`/`lookup_email` dispatch google->proton->fastmail, same `{"status":...}` shape the orchestrator's stubs already use. Real gap found and flagged (not fixed, out of scope): `agent-orchestrator`'s venv has no `--system-site-packages`, so the Google RPMs won't be visible to `daily_assistant.py` even once installed. No live OAuth/IMAP/CalDAV exercised (no credentials in this environment) -- self-test covers token store/refresh/read round-trip and CalDAV VEVENT parsing with stubbed network boundaries instead. 135/135 tests pass; module stays `.disabled`. Reserved `cli.py`/`daily_assistant.py` for 大哥, provided the exact `aipc agent oauth google` Click snippet and the exact `calendar_lookup`/`email_lookup` replacement bodies -- both applied verbatim by 大哥's reconciliation pass. |
| 2026-07-06 | 副官 | claude-sonnet-5 | phase4-catchup-search-2026-07-06 | phase-4-agent#1.7,4.5,4.6 | d83b7fd (merged bee322c) | Isolated-worktree dispatch (4 of 5). Implemented agent-tools-search's `search_searxng`/`search_tavily` wrapper, same stdlib-`urllib.request`/fail-soft pattern as `agent-orchestrator/aipc_agent/memory.py` (no new HTTP dependency): SearXNG queried via the module's own `env/endpoint` (added -- didn't previously exist), Tavily only advertised (`available_tools()`) when a real key resolves, never baked. Real gap found and flagged (not fixed, out of scope): `secrets-sops`'s `decrypt-cloud-keys.sh` awk filter never extracts `tavily_api_key` at all, so `search_tavily` will correctly self-report `not_configured` forever until that separate script is updated -- verified by reading the actual filter, not assumed. Self-test covers JSON parsing, fail-soft against a closed port, and the with/without-key gating. 135/135 tests pass (baseline, orchestrator untouched by this dispatch); module stays `.disabled` (`verify.sh` correctly short-circuits exit 2). Reserved `daily_assistant.py` for 大哥, provided the exact `search`/`search_tavily` `@tool` snippet -- applied verbatim. |
| 2026-07-06 | 副官 | claude-sonnet-5 | phase4-catchup-gate-mcp-2026-07-06 | phase-4-agent#5.3,6.1,6.2,6.4 | 30493bb (merged fcf8d83) | Isolated-worktree dispatch (5 of 5). Implemented `tools/aipc_lib/screen_client.py` (thin wrapper over the already-hardware-verified `gate_client.send()` -- no RPC logic duplicated; `--mode always` maps to a fixed task-scope sentinel since the gate only has session/task scopes, `--revoke` therefore only cancels the always-on grant, documented as a `ponytail:` upgrade path) plus a new `tools/tests/test_screen_client.py` that spins up a real (ephemeral) gate daemon, not a mock -- 4 new tests, 139/139 total pass. Also documented `agent-mcp-gateway`'s JSON schema (6.1), seeded the empty registry (6.2). Task 6.4 investigation (read-only, `dev-ai-mcp-dev-servers` untouched): found it does NOT conform to the schema -- ships a flat `{name:{command,args,env}}` object, no `version`/`servers` wrapper, no `transport`/`enabled`/`consumers`; both modules are `.disabled`, but alphabetical `discover()` order means enabling both would let `dev-ai-mcp-dev-servers`'s COPY silently overwrite `agent-mcp-gateway`'s seed with the non-conforming file -- flagged in the README, left for Phase 6 per this dispatch's scope boundary rather than fixed opportunistically. Not hardware-verified: no real `/run/aipc-agent-gate.sock` in this environment, only the ephemeral test-harness daemon. Reserved `cli.py` for 大哥, provided the exact `aipc agent screen` Click snippet -- applied verbatim. |
| 2026-07-06 | 大哥 | claude-sonnet-5 | phase4-catchup-reconciliation-2026-07-06 | phase-4-agent#1.2,1.3,1.6,1.7,3.1,3.2,3.3,4.2,4.3,4.4,4.5,4.6,4.9,5.3,6.1,6.2,6.3,6.4 | (this commit) | Merged all 5 isolated-worktree teammates' branches into `main` -- zero conflicts (disjoint module directories, confirmed before dispatch by construction: each teammate's file boundary was assigned up front). Then applied the reserved shared-file wiring each teammate's report specified verbatim: `daily_assistant.py`'s `calendar_lookup`/`email_lookup` stub bodies replaced with real imports from `aipc_agent_tools_calendar` (teammate 3), two new `@tool` functions `search`/`search_tavily` added to `TOOLS` wrapping `aipc_agent_tools_search` (teammate 4), `cli.py` gained `aipc agent oauth google` (teammate 3) and `aipc agent screen` (teammate 5) command groups. Cross-checked every snippet's function signature against the real target module before applying (`run_google_oauth_flow`'s positional/keyword args, `screen_client.grant_session`'s `str`-typed duration, both packages' `__init__.py` exports) rather than pasting blind. Reconciled `openspec/changes/phase-4-agent/tasks.md`: checked off every item the 5 dispatches actually completed, using `[~]` (not bare `[x]`) wherever only static/render-verified evidence exists and the module stays `.disabled`, with each real finding (distrobox no-`--no-home`, missing `tavily_api_key` extraction, orchestrator venv missing `--system-site-packages`, `dev-ai-mcp-dev-servers` schema mismatch) carried through into the task notes rather than silently dropped. Verified after every step: 139/139 tests, both bootc/ansible renders clean, `openspec validate phase-4-agent --strict` passes. Still unbuilt and out of this dispatch's scope: Researcher/Coder/Browser sub-agents (2.3-2.5), the LangGraph checkpointer (2.7), and most of Groups 8/9/10/11/12 (doctor checks, docs, build/hardware verification, archive). |
| 2026-07-06 | 大哥 | claude-sonnet-5 | memory-mem0-real-server-2026-07-06 | phase-2-memory#6.1,6.2,6.3 | (this commit) | User's Claude Code mem0 plugin hit its cloud SaaS retrieval quota (1000/1000, screenshot shown); confirmed that plugin's MCP server is hardcoded to `mcp.mem0.ai` with no self-host option (read its `.mcp.json`/README directly -- "No local dependencies required" is explicit), so redirected the ask to what's actually fixable: `modules/memory-mem0`, disabled since scaffolding with "enablement is future work". Root-caused why it was never enabled: the quadlet's `docker.io/mem0/mem0:latest` doesn't exist (`skopeo inspect`: access denied) and the real published image `mem0/mem0-api-server` has zero amd64 variants on Docker Hub (arm64/unknown only) -- same fictitious/wrong-arch-image bug class as voice-stt-sensevoice/dev-ai-mcp-dev-servers/agent-browser, fixed the same way: replaced the quadlet with a native `mem0.service` (systemd+venv) wrapping the real `mem0ai` 2.0.11 PyPI package directly. Config: `vector_store=pgvector` against `db-postgres`'s `aipc` DB, `llm`/`embedder` both `openai` provider with `openai_base_url` at the LiteLLM gateway (mem0's own bundled `litellm` provider is the in-process SDK, not this repo's gateway, so intentionally not used -- CLAUDE.md §7), `MEM0_TELEMETRY=False` in the unit (mem0 phones home to PostHog by default, disabled per CLAUDE.md §6). Real functional testing via podman on this dev host (not the physical AI PC, not hardware-verified): started the actual `pgvector/pgvector:pg16` image `db-postgres` uses, applied its real `init-pgvector.sql` (confirmed the `:ro,Z` SELinux flag the quadlet already carries is genuinely required -- a bare `:ro` mount 403s on this host), then `Memory.from_config()` against it -- succeeded, real pgvector connection + collection creation. Found and fixed a real dependency gap along the way: mem0's pgvector backend imports `psycopg_pool`, so `requirements.txt` now pins `psycopg[binary,pool]`, not bare `psycopg[binary]`. Ran the real FastAPI app over real HTTP: `GET /healthz` -> 200; `POST /search` reached a real `Memory.search()` -> real pgvector query -> real LiteLLM call, and failed with a precise traced error (`Invalid model name passed in model=embed-bge`) because this dev host's *live* LiteLLM service doesn't have `embed-bge` loaded despite it being in the repo's committed config -- a pre-existing drift, not touched (out of this module's file scope, and restarting a shared live service without being asked is not this dispatch's call to make). Confirmed the server survives that error (`/healthz` still 200 immediately after). Confirmed inclusion in both bootc/ansible renders via the manual `.disabled`-toggle technique. Torn down all test state after (podman container, venv, `/var/lib/aipc-mem0`, background uvicorn) -- nothing left running. Noticed and deliberately left alone: unrelated uncommitted changes already in this shared working tree (`qwen35-122b-q3` model registration touching llm-lemonade/llm-litellm/llm-models) from a concurrent session -- staged and committed only `modules/memory-mem0/**` and `openspec/changes/phase-2-memory/tasks.md`. Module stays `.disabled` (functional/render-verified, not hardware-verified; also blocked on the target machine's LiteLLM actually serving `embed-bge`). 139/139 tests, both renders clean, `openspec validate phase-2-memory --strict` passes. |
| 2026-07-08 | 副官 | gpt-5.5-high | memory-local-mcp-hw-verify-2026-07-08 | phase-2-memory#1.3,6.1,6.2,6.3,6.5,12.2 | (uncommitted) | Finished the local mem0 path on the physical Strix Halo AI PC and repointed agents away from Mem0 SaaS: stood up `aipc-rag-embedder` + `aipc-mem0`, pre-staged bge-m3 under `HF_HOME`, verified `embed-bge` through LiteLLM, and verified mem0 add/search round-trips through pgvector with both infer=false and infer=true. Root-caused two hardware-only failures and synced fixes to repo: venv entrypoints labeled `etc_t`/`lib_t` left services in `init_t` (denying HF egress and oneDNN JIT mmap-exec), so post-install relabels `venv/bin` to `bin_t`; bge-m3 first request tried runtime HF fetch, so the rag unit runs offline after pre-staging. Added local stdio MCP server `aipc_mem0.mcp_server` plus `tools/aipc_mem0_point_mcp_local.sh`; live Claude Code plugin cache, opencode, and hermes now point at local mem0. Exposed the repair path as `aipc config tools`: the TUI row is service-first (`mem0 local service`) with `Configure Claude` / `Re-apply Claude` actions, while `--mem0-local` remains the headless Claude-plugin-cache option. Found and fixed the MCP `history.db` ownership bug with `aipc-mem0-state-dir.service`, resolving `AIPC_PRIMARY_USER`/uid 1000-59999 instead of hardcoding the live username. Updated memory/rag READMEs and phase-2 tasks. Verification tier: hardware-verified for live services/MCP round trip; static checks passed (`py_compile`, targeted mem0/config-tool pytest 26/26, `openspec validate phase-2-memory --strict`). |
| 2026-07-08 | 副官 | gpt-5.5-high | runtime-guards-storage-cleanup-2026-07-08 | n/a | (this commit) | Committed the remaining validated working-tree cleanup after the local mem0 branch: moved the experimental qwen35-122b-q3 route from Lemonade to Ollama/Vulkan with LiteLLM/model-manifest wiring and a Lemonade unload helper/test; kept Lemonade service startup bounded with SIGABRT on stop timeout; tightened power/oom guard behavior around charge-cap/no-drain and non-pinned Lemonade eviction; added a guarded `aipc storage reclaim-live` planner plus docs/tests for reclaiming an adjacent AIPC_LIVE partition. Verification: `py_compile` on changed Python, targeted tests 44/44, full tools tests 187/187, render generation for bootc+ansible with no generated diff, render/parity tests 15/15. Not committed: `openspec/changes/codexbar-usage-integration/` because `openspec validate codexbar-usage-integration --strict` fails with no spec delta. |
| 2026-07-08 | 大哥 + 副官 | gpt-5.5-high / claude-haiku-4-5 | phase-3-voice-2026-07-08 | phase-3-voice#4.3,#7.1,#8.1,#8.2,#9.2,#10.1,#10.2,#12.1 | 68c101c..HEAD | Staged local AIPC voice assistant completion: added `aipc-voice-bind-hotkey`, voice doctor checks/tests, `docs/voice-pipeline.md`, opportunistic stdlib TTS router with `notify-send`/stdout fallback, and recorded static/render verification. Verification tier: static + render-verified only (`doctor` voice/memory tests 8/8, `voice-pipecat` verify, `voice-stt-sensevoice` static OK, OpenSpec strict valid, bootc/ansible renders via current Python CLI). Hardware handoff remains pending; 11.x hardware tasks stay unchecked until exercised on the physical Strix Halo AI PC. |
| 2026-07-09 | 副官 | glm-5.2 | disk-cleanup-lemonade-repoint-2026-07-09 | n/a | (this commit) | Disk-pressure cleanup + model-store consolidation on the physical Strix Halo AI PC. Reclaimed ~141G (disk 96%->67%, free 24G->165G): deleted `~/models/qwen3.5-122b-a10b` (93G standalone GGUF, redundant with Ollama's `qwen3.5:122b`, download incomplete `00001-of-00002`, zero repo refs), `~/.cache/pip` (5.1G), 3 podman dangling images, and `/var/lib/aipc-lemonade/huggingface/hub/models--unsloth--Qwen3.5-122B-A10B-GGUF` (~54G stale Lemonade copy of the model moved to Ollama 2026-07-07 per `configure-lemonade.sh`). Repointed Lemonade's HF cache volume to the shared root: `lemonade.service` now mounts `/var/lib/aipc-models/hf:/root/.cache/huggingface:Z` instead of `/var/lib/aipc-lemonade/huggingface`; moved the two live models (Ornith-1.0-35B, gemma-4-26B-A4B-it) into `~/aipc-models/hf/hub` so all weights — Ollama blobs + Lemonade HF — live under one path. Live-deployed via the hotfix loop (edit repo -> sudo cp to /etc -> daemon-reload -> restart). Hardware-verified: `lemonade.service` active, `ExecStartPre` mkdir of the new path SUCCESS, lemond booted (`NPU hardware: Yes`, 121.5GB RAM), `ModelManager Cache built: 144 total, 4 downloaded` confirms it reads the relocated models, zero SELinux `container_file_t` denials after the `:Z` relabel, `/api/v0/models` responds. Backup at `/etc/systemd/system/lemonade.service.bak-pre-hf-repoint` for rollback. No OpenSpec change filed (user directive, implement-first); should be backfilled if the consolidation is to be treated as a durable module contract. |
| 2026-07-09 | 副官 | glm-5.2 | openspec-backfill-disk-install-2026-07-09 | n/a | (this commit) | Backfilled the three OpenSpec changes the earlier disk-cleanup work had skipped, plus an install-layout fix discovered while investigating why `aipc storage reclaim-live` cannot run on this host. (1) `install-windows-direct` (existing change, extended): added `system-foundation` requirement that the R6b diskpart step place `AIPC_LIVE` *after* the bazzite root region via explicit offset, so post-install order is `[Windows][root][AIPC_LIVE]` — root can only grow toward its end, so the original before-root layout stranded the 30G permanently unreclaimable (observed on the physical host). (2) New `reclaim-live-root-detect`: `storage_reclaim.py:load_plan` reads `findmnt SOURCE /` which returns `composefs` on ostree/composecs -> `root partition composefs not found`; fix resolves the backing device via `/sysroot`->`/var`->`/` and strips the btrfs `[subvol]` suffix; adjacency guard kept (depends on change 1's layout). (3) New `lemonade-model-consolidation`: codifies today's HF-cache repoint under `/var/lib/aipc-models/hf` as the `ai-runtime` requirement that Ollama + Lemonade weights share one root. Root cause across all three: the install flow and the reclaim tool disagreed about where AIPC_LIVE ends up, and root detection assumed a non-ostree `/`. All three `openspec validate --type change --strict` pass. No code changed in this commit (reclaim-live code fix is specced, tasks left open). |
| 2026-07-09 | 副官 | glm-5.2 | reclaim-live-composecs-fix-2026-07-09 | reclaim-live-root-detect#1,2,3 | 9130905 | Implemented the reclaim-live composecs root-detection fix (change #2 from the prior backfill). `storage_reclaim.py:load_plan` no longer reads `findmnt SOURCE /` (returns "composefs" on ostree); new `_resolve_root_device()` walks /sysroot -> /var -> /, takes the first /dev/-backed mount, strips any btrfs [subvol] suffix, and skips missing mount points (plain-host case). build_plan's adjacency guard unchanged, now commented. 3 new unit tests (ostree composecs fall-through, plain host, no-block-device raise); 12/12 green, ruff clean, openspec validate --type change --strict valid. Hardware dry-run on this Strix Halo host: root now resolves correctly and the (unchanged) adjacency guard correctly refuses it -- "AIPC_LIVE is not immediately after the root partition" -- instead of the "root partition composefs not found" abort; destructive path not hardware-verified (no R6b-after-root host exists). |
| 2026-07-09 | 副官 | glm-5.2 | reclaim-live-composecs-fix-2026-07-09 | reclaim-live-root-detect (archived) | c69af3b | Confirmed the change fully done and archived it. Did task 3.1 first (composecs corrigendum appended to docs/superpowers/specs/2026-07-07-reclaim-aipc-live-design.md). Ran `openspec archive reclaim-live-root-detect --yes`: applied the ADDED requirement to openspec/specs/system-foundation/spec.md (first archived spec in this repo — openspec/specs/ was empty), moved the change to openspec/changes/archive/2026-07-08-reclaim-live-root-detect/, left a `## Purpose` TBD placeholder which I filled in. `openspec validate system-foundation --type spec --strict`: valid. `openspec validate --all --strict`: 14/15 — sole fail is codexbar-usage-integration (no spec delta, predates this, someone else's uncommitted change). |
| 2026-07-09 | 副官 | gpt-5.5-high + subagents | z13-side-button-voice-trigger-2026-07-09 | phase-3-voice#7.1 | 41b396d | Added static/render-verified ASUS ROG Flow Z13 side-button voice trigger path: `F20` hotkey config, KDE autostart binder, ASUS discovery helper, safe hwdb template, renderer `systemd-hwdb update`, docs and OpenSpec status. Verification tier: static + render-verified only (`voice-pipecat` verify, `system-asus-input` verify, 26 pytest checks, OpenSpec strict valid, bootc/ansible render marker checks). Hardware side-button press remains pending; 11.x hardware tasks stayed unchecked. |
| 2026-07-09 | 副官 | grok-4.5 | model-uncensored-presets-122b-2026-07-09 | n/a | working-tree | coder-agentic→HauhauCS Qwen3.6-35B Aggressive Q4_K_P (Lemonade); assistant-gemma=HauhauCS Gemma4-26B QAT Balanced; enable_thinking false. Interrupt zombies: -np 1 -kvu. 122B optimized qwen3.5:122b-aipc (ctx 65k, keep_alive 15m). aipc models use agent|122b|free one-shot mutual exclusion (tests 8/8; free live freed ~100Gi available). dealignai Gemma CRACK rejected (MLX). Uncommitted. |
| 2026-07-09 | 副官 | grok-4.5 | voice-sensevoice-selinux-live-2026-07-09 | phase-3-voice#1.3,3.1 | (this commit) | Unblocked local voice assistant Stage-1 path on physical Strix Halo: `aipc-voice-stt-sensevoice` was crash-looping with status=203/EXEC (SELinux `var_lib_t`/`lib_t` on venv entrypoints — same class as mem0/rag-embedder). Repo durable fix: `post-install.sh` relabels `…/venv/bin` to `bin_t`; unit invokes `python3 …/uvicorn` and sets `AIPC_STT_DEVICE=cpu`. Live-hotfix: chcon + unit rewrite under `/var/lib/aipc-voice` (ostree `/usr` RO). First start downloaded SenseVoice `model.pt` (~936MB) into MODELSCOPE_CACHE. Hardware-verified: `GET /healthz` ok device=cpu; `POST /transcribe` on espeak WAV; `POST /chat` 200; full `aipc-voice-once --seconds 3` mic→STT→/chat→notify with real Chinese ambient speech. Render-verified bootc+ansible include module. Not done: wake/TTS/full Phase-3 (still deferred). |
| 2026-07-10 | 副官 | grok-4.5 | voice-tts-hotkey-wake-2026-07-10 | phase-3-voice#1.2,1.6,2.1,4.2,7.2,7.5 | (this commit) | Completed Stage 2+3 slice for local assistant: (1) local TTS on :8880 stdlib espeak-ng OpenAI-speech API + aipc_voice_tts play path (fixed paplay endswith aplay bug); (2) aipc-voice-bind-hotkey accepts qdbus, best-effort KDE reload, wrote F20→aipc-voice-once; Asus discover present; (3) voice-wake energy VAD service + mute.target flag + train stub + screenlock autostart. Hardware-verified: healthz TTS, speak EN/ZH, aipc-voice-once spoke reply via TTS, wake active, mute flag toggle. Render-verified bootc+ansible include voice-tts-kokoro+voice-wake. OpenWakeWord custom ONNX fit + neural Kokoro/CosyVoice still deferred. |
| 2026-07-10 | 副官 | grok-4.5 | kokoro-neural-zh-tts-2026-07-10 | phase-3-voice#1.6,4.2 | (this commit) | Replaced espeak placeholder with real Kokoro-82M neural TTS (ghcr.io/remsky/kokoro-fastapi-cpu:v0.2.4 on :8880). Mandarin default voice zf_xiaoxiao (+ zf_*/zm_* packs); English af_heart. Client CJK-ratio routing + OpenAI speech WAV play. Hardware-verified: health healthy, zh+en synthesis+paplay, speak() True. User systemd aipc-kokoro.service enabled. CosyVoice clone still deferred. |
| 2026-07-10 | 副官 | grok-4.5 | voice-npu-placement-2026-07-10 | phase-3-voice | (this commit) | Investigated NPU TTS on Strix Halo Linux: FLM NPU stack ready (flm validate ready=true, /dev/accel/accel0). Lemonade kokoro-v1 is CPU recipe only; load failed downloading kokoros binary (GitHub 504). FLM has whisper ASR not TTS. Documented compute placement: TTS stays Kokoro CPU container :8880; NPU reserved for FLM LLM + future wake/STT. |
| 2026-07-10 | 執行兵 | grok-4.5 | voice-zh-default-xiaoyi-2026-07-10 | phase-3-voice | c135641 | Default AIPC_TTS_VOICE_ZH zf_xiaoxiao → zf_xiaoyi. Live hotfix + self-test. |
| 2026-07-10 | 副官 | grok-4.5 | voice-xiaoyi-lock-config-2026-07-10 | phase-3-voice | (this commit) | Locked zf_xiaoyi; added /etc/aipc/voice/tts-zh-voice + tts-en-voice config files; E2E once spoke TTS; F20 rebound to /usr/local/bin/aipc-voice-once; wake active. |
| 2026-07-10 | 大哥 | grok-4.5 | bt-lg-xt7s-a2dp-routing-2026-07-10 | n/a | (no commit) | Hardware-diagnosed LG-XT7S(44) 68:52:10:35:29:44: KDE showed Connected/音效 but no audio — bluetoothd `a2dp_select_capabilities Unable to select SEP` / `a2dp-sink profile connect failed: Device or resource busy`; PipeWire had no bluez sink; configured default still pointed at AirPods. Fix: restart user wireplumber+pipewire+pipewire-pulse then reconnect → bluez_output.68_52_10_35_29_44.1 appeared (A2DP AAC); set default sink + volume ~25%. After BT off, built-in did not 'return' because runtime default fell to HDMI (XV275K) while default.configured.audio.sink stayed sticky on dead bluez — analog sink was still present (alsa_output.pci-0000_c4_00.6.analog-stereo / ALC294). Fixed with pactl set-default-sink analog + pw-metadata default.configured.audio.sink to analog-speaker port; moved Zen streams. Hardware-verified on Bazzite/PipeWire 1.6.7 only; no module/code change. See claude-ops LESSONS L4. |
| 2026-07-10 | 大哥 | grok-4.5 | cosyvoice-teammates-prep-2026-07-10 | phase-3-voice#1.5,4.3,6.1 | 8b27630..1b4a5eb | Dispatched 3 teammates: T1 cosyvoice module scaffold (.disabled), T2 CJK→:9880 client routing, T3 docs+persona.yaml. Clone sample cleaned (mild denoise) at persona/clone.wav. Model CosyVoice2-0.5B download still in progress under /var/lib/aipc-voice/models/cosyvoice2. |
| 2026-07-10 | 副官 | grok-4.5 | voice-parallel-prep-while-cosy-dl-2026-07-10 | phase-3-voice | (this commit) | Parallel prep while CosyVoice weights download: honest model_present, prefer-cosyvoice default 0, aipc-voice-status + aipc-voice-record-clone, mic 55% autostart, live cosy unit degraded, Kokoro primary. |
| 2026-07-10 | 大哥 | grok-4.5 | baseline-resident-stack-2026-07-10 | n/a | (this commit) | Document standing always-on baseline: resident-small + SenseVoice + Kokoro + mem0. Role presets agent/122b only exclusive at heavy-LLM layer; model_presets docstring + voice-pipeline updated. No runtime unload of baseline services. |
| 2026-07-10 | 副官 | grok-4.5 | aipc-portal-v0-2026-07-10 | aipc-portal-v0#1-6 | (this commit) | Portal-only v0: OpenSpec `aipc-portal-v0`, module `system-aipc-portal` (stdlib HTTP 127.0.0.1:7080), metadata cards (mem0/sensevoice/kokoro/litellm/lemonade/pipecat/self), `aipc portal`/`open`/`serve` with live-host repo fallback. No official Mem0 dashboard rewrite. Static+render verification; hardware unit start deferred — use `aipc portal serve` pre-bootc. |
| 2026-07-10 | 大哥 | grok-4.5 | closed-loop-docs-2026-07-10 | n/a | (this commit) | Document always-on **closed loop** in project: docs/architecture.md goals + Phase 3, docs/voice-pipeline.md, voice-pipecat/agent-orchestrator/system-aipc-portal READMEs, model_presets.py. Loop = SenseVoice→resident-small/chat→Kokoro+mem0+portal; voice open dashboard; aipc voice status|loop. |
| 2026-07-10 | 副官 | grok-4.5 | assistant-chatgpt-online-v0-2026-07-10 | assistant-chatgpt-online#1-5 | (this commit) | Modular super-aggregator + multi-site Playwright engine (ChatGPT pack), NPU-first local/control (resident-small), unified text entry, voice→aggregator, auth session export, first-run setup UX, OpenSpec change. Online module ships .disabled until Voice hardware QA. |
| 2026-07-10 | 副官 | grok-4.5 | assistant-complete-headless-2026-07-10 | assistant-chatgpt-online | (this commit) | Headless-default web engine; docs voice-pipeline online section; doctor catalog entry; project/gpt/handoff/system_audio packs; timeouts wired; bootc+ansible render include modules; no desktop-steal automation. |
| 2026-07-10 | 大哥 | grok-4.5 | qwythos-role-layer-standing-2026-07-10 | n/a | (this commit) | End trial → standing **role layer** decision: `qwythos-9b` = Hermes decision + tool-call brain via LiteLLM. Explicitly **not** baseline / not always-on / not voice `/chat` default (`resident-small` stays). Updated models.yaml, litellm comment, model_presets docstring + agent preset notes, docs/voice-pipeline.md Role layer section, docs/architecture.md goals/flow/Phase 3–4. Architecture constraints preserved: baseline coexists; agent Vulkan vs 122B exclusive; CLAUDE.md §7 LiteLLM-only consumers. |
| 2026-07-10 | 大哥 | grok-4.5 | qwythos-ctx-half-131k-2026-07-10 | n/a | (live + this commit) | Cut Qwythos default Lemonade `ctx_size` 262144→131072 (half). Live: `/var/lib/aipc-lemonade/cache/recipe_options.json` + lemonade restart (in-memory cache ignored file edits). Hardware: bare load now reports ctx 131072; ΔGTT ≈8.2 GiB vs ≈12.7 at 256k/-np4; infer OK; unloaded (role layer). Repo: models.yaml load notes, litellm `max_input_tokens: 131072`, voice-pipeline role table. Baseline FLM re-warmed after restart. |
| 2026-07-10 | 副官 | grok-4-5 | codexbar-agent-usage-2026-07-10 | codexbar-usage-integration | cf46c8e | Committed CodexBar usage/gui modules + agent-tools-usage: Daily Assistant usage_lookup for local assistant calls; aipc usage CLI; wired fetch path (not perfect port). Static: usage 98 tests, tools suite green, openspec validate, verify.sh. Left uncommitted: lemonade/models/asus-input/voice docs dirty tree. |
| 2026-07-10 | 副官 | grok-4-5 | merge-phase3-to-main-2026-07-10 | n/a | (this merge) | Merged `phase-3-voice-assistant-2026-07-08` into `main` (voice closed-loop + assistant tooling + CodexBar usage_lookup / agent-tools-usage). Conflict resolved in docs/agent-log.md by taking phase-3 log (superset). |
| 2026-07-10 | 大哥 | grok-4.5 | ostree-step1-cleanup-p-2026-07-10 | n/a | (live + docs) | Careful re-layer Step1 only: `rpm-ostree cleanup -p` dropped staged `72f13aa…` (pending digest 3411c186). Booted + rollback kept. `/ostree/deploy` 72G→56G. Still Unlocked:transient until planned reboot (Step2). No `-r`/reboot/bootc switch. Recreated `docs/disk-layer-hygiene.md` (plan + D1–D5); claude-ops L5 updated. |
| 2026-07-10 | 副官 | grok-4.5 | phase3-voice-tts-duck-ux-2026-07-10 | phase-3-voice | (this commit) | Voice closed-loop UX: phrase wake + PTT (KEY_PROG3), Siri-like overlay, media duck via sink-input fade (never master volume; hard-block set-sink-volume), TTS paplay stream normalize to 100% stereo (fix L21%/R0% silence), primary-sink play, short voice LLM replies. Hardware-verified TTS hearable on built-in speakers at user volume. |
| 2026-07-10 | 副官 | grok-4.5 | phase3-voice-usable-stm-2026-07-10 | assistant-self-improvement#stm-multiturn | 40d9104 | Disk STM/pending multi-worker; stock AMD→那昨天呢 keeps ticker; farewell not tool-hijacked; AIPC_CLASSIFIER=always productized; 16 unit tests OK; live multi-turn hardware PASS (`/tmp/grok-goal-17bc0ac1f9a6/implementer/session-multiturn.json`). |
| 2026-07-10 | 副官 | grok-4.5 | agent-uncensored-stack-2026-07-10 | n/a | (this commit) | Agent whole path on uncensored locals: supervisor/daily/Hermes/mem0→coder-agentic (Qwen3.6 Uncensored Aggressive); classify→qwythos-9b; screen VLM→Gemma4 Uncensored Vision; system prompts direct/no-filter; merge multi-system msgs for Qwen template; live smoke 「完全本地运行的无审查模型」. |
| 2026-07-10 | 副官 | grok-4.5 | agent-npu-hybrid-speed-2026-07-10 | n/a | (this commit) | Hybrid after uncensored: NPU resident-small for chat/classify/mem0 (speed); Vulkan coder-agentic uncensored only for Hermes/daily LLM/tools; screen→vlm-screen; canned greet ~ms; AIPC_LLM_VOICE_TIMEOUT=20. |
| 2026-07-10 | 副官 | grok-4.5 | qwythos-classify-uncensored-2026-07-10 | n/a | (this commit) | Intent classify + chat on qwythos-9b (Mythos ~5.6GB): uncensored routing (~0.2s warm), no IT refusals on sensitive intents; Hermes stays coder-agentic; parse guard against enum echo. |
| 2026-07-11 | 副官 | Codex-gpt-5 | voice-tts-cosyvoice3-switch-2026-07-11 | phase-3-voice#1.5,4.3,6.1 | b12d29c | Switched CosyVoice TTS defaults from CosyVoice2 to Fun-CosyVoice3-0.5B-2512, tightened model readiness checks to the CosyVoice3 core file set, restarted `aipc-voice-tts-cosyvoice`, and launched a new background download unit for the new model path. Static verify OK; live healthz is degraded until the CosyVoice3 weights finish downloading. |
| 2026-07-11 | 副官 | Codex-gpt-5 | voice-tts-cosyvoice3-runtime-fix-2026-07-11 | phase-3-voice#1.5,4.3,6.1 | dd5c44e | Made the CosyVoice3 runtime loadable: accepted `llm.rl.pt` layout, auto-aliased it to `llm.pt` when needed, added missing venv deps (`pkg_resources` via setuptools<81, `wget`), added `<|endofprompt|>` handling, and verified `/healthz` ok plus a real `/tts` request returning audio/wav and playback on the local sink. |
| 2026-07-11 | 副官 | Codex-gpt-5 | active-assistant-worktree-checkpoint-2026-07-11 | 0002, 0003, 0004, phase-3-voice, voice-audio-front-gate, voice-streaming-turn | (this commit) | User-authorized checkpoint of the existing active assistant, portal, voice, and runtime worktree. Static tools tests and generated bootc/ansible renders pass; agent-orchestrator tests are blocked at import time by missing `langchain_core`, not a test assertion. |
| 2026-07-11 | 副官 | Codex-gpt-5 | aipc-control-center-spa-2026-07-11 | 0004-aipc-control-center-spa#1-7 | uncommitted | Added the Astro-built localhost Control Center SPA, dashboard JSON API, Type-C I/O snapshot, XDNA NPU state, Command Center state publication and SSE updates; preserved portal start/baseline/cancel actions. Static: 34 portal/dashboard/control-center tests, module verifier, strict OpenSpec validation, and bootc/ansible renders pass. Hardware verification remains pending deployment. |
| 2026-07-11 | 副官 | Codex-gpt-5 | hermes-local-advisor-context-2026-07-11 | 0002-assistant-intelligence-routing#3.2,3.3 | (this commit) | Hermes bridge now uses `--yolo`; local Ornith receives only a neutral technical artifact packet and a refusal is not retried. Live orchestrator hotfixed and restarted. Hermes compaction config uses 79,872-token trigger / 27,955 target / 43,008 input headroom, 90s auxiliary timeout, and abort-on-summary-failure. Static + agent venv 77 tests + renders pass; no 80K live soak run. |
| 2026-07-11 | 副官 | grok-4.5 | coder-compact-e2b-qat-ondemand-2026-07-11 | n/a | (this commit) | coder-compact → Unsloth Gemma4-E2B-it-qat-UD-Q4_K_XL on Lemonade Vulkan; on-demand (not pinned); enable_thinking=false; recipe ctx 131072 -np 1 -kvu. Live: cold LiteLLM request auto-loads E2B, returns pong-compact; unloaded after. resident-small stays NPU pin. |
| 2026-07-11 | 副官 | claude-opus-4-8 | voice-latency-instrumentation-2026-07-11 | voice-latency-instrumentation#1-4 | (this commit) | New voice-telemetry capability: stdlib `TurnTimer` records one JSON line per mic-captured turn (perceived/llm_ttft/tts_ttfa + path/backend/preset labels) at ~/.local/state/aipc-voice/turns.jsonl; best-effort (never fails/delays a turn), no spoken content, size-capped, `AIPC_VOICE_TIMING=0` disables. Wired into aipc-voice-once (batch, guarded, zero behaviour change); added `aipc voice timings` reader (raw tail + mean, percentiles/grouping deferred). Scope was trimmed from a full telemetry backend to the minimum needed to verify voice-streaming-turn's TTFA. Render-verified: ruff clean, verify.sh green, 14 new tests + render parity/bootc/ansible green, zero regressions (2 pre-existing chat-timeout test failures + 16 missing-dep failures confirmed unrelated via stash). Group 5 hardware baseline not run (no AI PC). |
| 2026-07-11 | 副官 | claude-opus-4-8 | voice-stream-telemetry-2026-07-11 | voice-streaming-turn#4.6 | (this commit) | Wired the voice-telemetry `TurnTimer` into the streaming worker (aipc-voice-stream): path=stream records with capture_end/llm_request/llm_first_token(first token or whole-done reply)/tts_request/first-audio → perceived+llm_ttft+tts_ttfa, flushed only on a successful streamed turn (stream_fallback lets aipc-voice-once log its own batch turn). Added `TurnTimer.mark_once` for repeat/threaded first-token/first-audio events. This closes the batch-vs-stream comparison so `aipc voice timings` can verify streaming's TTFA win (§7.3). Render-verified: recorder+worker self-tests, verify.sh green, ruff clean, 20 timing/stream tests pass; guarded so absent/disabled telemetry never affects a turn. |
| 2026-07-11 | 大哥/副官 | grok-4.5 | deferred-main-model-live-kv-compact-2026-07-11 | n/a | (note only) | User asked to record (not implement): compact on the **same conversation model** with **live KV** — append summary instruction to existing llama-server slot so 65k-class history prefill is mostly skipped. Contrasts current `coder-compact`=E2B ondemand separate model (full re-prefill, better isolation). Needs Hermes/stateful session compact; trade-off steals coder-agentic -np slots. Pointer also in llm-litellm config.yaml coder-compact DEFERRED comment. |
| 2026-07-11 | 副官 | claude-opus-4-8 | hermes-timeout-reap-2026-07-11 | n/a (direct live bugfix) | (this commit) | Root-caused voice assistant stuck-on-"工具執行中": Hermes turn timeout at hermes_bridge.run() called proc.kill(), but when orchestrator runs as root it spawns `runuser -u <user> -- hermes`, so proc is the runuser wrapper and the real hermes worker is a GRANDCHILD — kill orphaned it, and a voice weather turn kept prefilling an ~80K-token context on ornith-35B for 16+ min after "Hermes 超時" was already logged. Fix: new stdlib `proc_reaper.terminate_tree()` (SIGTERM→SIGKILL the whole process group) + `start_new_session=True` on the Popen. Live-verified at the real root→runuser→grandchild boundary (grandchild reaped); proc_reaper self-test + 3 unit tests + ruff green; deployed to /var/lib/aipc-agent and orchestrator restarted (healthz 200). Killed the stuck 16-min zombie turn 938721. Separate follow-up (task #6): voice turns still escalate to 35B with giant context — the reap stops the hang; routing/context cap is the perf fix. |
| 2026-07-11 | 執行兵 | claude-sonnet-5 | hermes-compact-recon-2026-07-11 | n/a | (no commit, read-only) | Recon for Hermes compact timeout+repeat loop (orchestrator claude-fable-5): mapped hermes_agent 0.18.0 compressor (trigger ~79,872 tok @ threshold 0.65; aux=coder-compact timeout 600s; on aux fail → immediate no-backoff retry on main 35B → worst 1200s > bridge wall 720/900s → tree killed → cooldown never recorded → re-compact loop). Live health max_models.llm=2 (NPU pin + 1 slot → E2B/35B mutual eviction, dual "No model loaded" 500s in agent.log 16:46). Plan doc's 90s compact timeout was never applied (live 600s). |
| 2026-07-11 | 執行兵 | claude-sonnet-5 | compact-cost-bench-2026-07-11 | n/a | (no commit, scratchpad) | Hardware bench, 23.5k-tok compact: E2B lane flat ~30-32s incl 5.4s load (prefill 1,660 tok/s, decode 85 tok/s); 35B cache-MISS 54.8s (542 tok/s prefill + 11.6s reload); 35B live-KV cache-HIT 22.0s (cache_n=23,374 real hit via LiteLLM, decode 53 tok/s). Under 2-slot cap each E2B compact taxed next agent turn ~55s of 35B rewarm; 35B also idle-unloads on its own → live-KV compact stays DEFERRED (warmth unreliable), E2B lane confirmed as compact route once slots ≥3. |
| 2026-07-11 | 副官 | claude-sonnet-5 | hermes-compact-fix-2026-07-11 | n/a (direct live bugfix) | b4c1026 | Fixed Hermes compact timeout+repeat loop. RC1 corrected: NOT a Lemonade 10.8.1 schema change — lemond binary symbols prove max_loaded_models is the single knob mirrored into every max_models.<type>; live 2-slot state was a STALE ostree /usr copy of configure-lemonade.sh (repo fix predates last rebuild). Live hotfix: ExecStartPre drop-in → /etc/aipc copy (99-live-hotfix-configure-until-rebuild.conf; REMOVE after next bootc rebuild); health now max_models.llm=8, resident-small+coder-agentic+coder-compact resident together, no eviction/swap. RC2: ~/.hermes/config.yaml auxiliary.compression.timeout 600→180 (bench-derived: worst ~110s @80k trigger; 2 attempts=360s < 720s bridge wall → cooldown always recordable). Acceptance hardware-verified: real Hermes session, 2 compactions via coder-compact in 6.1s/6.7s, zero timeout/fallback/"No model loaded"/repeat lines, 35B stayed loaded throughout. verify.sh now checks live health max_models.llm>=3; shellcheck+17/17 CLI tests+both renders green. Caveat: hermes_agent floors compact trigger at 64,000 tok (MINIMUM_CONTEXT_LENGTH) — threshold 0.08 test triggered at 64k, not 9.8k. |
| 2026-07-11 | 大哥 | claude-fable-5 | hermes-compact-fix-2026-07-11 | n/a | (dispatch/review) | Orchestrated recon+bench+fix (3 sonnet dispatches above), reviewed b4c1026 diff, independently re-verified live values (llm=8, 3 models resident, hermes timeout 180/threshold 0.65), synced /etc hotfix script to repo, renamed drop-in with removal condition, appended guidelines to ~/claude-ops/LESSONS.md (stale-/usr config drift; nested-timeout-budget rule). |
| 2026-07-11 | 副官 | claude-opus-4-8 | overlay-markdown-media-2026-07-11 | n/a (design+plan under docs/superpowers) | 024363b..f7ab6cc | Voice overlay answer body renders markdown instead of plain text. New pure helpers in aipc_voice_overlay.py: _extract_and_strip_media (pull markdown/bare image URLs to the safe gallery, keep non-image links), _markdown_to_html (QTextDocument.setMarkdown→HTML, escape fallback, never raises), _source_host; BodyScroll.set_body routes text through them, label set to RichText. Verified QTextDocument.toHtml bakes no body text color so the dark-HUD color stylesheet still applies. Render-verified: 7 headless PySide6-offscreen tests, ruff clean, verify.sh rc=0, overlay import OK, bootc+ansible render parity. Qt visual NOT yet live-confirmed (needs desktop). Follow-up (plan task 3): 2-col image grid + source caption. design 2026-07-11-overlay-markdown-media-layout-design.md, plan .../plans/2026-07-11-overlay-markdown-media-layout.md. |
| 2026-07-11 | 副官 | claude-sonnet-5 | overlay-state-dock-animate-2026-07-11 | phase-3-voice-assistant#overlay-state-dock | 90e8b73 | Voice overlay HUD now docks by state: idle/listening states (listening, wake, recording, no_speech, followup) compact to the right (reusing the existing mini pill layout); activity/result states (thinking, working, speaking, done, error) expand centered. New pure `_anchor_for_state`/`_effective_anchor`/`_mini_for_state` helpers in aipc_voice_overlay.py; AIPC_OVERLAY_ANCHOR still forces an anchor when set, new AIPC_OVERLAY_STATE_DOCK=0 restores old env/top-center-only behavior. `_place()`'s animation gate extended from size_delta>=40 to also fire on x_delta>=40, and apply_status() now raises an anchor_switch flag alongside the existing mini mode_switch so right<->center transitions always animate via the existing _animate_geometry() tween. Render-verified only: 10/10 new headless PySide6-offscreen tests pass (tools/tests/test_overlay_dock.py), ruff clean, verify.sh rc=0, import sanity OK, bootc+ansible render parity green; full tools/tests suite shows 19 pre-existing failures unrelated to this file (confirmed via git stash — same failures with/without this change, from concurrent litellm/portal WIP). Deployed to both live paths (~/.local/lib/aipc-voice + /var/lib/aipc-voice/lib) but did NOT restart aipc-voice-overlay — Qt visual slide/dock NOT yet confirmed on the physical desktop, pending user restart.
| 2026-07-11 | 副官 | claude-sonnet-5 | assistant-closure-2026-07-11 | phase-4-agent#4.7 | e63695a,a77951d,292829e | Fixed ydotool.service boot-time socket race: brought the live-only /etc/systemd/system/ydotool.service.d/override.conf into modules/agent-screen-control/files/ with After=/Requires=user-runtime-dir@1000.service added (root cause: ydotoold's socket path under /run/user/1000 raced that dir's creation early in boot, hit Restart=always's start-limit burst, and gave up). post-install.sh now systemctl enable ydotool.service (build-time, no --now). Applied live via live-hotfix workflow: daemon-reload+restart -> active, ydotool mousemove 0 0 as uid 1000 -> exit 0 (hardware-verified). Boot-race elimination itself is reboot-pending (could not reboot mid-session). verify.sh exit 0; aipc render bootc + render ansible both exit 0 and include the new override.conf via files/ COPY (render-verified, targets in sync). Also had to forward-fix (a77951d) an accidental unrelated modules/assistant-chatgpt/README.md change that a concurrent agent session's git-index race swept into e63695a on this shared working tree; e63695a's .disabled removal for agent-screen-control was pre-existing/already-decided by an earlier session (module already documented enabled) and left as-is. |
| 2026-07-11 | 副官 | claude-sonnet-5 | turn-state-followup-contract-2026-07-11 | phase-3-voice-assistant#turn-state-contract | 4f487bf | Turn-state contract: expect_reply (from clarify_question) added to /chat response, rc mapping (2=end/3=reply/0=done) in aipc-voice-once, wake follow-up only opens on rc==3. Static: pytest green (77 pass, 3 pre-existing unrelated fails, 3 new tests skip cleanly without fastapi), ruff clean on touched files. Render-verified: voice-wake/voice-pipecat verify.sh exit 0, bootc+ansible render exit 0. Hardware NOT verified by this agent (no mic/speaker) — deployed live copies (diff-clean) to /var/lib/aipc-voice, ~/.local/bin, /var/lib/aipc-agent for user hardware-verify; did not restart services. |
| 2026-07-11 | 副官 | claude-sonnet-5 | hermes-compact-frequency-tune-2026-07-11 | n/a (live third-party config) | (no repo change) | Reduced Hermes per-turn compaction. Corrected two earlier hypotheses with log evidence: no 64k-floor bug (the ~65k figure was the POST-compaction estimate; real trigger was in=89,785 ≥ 79,872 as configured) and real usage DOES flow back (in= values change per call). True root cause: tail-protection soft ceiling (1.5×tail_token_budget=41,933) + huge tool outputs + 18,553-token fixed system/tool-schema overhead → each compact removed only ~1 message, landing at 61-65k → re-trigger within 1-2 tool-heavy turns (9+ compacts/day on one session). Fix (~/.hermes/config.yaml): threshold 0.65→0.75 (trigger 92,160), target_ratio 0.35→0.25 (soft ceiling −18%). Hardware-verified: in=80,312 no longer triggers; at ~111k two compacts completed 44s/62s cleanly, zero timeout/repeat lines. Residual (report-only): deeper lever is code constants at context_compressor.py:892-951,2586. |
| 2026-07-11 | 副官 | claude-sonnet-5 | hermes-title-timeout-fix-2026-07-11 | n/a (live third-party config) | (no repo change) | Fixed "Auxiliary title generation failed: Request timed out": task had NO auxiliary.title_generation config → universal fallback ran it on the contended 35B main lane with the 30s _DEFAULT_AUX_TIMEOUT (3×30s attempts all timing out, 16 historical failures in errors.log). Fix: added auxiliary.title_generation → resident-small (NPU, always loaded) timeout 60. Hardware-verified via the exact production code path: "using custom (resident-small)", HTTP 200, title generated in 11.44s. Pre-existing quirks reported not fixed: CLI -q oneshot never joins the daemon title thread (single-turn -q sessions never get titles, days old); TUI status bar 0/131.1K reads context_compressor.last_prompt_tokens which resets to 0 if the agent object is swapped mid-session (top lead, unconfirmed; cli.py:4530-4631, tui_gateway/server.py:2974-3011). |
| 2026-07-11 | 副官 | claude-sonnet-5 | async-timeout-detach-2026-07-11 | phase-3-voice-assistant#async-detach | dea9d8b | Dynamic-timeout auto-detach (Slice 2, does not touch Slice 1's turn-state contract 4f487bf): task_jobs.submit() gains grace_s — waits up to grace_s for fn() to finish; if it does, returns fn()'s full result dict unchanged (inline semantics incl. keys like "trail") and suppresses the background completion notify/memory-internalize via a result_ready/claim_done Event handshake gated only when grace_s>0 (grace_s=0.0 default is byte-identical to prior behavior); otherwise falls back to today's immediate async ack, worker keeps running, notify fires normally on finish. graphs.py's _hermes_node inline branch now routes through task_jobs.submit(..., grace_s=DETACH_S=45s via AIPC_ASYNC_DETACH_S) instead of calling _run() directly, reusing the same _run closure the explicit mode=long path uses; explicit _should_run_long_async path untouched (still immediate grace_s=0). Plain chat/respond node never calls task_jobs.submit — confirmed unaffected. daily_assistant inline path left alone per spec (flagged, not changed). Optional overlay "背景進行中" pill deferred to Slice 3 (needs new turn-state rc through voice-once, out of scope for a clean diff). Static: ruff clean; new test_task_jobs_grace.py 2/2 pass; full orchestrator suite 76 passed/3 skipped/3 failed, failures are pre-existing ModuleNotFoundError: langchain_core confirmed via git-stash (identical without this change). Render-verified: verify.sh exit 0 (real venv has langchain_core), bootc+ansible render both exit 0. Hardware NOT reachable — actual detach timing/notify behavior needs physical-AI-PC verification. Deployed diff-verified-identical copies to /var/lib/aipc-agent/aipc_agent/{task_jobs,graphs}.py, restorecon'd; no restart performed.
| 2026-07-11 | 副官 | claude-sonnet-5 | async-pending-overlay-2026-07-11 | phase-3-voice-assistant#async-pending-pill | fff4b4e | Slice 3/3: added the async/background turn-state (rc==4) and the "bg_task" right-docked pending pill. Threaded `background: True` through both of graphs._hermes_node's detach-ack return sites (auto-detach past DETACH_S, and the pre-existing explicit mode=long ack — both confirmed to always carry detail=="background" since grace_s=0/short waits skip the inline-delivery branch in task_jobs.submit). server.ChatResponse gained `background: bool` via a new pure `background_from_result()` helper mirroring the existing `expect_reply_from_result`. aipc-voice-once's chat() tuple extended to (reply_text, end_session, expect_reply, background, session_id) at all 6 return sites + both main() call sites; _turn_rc gained background param, precedence end_session(2) > background(4) > expect_reply(3) > done(0) — background beats expect_reply because a detached long turn already frees the mic. rc==4 still speaks the ack via show() then overrides the overlay to the bg_task pill (does not suppress TTS). voice-wake's _on_once_finished gained an rc==4 branch: frees the mic (no follow-up, same miss_streak/energy_thr bookkeeping as the plain-done case) but calls _clear_followup(hide=False) so the pill is not stomped back to "listening" — it persists until activity.complete_notify's "done" state replaces it. Also fixed a latent bug this depended on: OnceWorker.submit_wav's `ok = rc in (0, 2)` silently excluded rc==3, meaning Slice 1's expect_reply follow-up was unreachable in production; widened to `(0, 2, 3, 4)`. Overlay: added bg_task to SHOW_STATES, STATE_COLORS/HINTS/LABELS, the right-anchor set, and the no-auto-hide list; _mini_chip_label gained an optional state param that returns a fixed "背景任务进行中 ⋯" label for bg_task (content-independent, unlike other mini phases) — _mini_for_state needed no change, it already falls through its right-dock fallback for any state in the right-anchor set. Static: ruff clean on all touched files (one pre-existing E702 semicolon at aipc-voice-once:1054 predates this slice, untouched). git-stash-verified both tools/tests/ (352→360 passed, same 19 pre-existing failures incl. the already-broken test_voice_once_chat_timeout.py which pre-dates this change) and modules/agent-orchestrator/tests/ (76 passed both before/after, same 3 pre-existing langchain_core ModuleNotFoundError failures) — zero regressions from this diff. Render-verified: voice-pipecat/verify.sh and voice-wake/verify.sh exit 0; render bootc + render ansible both exit 0. agent-orchestrator/verify.sh times out in this sandbox (needs live systemd + curl to :4100, hardware tier) — isolated its pure-Python construct check instead: `supervisor()` constructs cleanly and `background` is a real SupervisorState/ChatResponse field via the live /usr/lib/aipc-agent/venv. This session turned out to have live hardware access (aipc-agent-orchestrator, aipc-voice-wake, aipc-voice-overlay all active/running) — deployed diff-verified-byte-identical copies to /var/lib/aipc-agent/aipc_agent/{graphs,server}.py, /var/lib/aipc-voice/lib/{aipc_voice_wake,aipc_voice_overlay}.py, ~/.local/lib/aipc-voice/aipc_voice_overlay.py, ~/.local/bin/aipc-voice-once; restorecon'd; confirmed zero restarts (ActiveEnterTimestamp unchanged on all three units, a live Hermes voice turn was mid-flight during deploy). The actual pill appearance/persist-until-notify visual timing is NOT observed by this agent (no eyes/ears) — needs user to trigger a >45s Hermes voice turn and confirm visually. Spec gap flagged, not fixed (out of this task's named scope): _daily_assistant_node's parallel explicit-long return site (graphs.py ~line 794-798) has the identical always-background task_jobs.submit() pattern as Hermes's explicit-long site but was not in the task's file/site list, so it still returns without `background: True` — a background daily-assistant long turn will currently show a "done" card instead of the bg_task pill. |
| 2026-07-11 | 副官 | Codex-gpt-5 | hermes-context-rollover-2026-07-11 | 0005-hermes-context-rollover#1.1-4.4 | AIPC c950f09..81087ce; Hermes 7ea2959..59943ec | Fixed Hermes context exhaustion in its local carried-commit fork: oversized tool artifacts now expose bounded head+tail previews; every terminal compression-exhaustion result carries a deterministic bounded handoff; CLI and gateway create one successor session, persist the handoff, notify once, and never replay the failed turn or tools. Fast-forward merged to live Hermes `main`; live YAML remains 131072 tokens with threshold 0.70, protect_last_n 4, abort_on_summary_failure true, coder-compact timeout 180. Static: 86 focused tests and ruff clean; OpenSpec strict valid. Render-verified: current bootc and ansible CLI renders wrote non-empty outputs; parity 2/2. Live source import confirmed. Hardware 128K model soak not run, so task 4.3 remains open; synthetic 128K-class protected-tail regression passes. AGENTS.md render examples are stale for the current CLI (`--image-ref`/`--build-date` now required; ansible has no `--check`). |
| 2026-07-11 | 副官 | Codex GPT-5 | glm-tool-2026-07-11 | 0002-assistant-intelligence-routing#6.4; cloud-llm-fallback#4.1; codexbar-usage-integration#6.1 | 2d50a2d..595008d | Added quota-gated Z.AI GLM 5.2 as an optional Daily Assistant tool through LiteLLM, CodexBar-compatible monthly quota reporting, SOPS runtime secret wiring, fresh-quota gating, credential-shaped prompt rejection, and irreversible private-context taint after memory/history or non-GLM tools. API key was not stored. Static: 188 passed, 6 skipped, 3 pre-existing environment-dependent tests deselected, 5 subtests; changed-file ruff clean; three OpenSpec changes strict-valid. Render-verified: bootc and ansible renders succeeded and 18 render/parity tests passed. Agent runtime verify and hardware behavior were not run because the worktree lacks the installed `/usr/lib/aipc-agent/venv` and live service context. Independent final review: clean. |
| 2026-07-12 | 副官 | grok-4.5 | codexbar-gui-tray-shell-2026-07-12 | dev-ai-codexbar-gui#tray-shell | (uncommitted) | Added shell_adapter compatibility layer: same UsagePopover hosted via SNI context menu on Wayland/Plasma (host places under tray) with window fallback; left-click re-enters StatusNotifierItem.ContextMenu(x,y); height clamp + multi-monitor screen bind kept for window mode. Flatpak rebuilt+running (tray shell=sni_menu). Static: 49 passed, 1 skipped. |
| 2026-07-11 | 副官 | claude-sonnet-5 | hermes-longtask-soak-2026-07-11 | n/a (acceptance soak) | (no repo change) | Long-task soak of the compact fixes: compactions #1/#2 clean (<180s) + abusive 72KB-read recovery PASSED; compaction #3 FAILED under the concurrent OOM-era GPU storm — aux summary hit exactly 180s, the streaming-closed/network carve-out (context_compressor.py:2036-2046) forced abort regardless of abort_on_summary_failure, zero reduction → terminal "Cannot compress further" at 44,626 tokens. Agent terminated by API session limit before applying a fix; finding handed over — superseded the same night by Codex's 0005-hermes-context-rollover (terminal compression failure now rolls the session forward with a 12k handoff instead of dying) + the OOM root fix removing the GPU-storm precondition. |
| 2026-07-11 | 大哥 | claude-fable-5 | lemonade-oom-cap-2026-07-11 | n/a (live incident) | cc7efb6 | OOM incident 22:49 (RAM 121/121, swap full, oom-killer firing): max_loaded_models=8 let 5 LLMs accumulate (60GiB GTT) alongside ComfyUI. Relief: POST /api/v0/unload idle VL-7B + 26B (avail 0→23G); root fix: cap=4 (pinned NPU + 35B + E2B + 1 floater) in live config + repo + /etc hotfix copy; effective after the 23:22 lemonade restart (verified live max_models.llm=4, healthy watermarks since). Also raised hermes max_turns 60→120 (user session hit iteration budget on a long ComfyUI task; TUI maps max_turns→max_iterations). Guidelines L9 (capacity caps from memory budgets) + L8 (explicit lane+timeout per aux task) committed+pushed to claude-ops. Rollover-failure-path hardware verification dispatched 2026-07-12. |
| 2026-07-11 | 副官 | claude-fable-5 | mem0-portal-tab-2026-07-11 | mem0-portal-management-tab#1.1-1.5 | 9487742 | Integrated mem0 management UI into Control Center portal as a modular /memory tab. memory-mem0 gained GET /memories (unscoped browsing via Memory._get_all_from_vector_store() directly — mem0ai==2.0.11 get_all() rejects filters lacking user_id/agent_id/run_id, confirmed live) and DELETE /memories/{id}. Portal gained same-origin proxies /api/v1/memories* → :7000, generic traversal-guarded static serving (fixing the pre-existing /_astro/*.js 404), and the /memory Astro page (search, scope filters, confirm-gated delete). Hardware-verified: mem0 live-hotfixed at /etc/aipc/mem0 (backup server.py.bak-2026-07-11), add→list→delete→search against real pgvector data; portal run from repo tree end-to-end. Render bootc/ansible green; 8 new tests, touched test files 32 passed (suite's 19 failures pre-existing voice/session, fail with change stashed too). Note: committed static/index.html is rebuilt output incorporating a concurrent session's uncommitted index.astro edits (shared tree); portal is not in the currently deployed image, so the web tab needs the next rebuild or the live /usr/local deployment done this session. |
| 2026-07-12 | 副官 | claude-opus-4-8 | overlay-content-dock-flicker-2026-07-12 | phase-3-voice-assistant#overlay-state-dock | a91df75 | Content-driven overlay dock: center only for rich results, fixes flicker + short-answer-centering (hardware-surfaced). Static+render green, deployed, overlay restarted. |
| 2026-07-12 | 副官 | claude-opus-4-8 | overlay-content-dock-flicker-2026-07-12 | phase-3-voice-assistant#overlay-state-dock | 935c6bd | Fixed HUD flicker: idle orb repaint (~20Hz) on translucent HUD on fractionally-scaled monitor. Orb animates only when active. X11-sampling-diagnosed, user-confirmed. |
| 2026-07-12 | 副官 | claude-opus-4-8 | overlay-single-launcher-2026-07-12 | phase-3-voice-assistant#overlay-state-dock | 8735235 | Fixed duplicate overlay ("兩個"): removed KDE autostart .desktop, build-enable user service via default.target.wants symlink. Live: killed 3.3h orphan + disabled live autostart. Fresh-image boot render-verified only. |
| 2026-07-12 | 大哥 | claude-fable-5 | hw-optimize-np-drift-0007-2026-07-12 | 0007-coder-heavy-on-demand#proposal | (uncommitted) | Root-caused 30B queuing: live coder-agentic recipe drifted to -np 2 (models.yaml says -np 4; Ornith also drifted ctx 32768/-np 1). Fixed live via forced reload (Hermes continuous traffic never yields an idle gap; idle-wait polling failed twice) — hardware-verified: recipe saved '-kvu -np 4', 4 concurrent requests completed in 0.6s wall. Costs/learnings logged to mem0: forced reload wipes KV; Qwen3.6 hybrid attention cannot restore context checkpoints (llama.cpp SWA limit) so each ~92k Hermes session re-prefills (~320 tok/s ≈5min); giant prefill starves parallel decode to 0.22 tok/s (future bench: smaller -ub). Confirmed GTT already 122GiB (amdgpu.gttsize=125000) — offload techniques inapplicable on UMA. Authored 0007 proposal (coder-heavy = Qwen3-Coder-Next 80B-A3B UD-Q4_K_XL ~46GB, idle_unload_after_s 600, headroom policy, llama.cpp Feb-2026 build gate). Fleet review researched (2 web agents): recommend retire ornith-35b, swap vlm-screen→Qwen3-VL-8B, coder-compact→Qwen3.5-4B, wire assistant-gemma MTP; user decision pending; evaluated user-suggested DavidAU 40B dense (reject: bandwidth shape), HauhauCS Qwen3.5-122B-A10B uncensored GGUF (viable general-heavy lane), SC117 Ornith MTP+mmproj (rejected: MTP/mmproj/-np mutually exclusive in llama.cpp; role still redundant). |
| 2026-07-12 | 副官 | claude-sonnet-5 | task-registry-persist-reap-2026-07-12 | phase-3-voice-assistant#task-registry | 34cabe7 | Persisted task_jobs.py registry to disk (atomic JSON write, AIPC_TASK_JOBS_STORE-overridable), added register_proc()+reap_orphans_on_startup() to kill orphaned hermes process groups on service boot (PID-reuse guarded), widened 任务进度 intent triggers; static+render verified, 3 pre-existing langchain_core failures confirmed via git-stash unrelated, hardware reap-on-real-boot not reachable in this session. |
| 2026-07-12 | 副官 | claude-sonnet-5 | fleet-refresh-0008-2026-07-12 | 0008-fleet-refresh#proposal | 135f262 | Authored 0008-fleet-refresh proposal (why/what/how/tasks + ai-runtime spec delta, openspec validate --strict passes) covering four swaps: vlm-screen Qwen2.5-VL-7B->Qwen3-VL-8B-Instruct (added to models.yaml for the first time, was litellm-only drift), coder-compact Gemma-4-E2B->Qwen3.5-4B (corrected repo id: unsloth/Qwen3.5-4B-GGUF not -Instruct-GGUF, which does not exist), assistant-gemma MTP draft wired (-np 4->1, confirmed n_parallel=1 constraint via live llama.cpp bug report; the often-repeated --mmproj-incompatible claim did NOT hold up against Lemonade's own MTP catalog and is moot for this alias anyway), ornith-35b -> llmfan46 Heretic-decensored quant (+idle_unload_after_s:900, bridging 0007's plan for this alias only). All facts (repo ids, exact gguf filenames, byte sizes, MTP CLI flags/Lemonade checkpoints:{main,draft,mmproj} mechanism) verified live via HF tree API + lemonade-sdk/lemonade PR #2317 diff, not assumed. Static: yaml.safe_load clean on both models.yaml and litellm config.yaml. Render-verified: tools/aipc render bootc and render ansible (no --check flag in this CLI version, ran plain --out) both exit 0, both COPY modules/llm-models/files correctly. No hardware access this session -- all pull/load/tool_calls/tok-s/OCR-smoke-test items left as tasks.md hardware-only checkboxes. Did not commit models.yaml/litellm config/READMEs/agent-log.md (other sessions have uncommitted WIP there); only the new openspec/changes/0008-fleet-refresh/ directory is committed. |
| 2026-07-12 | 副官 | claude-sonnet-5 | task-registry-followup-2026-07-12 | phase-3-voice-assistant#task-registry | (this commit) | Slice 2/2: session-bound proactive followup. task_jobs._worker's finish block now sets needs_followup=not delivered_inline (computed after the grace-window claim, so grace_s-inline-delivered jobs never owe a mention); new take_pending_followups(sid) atomically claims+acks matching ok/error/interrupted jobs for that session under _JOBS_LOCK (one-shot, cross-call, persisted); new followup_notice(jobs) renders a short zh notice. server.py's /chat handler (single chokepoint) prepends the notice to both text and spoken_summary when not background and not end_session, wrapped in try/except so a failure never breaks the reply. format_status_speech gained optional sid= to surface the caller's own session's jobs first; graphs.py's _job_status_node now passes sid. Static: ruff clean on touched files; new tests/test_task_jobs_followup.py 5/5 pass; full orchestrator suite 94 passed/6 skipped/3 pre-existing langchain_core ModuleNotFoundError failures (git-stash-confirmed identical without this diff). Render-verified: bootc and ansible renders both exit 0. Hardware (real proactive mention on a live next voice turn) not reachable in this session — needs the physical AI PC with Slices 1+2 both active after a coordinated restart. Deployed byte-diff-verified copies of task_jobs.py/server.py/graphs.py to /var/lib/aipc-agent/aipc_agent/, restorecon'd (var_lib_t confirmed); did not restart aipc-agent-orchestrator. |
| 2026-07-12 | 副官 | claude-sonnet-5 | fleet-refresh-0008-item6-2026-07-12 | 0008-fleet-refresh#proposal | bb45a89 | Added item 6 (resident-small NPU swap gemma4-it-e4b-FLM -> qwen3.5-4b-FLM, replacement not addition) to 0008-fleet-refresh why/what/how/tasks/spec; openspec validate --strict and render bootc+ansible pass (render-verified only, no hardware access). Overlaid working-tree edits (not committed) to models.yaml, litellm config.yaml, both ensure-resident-small.sh copies, aipc-resident-small.service, llm-lemonade verify.sh, both module READMEs, and assistant-aggregator runtime.yaml comment. |
| 2026-07-12 | 副官 | codex-gpt-5 | lemonade-compact-idle-unload-2026-07-12 | 0006-lemonade-compact-idle-unload#1-6 | f3667e2 | Added per-alias `idle_unload_after_s`, with `coder-compact=300`, plus a Lemonade timer job that uses the live monotonic-millisecond `last_use` clock and skips pinned or `in_use` models before calling `/api/v0/unload`. Static: Python compile/self-test, shell syntax, YAML assertion, diff/secret checks, and OpenSpec strict validation passed. Render-verified: bootc and ansible renders succeeded. Hardware behavior was not activated or verified; the live compact model was busy during inspection. |
| 2026-07-12 | 副官 | codex-gpt-5 | hermes-compact-timeout-repair-2026-07-12 | 0005-hermes-context-rollover#3.1,3.3,4.3 | (this commit) | Root-caused repeated GPU saturation to leaked soak values in live Hermes YAML (`threshold: 0.10`, compact timeout `1s`). Restored threshold `0.70`, set compact timeout to LiteLLM's 600s ceiling, restarted the stale CLI, and updated the existing spec/design/plan/docs. Hardware-verified: resumed 97,100-token session compacted 299→211 messages / ~66,571 tokens in 28s through `coder-compact`, with no timeout, retry, or repeat; the following 66K `coder-agentic` turn was normal main-model work. |
| 2026-07-12 | 副官 | claude-opus-4-8 | voice-assistant-handoff-2026-07-12 | phase-3-voice-assistant | 45a91a1..ca5c55f | Session wrap: task-registry (persist/reap/followup) + job-status routing verified; overlay active-center/idle-hide + wake denoise-off deployed. Wake STT flaky + "wake→執行中" screen symptom flagged NEEDS-HANDS-AT-MIC. Handoff doc written. |
| 2026-07-12 | 副官 | claude-opus-4-8 | hermes-npu-route-fallback-2026-07-12 | phase-3-voice-assistant#hermes-npu-route | (this commit) | Assistant-dispatched Hermes now runs on NPU `resident-small` via `hermes chat -m`, with auto-fallback to `coder-agentic`(35B) when the small model fails/times out/emits NEED_STRONGER or returns unusable output. Added `AIPC_HERMES_MODEL`/`AIPC_HERMES_FALLBACK_MODEL`/`AIPC_HERMES_PRIMARY_TIMEOUT`(300s cap so fallback fits the voice CHAT_TIMEOUT) envs; hardened `_build_query` with anti-bluff wording + a NEED_STRONGER escape token (allow_escape only on the primary attempt); `_is_unusable_answer` now catches OOB/prompt-echo + gateway-error leaks; the rc≠0 partial-answer path is gated through `_is_unusable_answer` (root cause: it previously returned status=ok for a leaked `HTTP 400 Invalid model name` string, defeating fallback). Static: py_compile + self_test green (incl. new asserts). Hardware-verified on live `/var/lib/aipc-agent` (orchestrator restarted): hermes agent.log confirms `model=resident-small` @127.0.0.1:4000 (NPU FLM); deterministic fallback fires (bogus primary→error→fallback→ok "現在是七月"). Observed pitfall: `resident-small` nondeterministically echoes the OOB prompt scaffold instead of answering and does NOT self-report incapacity — output-side unusable-detection, not model self-assessment, is the real safety net. Standalone terminal `hermes` unaffected. No OpenSpec change authored (part of phase-3-voice-assistant iterative work). |
| 2026-07-12 | 大哥 | claude-opus-4-8 | hermes-webui-portal-link-2026-07-12 | 0009-hermes-webui-portal-link | (this commit) | Adopted external `nesquena/hermes-webui` (NousResearch hermes-agent's web console) instead of building one: it auto-discovers the existing `~/.hermes/hermes-agent`, reuses `~/.hermes/config.yaml` (models still via LiteLLM §7), and its CLI-session bridge lists our `source_tag: aipc-voice` Hermes sessions from the same `state.db`. Stood it up home-managed (phase A, user's call): `~/.hermes/hermes-webui` via `ctl.sh`, `.env` PORT=8788 (default 8787 taken by codexbar-gui), HOST=127.0.0.1 loopback, PYTHON=python3.11 (system 3.14 too new), agent auto-detected. Hardware-verified: /health 200, `/api/sessions` returns the aipc-voice session. Repo change = OpenSpec 0009: `dashboard.py` snapshot now emits `ui`+`health` per service; `index.astro` renders an Open UI link when `ui` set, hides Start for unit-less (`n/a`) services, and shows a health-derived label; new `hermes-webui.yaml` service card (no `systemd:`, phase A). NOTE: dashboard.py+index.astro also carry a pre-existing (prior-session) NPU service/activity telemetry hunk bundled in the same files — not authored here, committed together to keep the built static artifact consistent. Static: pytest 16 passed, py_compile clean. Render: bootc+ansible both exit 0, symmetric (§4 parity). Hardware-verified via live-hotfix: the deployed portal ran from `/usr/local/lib/aipc-portal` with an empty `/etc/aipc/portal/services` (pre-existing hotfix leftover → 0 cards); synced portal code + populated the services dir with all module cards + restarted → live dashboard now shows 9 cards incl. Hermes WebUI (state=n/a health=true ui=:8788, Open UI link works). PENDING: hermes-webui not persisted across reboot (ctl.sh, not a unit — confirmed down after a crash-reboot); phone/external-access requested by user, deferred to a security-reviewed exposure choice (Tailscale recommended). |
| 2026-07-12 | 副官 | grok-4-5 | lemonade-multi-model-thrash-diag-2026-07-12 | (ops note, no spec task) | (this commit) | Diagnosed Lemonade thrash: voice+agent-orchestrator fan-out to 7 local aliases vs max_loaded_models=4; LiteLLM ~100+ backend conns; health timeout. Temporary stop: agent-orchestrator, voice-*, command-center, portal; restart lemonade+litellm; Hermes webui restart. Write-up: docs/lemonade-multi-model-thrash-2026-07-12.md. Not fixed permanently. |
| 2026-07-12 | 副官 | claude-sonnet-5 | suspend-hang-forensics-journald-2026-07-12 | n/a (no OpenSpec change; extends existing single-purpose bugfix module) | 633b41c | User reported a real s2idle-resume hang: suspended, unplugged an unrecognized USB-powered fan mid-sleep, woke via power button to an unresponsive screen, forced power-cycle. `journalctl -b -1` for that boot had almost nothing left except ~40k lines of `plasma-systemmonitor` spamming `TreeViewDelegate.qml: Unable to assign [undefined] to QString` (its sensor-tree lost the data source when the USB device vanished) — no OOM/panic/GPU-reset/coredump signature found, but inconclusive since the flood likely evicted the real evidence. Same hang *class* as `system-suspend-gpu-guard`'s task #10 (2026-07-05) but a different trigger (USB hotplug during sleep, not GPU-busy-at-suspend) that the existing AC/GPU-busy guard would not have caught. Added `files/etc/systemd/journald.conf.d/90-suspend-hang-forensics.conf` to that module (RuntimeMaxUse=300M, SystemMaxUse=500M, RateLimitBurst=1000/30s) so a future runaway app can't evict a whole boot's forensic evidence; extended `verify.sh` to check the drop-in exists. Hardware-verified: copied live to `/etc/systemd/journald.conf.d/`, `systemctl restart systemd-journald`, confirmed active via `systemd-analyze cat-config systemd/journald.conf`. Separately (personal dotfile, not repo-tracked): added `~/.local/bin/plasma-systemmonitor`, a `$PATH`-shadowing wrapper that runs the real binary under `systemd-run --user --collect -p LogRateLimitIntervalSec=30s -p LogRateLimitBurst=20` to throttle that specific app's own log rate — `--scope` does not accept those properties (must be a plain transient service unit), and since the app is single-instance/D-Bus-activated the pre-existing unwrapped process had to be killed and relaunched through the wrapper before the throttle took effect (verified via `/proc/<pid>/cgroup` → `systemctl --user show`). |
| 2026-07-12 | 大哥 | claude-opus-4-8 | hermes-webui-module-2026-07-12 | 0010-hermes-webui-module | (this commit) | New module `dev-ai-hermes-webui` making Hermes WebUI a reproducible part of the image (user asked to "做進系統"). Design (user-approved): ship the INTEGRATION, not a frozen copy — hermes-webui self-updates (git) and is deeply coupled to `hermes-agent` (`api/config.py` puts the agent dir on sys.path + imports `hermes_cli`/`run_agent` in-process, so it runs inside the agent venv), and hermes-agent itself is a user-home checkout not baked into the image; freezing a self-updating home-coupled app into read-only /usr fights all of that. Ships: USER systemd unit (`files/usr/lib/systemd/user/hermes-webui.service`, runs server under `~/.hermes/hermes-agent/venv/bin/python` — 3.11, verified carries yaml 6.0.3 + cryptography 46.0.7; `bootstrap.py --foreground` os.execv's the real server; ConditionPathExists on checkout+agent-venv → dormant not crash-loop), auto-enable `default.target.wants` symlink, runtime oneshot (`aipc-hermes-webui-setup.service` + `setup-hermes-webui.sh`) that resolves primary user (ops-firstboot awk/`runuser -u` pattern), enable-lingers, clones/updates `~/.hermes/hermes-webui` at pinned `exp-v0.52.39` (idempotent, offline-safe, sentinel-by-pin), starts the unit. Moved portal `hermes-webui.yaml` card from agent-orchestrator to this module (correct ownership, no dup id). WHY user-not-system service: system unit execing from /home / reading ~/.hermes is SELinux-denied (init_t→user_home_t: "Failed to load environment files: Permission denied" / result 'resources', same class as venv 203/EXEC) — a live system-unit attempt failed exactly this; user unit runs in user context (proven live in 0009). Verified: sh -n clean on all scripts; pytest 16 passed; `openspec validate 0010 --strict` valid; bootc+ansible renders both exit 0, module included symmetrically (files/post-install/git 1/1/2 each → §4 parity). NOT hardware-verified: needs fresh bootc rebuild+reboot to exercise setup oneshot + auto-enable + linger boot-start (the unit+linger+agent-venv runtime pattern is already hardware-proven by the live 0009 user service). Live machine unchanged this commit (module not deployed; existing ~/.config user service keeps webui running until a rebuild — remove that hand-made override once the module deploys, to avoid shadowing). |
| 2026-07-12 | 副官 | claude-sonnet-5 | mem0-portal-search-facets-fix-2026-07-12 | n/a (no OpenSpec change; bugfix to existing memory-mem0/portal modules) | 7145f67 | User reported mem0 Control Center (127.0.0.1:7080/memory) showing no recent memories. Diagnosed two independent write/read paths (agent-orchestrator internalize + Claude/agents mem0 MCP, both confirmed pointed at the same local pgvector store) and confirmed the actual gap was a real usage lull (no add_memory calls for several hours), not a broken pipeline — verified by a live test add_memory that landed immediately. Found and fixed two real bugs surfaced by that investigation: (1) `PGVector.list()`/`.search()` emit no ORDER BY (confirmed via mem0ai source) so a bare LIMIT/top_k returned arbitrary/insertion-order rows — once a scope passed the UI's default limit, new memories silently stopped appearing under "List all". Fixed in both `aipc_mem0/server.py` (HTTP, used by Portal) and `aipc_mem0/mcp_server.py` (stdio MCP): over-fetch via `_get_all_from_vector_store()`, sort by `created_at` desc, truncate to caller's limit. (2) `Memory.search()` refuses filters without user_id/agent_id/run_id, but the Portal search box is meant to search by content alone — every unscoped search 500'd ("Memory service unavailable"). Bypassed via `_search_vector_store()` directly (no such restriction, PGVector tolerates empty filters) in both server.py and mcp_server.py. Also added `GET /memories/facets` (distinct user_id/agent_id/app_id in the store) + Portal proxy route, and converted `memory.astro`'s three free-text filter inputs into `<select>` dropdowns populated from it; rebuilt `web/dist` -> `static/` per module README workflow. Static: py_compile + ruff clean on all touched Python; astro build clean. Hardware-verified: copied to live-hotfix paths (`/etc/aipc/mem0`, `/usr/local/lib/aipc-portal`), restarted both services, confirmed live against the real store — `GET /memories?limit=N` now returns the true most recent N, `POST /memories/search` works with no scope, `GET /memories/facets` returns real distinct ids. Also wrote a project memory (`mem0-write-path-diagnosis-2026-07-12.md`) documenting the two write paths + the add_memory infer=True latency (3-5min observed via NPU resident-small) as a known-but-unfixed perf characteristic. |
| 2026-07-12 | 大哥+副官 | claude-fable-5 | ornith-apex-scheduler-2026-07-12 | 0012#proposal,1.1-1.7,2.1; 0013#0.1,1.1-1.3,2.1 | 9861161..979446c | User-directed triple: (1) ornith-35b swapped to SC117/Ornith-1.0-35B-MTP-APEX-GGUF APEX-I-Balanced + mmproj vision — hardware-verified via gateway (text 1.0s / tool_calls 1.4s / vision 256px→"紅色" 1.1s, ctx 131072 -np 4 -kvu; embedded-MTP+mmproj+np4 coexist; enable_thinking:false required, default mode leaves content empty). Live-deployed via surgical /etc patch + lemonade pull/load; heretic quant superseded (never deployed). (2) aipc models list/sync fix: marker now records model_id (swap→"stale"→re-pull), checkpoints/recipe/label manifest fields drive user.-prefixed custom Lemonade pulls; new `aipc providers sync` (opencode/ccs/hermes ← /v1/models, auto after models sync) — hardware-verified live (14/8/9 models synced, hermes non-provider keys intact). (3) 0012 memory scheduler for coder-122b (Qwen3.5-122B APEX-Compact 55.1GiB, tier:exclusive): litellm pre-call admission control (budget 96GB, tier preemption, request hold, degrade-open) + idle-release resident re-warm; self-tests+renders green; live litellm restarted with hook. last_use semantics hardware-confirmed host-monotonic-ms (existing idle-release math correct). NOT yet hardware-verified: scheduler preemption/hold/rewarm under real load + 122B first load (55GiB pull was in flight; 0012 tasks 2.2-2.6 pending). Ops note: cleared a wedged lemonade load queue (7-model round-robin persisting after all callers stopped — thrash mechanism evidence for 0012 why.md) by restarting lemonade+litellm per the 0011 止血法; aipc-mem0 briefly stopped and restored (user confirmed unrelated). Commit 979446c includes reconciliation of prior uncommitted 0008 hunks (shared tree). |
| 2026-07-12 | 副官 | claude-opus-4-8 | 0011-lane-consolidation-impl-2026-07-12 | 0011-assistant-lane-consolidation#4-6 | 8ee6cc6 | self-improve idle+AC preflight (resident-set-aware, health v1) + OnBootSec dropped + max 15->5; render-verified only, commit 1/2 (rest of 0011 waits on shared-WIP tree) |
| 2026-07-12 | 大哥+副官 | claude-fable-5 | ornith-apex-scheduler-2026-07-12 (follow-up) | 0012#2.2-2.5 | (this commit) | 0012 scheduler hardware verification completed on the live box. coder-122b end-to-end: chat 200 via gateway, decode 26.8 tok/s / TTFT 1.2s. Preemption verified twice (held request evicted idle ornith within 2s of slot free; lemond queue stayed empty = thrash mechanism gone at the gateway); hold verified (122B request held while ornith in_use, admitted after); coexistence verified (qwythos admitted into leftover budget beside loaded 122B); release+rewarm verified (daemon no-ops while exclusive loaded, then re-warms assistant-gemma → coder-agentic to the 0011 resident set; updated daemon live-hotfixed to /etc/aipc/llm-lemonade/). Three hardware findings folded back: budget 96→80 (non-model working set measured ~47GB); recipe MUST be pinned via recipe_options.json BEFORE a big model's first load (unpinned ctx 262144 load exhausted GTT → vk DeviceLost cascade: llama-server SIGABRT, zen SIGSEGV, lemond SIGSEGV, kwin GL_CONTEXT_LOST, plasmashell wedged — the "KDE 一直掛掉" the user hit; box rebooted); --no-warmup required in the 122B recipe (warmup measured 8.5min > global_timeout 600s ready-wait → timeout-kill-retry loop; repo configure-lemonade.sh now sets 1800, live /usr copy resets 600 each start until rebuild). 2.6 idle-hour soak pending until 0011 lands (lanes still hammer ornith when idle). |
| 2026-07-12 | 副官 | claude-sonnet-5 | rewarm-removal-2026-07-12 | 0012-memory-scheduler-coder-122b#rewarm-removal | 0b0e35b | User-directed removal of idle-release rewarm: lemonade-idle-release.py drops `_rewarm_candidate` + the run() rewarm branch + now-unused `_load_model`/`BUDGET_GB`/`HEADROOM_GB`/`_mem_available_gb`/`DEFAULT_LOAD_PATH`/`Callable`+`os` imports → daemon now only unloads, never reloads (pure on-demand). models.yaml: qwythos-9b/assistant-gemma/coder-agentic lose `tier: resident` and each gain `idle_unload_after_s: 1800` (30min, avoids reload+prefill thrash within a working session); coder-122b `tier: exclusive`, resident-small `pool: npu`, ornith 900/coder-compact 300 untouched. Steady-state resident set shrinks 4 Vulkan models → only resident-small (NPU-FLM pinned). Static+render-verified (self-test pass, ruff clean, yaml ok, pytest test_models_loaded_unload 12 passed unchanged, bootc+ansible renders exit 0); NOT hardware-verified. scheduler_hook.py admission/preemption/hold left untouched per spec — note its runtime tier read now sees the 3 as floating (no longer eviction-protected), consistent with intent. |
| 2026-07-13 | 大哥+副官 | claude-opus-4-8 | coder-agentic-mtp-swap-2026-07-13 | 0012-memory-scheduler-coder-122b#swap-admit | (this commit) | User-directed: (A) replace coder-agentic's HauhauCS Qwen3.6-35B-A3B Aggressive Q4_K_P with SC117 native-MTP (heretic, APEX I-Balanced) after a fair A/B, and (B) make other LLM models auto-unload when RAM is tight. A/B (副官 sonnet-5, llama-server -np 1 temp 0 single-stream, hardware): native 20-layer MTP + `--spec-type draft-mtp --spec-draft-n-max 2` = 78.5 tok/s vs 57.7 no-spec (+36%) and vs 61.2 old Aggressive (+28%), 87.5% draft acceptance, coherent uncorrupted Python — unlike ornith's 1-layer MTP (-19%, rejected). This is the 'fresh measured win' the earlier MTP-rejection note demanded. DOWNLOAD/REGISTER was the hard part: HF migrated the repo to Xet content-addressed storage, so naive HTTP (curl AND lemonade's own pull) fetch size-correct/content-wrong bytes (consistent d4f7590f != LFS etag 782636c4 — stale upstream etag from the Xet migration). Must fetch via the hf_xet client (`hf download`, run as user not sudo, to disk not tmpfs). lemonade load then still looped re-downloading: root cause = lemonade's `.download_manifest.json` in the snapshot dir is its 'download-incomplete' marker; while present, ModelManager doesn't count the model downloaded and passes the REPO DIR (not the .gguf) to llama-server -> 'No such file'. Fix = reproduce hf-CLI's completed cache layout (blobs/<etag> + snapshots/<commit>/<file> symlink + refs/main, DELETE the manifest), restart lemonade -> counted downloaded, loads. ctx 131072 warmup >3min so `--no-warmup` added to the recipe. (B) scheduler_hook.py: eviction of other LLMs under memory pressure already existed (0012 pick_victim+post_unload); the gap was that when the ONLY remaining blocker is non-LLM memory the scheduler can't evict (ComfyUI's resident 28GB diffusion model, which the user keeps), it held until HOLD_TIMEOUT and failed the request. Added swap-backed admission: once no evictable LLM remains, admit iff free swap can absorb the model AND MemAvailable stays above RAM_FLOOR_GB (4) — bypasses ONLY the real-memory gate, never the BUDGET_GB ledger. User directive: swap ok, OOM not, keep ComfyUI. models.yaml: coder-agentic -> model_id Qwen3.6-35B-A3B-heretic-NativeMTP-APEX-I-Balanced, checkpoints.main SC117 repo:file, size 24.2, recipe_options `-np 1 -kvu --spec-type draft-mtp --spec-draft-n-max 2 --no-warmup` (native MTP requires n_parallel=1). litellm config.yaml: coder-agentic routes to the new model_id. HARDWARE-VERIFIED end-to-end: model loads via lemonade (51 tok/s warm, coherent code, enable_thinking:false ok, MTP spec active in llama-server args); litellm coder-agentic request swap-admitted (no hold) despite ComfyUI 28GB -> loaded via swap -> correct fib() code (5.2 tok/s incl cold load). Static/render: scheduler self-test pass (added swap-admit + budget-not-bypassed + hold-when-swap-too-small cases), ruff clean, both YAML valid, bootc+ansible renders exit 0, `aipc providers sync` 14/8/9. DURABILITY GAP (documented, not fixed): `aipc models sync` will re-hit the Xet/stale-etag loop for this model (lemonade pull can't Xet); reproducible provisioning needs an hf_xet-based path — follow-up. Live recipe_options.json pinned + cache structure are live-hotfixed; a fresh bootc rebuild would need the hf_xet provisioning to re-place the gguf. Old Aggressive Q4_K_P gguf kept on disk for rollback. |
| 2026-07-13 | 大哥 | claude-opus-4-8 | coder-agentic-mtp-swap-2026-07-13 (follow-up) | 0015-xet-model-provisioning#proposal | d4f8831 | Opened OpenSpec change 0015 for the durability gap flagged in ce8a830: HF Xet-migrated repos with a stale LFS etag can't be provisioned by `lemonade pull` (naive HTTP fetches content-wrong bytes; verify-loops on the stale etag). 0015 proposes a `provision: hf_xet` manifest flag so `aipc models sync` fetches such models via the hf_xet client into Lemonade's cache in the completed layout (blobs/<etag> + snapshot symlink + refs/main, no download manifest), skips lemonade pull, idempotent, validates by load not etag. why/what/how/tasks + ai-runtime spec delta; `openspec validate 0015 --strict` green. Proposal only, not implemented. |
| 2026-07-13 | 副官 | claude-opus-4-8 | xet-provisioning-impl-2026-07-13 | 0015-xet-model-provisioning#1.1-1.3,2.1-2.2 | (this commit) | Implemented 0015. models.py: `ModelEntry.provision`+`revision` fields; `pull_command` returns None for `provision: hf_xet` (would loop on the stale LFS etag); `xet_provision_commands` builds the sequence — `env HF_HOME=<user staging> hf download REPO FILE --revision <commit>` (hf_xet client, as invoking user, staging never on tmpfs) then `sudo python3 -c <_XET_MATERIALIZE_SNIPPET>` reflinking the completed HF cache layout (blobs/<etag> + snapshot symlink + refs/main) into Lemonade's root cache and purging `.download_manifest.json`/`*.partial` (the presence of that manifest is what makes ModelManager hand llama-server the repo dir → 'No such file'); raises ValueError on missing revision/bad checkpoint; dedupes materialise per repo. `sync_pull` dispatches hf_xet entries to it, then pins recipe_options + writes the marker exactly as the pull path. on_disk_status UNCHANGED — the marker mechanism already handles present/stale and aipc never hashes the etag (the etag re-hash that loops is Lemonade-internal, sidestepped by the no-manifest completed layout). models.yaml: coder-agentic gains `provision: hf_xet` + pinned `revision: dc1e5523…`. Static+render: +10 unit tests (44 pass total, was 30 in test_models; pull_command None, xet argv, missing-revision/bad-checkpoint raises, per-repo dedupe, sync_pull fetch→pin→marker, fetch-failure-not-marked, and a real subprocess run of _XET_MATERIALIZE_SNIPPET asserting the completed layout+manifest purge); ruff clean; bootc+ansible renders green; `openspec validate 0015 --strict` valid. HARDWARE (partial, non-destructive — user actively using coder-agentic so no cache wipe / 24GB re-download): aipc is editable-installed so the code is live; ran _XET_MATERIALIZE_SNIPPET against the REAL home hf_xet download into a scratch dir → produced the exact completed layout (24GB blob reflinked, correct relative snapshot symlink, refs/main=commit, manifest purged); the hf_xet download step itself is already hardware-proven (it is how the live gguf was fetched). Also fixed a real correctness bug the swap left: the LIVE /etc/aipc/models/models.yaml (mounted into litellm as the scheduler's manifest) was badly stale — coder-agentic still `Qwen3.6-…-Aggressive-Q4_K_P` size 21.8, so the 0012 scheduler tracked the WRONG model_id for coder-agentic's loaded state; surgically replaced that one entry (model_id→heretic-NativeMTP, size→24.2, +checkpoints/provision/recipe_options) → scheduler+`aipc models list` now correct (present). REMAINING: 1.4 (ensure hf_xet in a fresh image's primary-user env) + 3.1 (full `aipc models sync` end-to-end with a real 24GB re-provision on a quiet box). NOTE: the rest of the live models.yaml is broadly stale (predates ~all recent model work) — separate deployment-hygiene follow-up, flagged to user, not touched here. |
